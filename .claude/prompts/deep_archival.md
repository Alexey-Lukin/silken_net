# Промт: Глибока архівація / цементація 00_07

> **Що читати на КОЖЕН пункт (≈10 kB): «⚡ Шпаргалка» + «✅ Чеклист».** Решта — раз на сесію. ⛔ **Хто не прочитав файл ЦІЛКОМ у цій сесії, читає цілком зараз:** поділ економить ПОВТОРИ, не перший прохід — виміряно, що прочитаний цілком файл і нуль прогнаних свіпів співіснують в одній сесії, тож скорочення першого читання зробило б цей рядок четвертим текстовим домом правила, яке й так не спрацьовує.

> **Як використовувати:** встав цей файл (або «виконай `.claude/prompts/deep_archival.md`») коли повертаєшся до кампанії архівації `docs/00_07_Action_Plan_Tracker.md`. Це **дім** методу; skill `ssot-maintenance` лише посилається сюди. Спілкуйся українською.

> **Сиблінг, і межа між нами вирішує, який файл відкривати:** [`module_restructure.md`](module_restructure.md) — той самий ярус дисципліни, але його обʼєкт **ЦІЛА СТОРІНКА** (винесення факту у власний док, злиття двох, розчинення, звільнення/заселення номера модуля). Тут обʼєкт — **ОДИН пункт `00_07`**. Спільний у нас лише примітив `migrate-first`; кроки різні, і плутати їх дорого: свіп по одному ID не шукає самопосилань і констант, ключованих на НОМЕР доку, а свіп по сторінці не питає «які пункти трекера втрачають свою СЕКЦІЮ». Норму обох тримає [`00_06 §4`](../../docs/00_06_SSOT_Documentation_Standard.md) (критерій + `Ruthless Pruning`), не ці файли.

---

## ⚡ Шпаргалка — читай ЦЕ перед кожним пунктом

> Решта файлу — обов'язкова, але один раз на сесію. Ця секція — те, що мусить бути в голові в **момент правки**, і вона тут нагорі саме тому, що раніше жила в кінці (чеклист) і в третьому рівні вкладеності (червона лінія №1) — тобто рівно там, де її не читають.

**Цикл на один ID** (по одному, не спішачи): широкий аудит (ID + семантика, ВСІ доки/код/spec, forward + inbound рефи) → verify у реальному коді (кожне ✅ + `git log`) → CANONIZE migrate-first → cement-trim / archive → гейти (`refs`+`tracker`) + zero-loss set-diff → окремий commit.

🔴 **ТРИ ВИХІДНІ СВІПИ архівації — у шпаргалці, бо в тілі файлу (Фаза 3-зворотна й Фаза 5-третя) їх систематично не доходять.** Архівуючи ID, питай не лише «на кого чекав Я», а всі напрямки НАЗОВНІ, і кожен має команду:
- **хто чекав на МЕНЕ:** `grep -n "<ID>" docs/00_07*.md` і читай КОНТЕКСТ кожного хіта на слова блокування (`⛔` · `ЗАБЛОКОВАНО` · `до присуду` · `чекає на`) **⊕ гліф `🔗`** — у нього словника немає;
- **хто шле СЮДИ по стан:** `bash .claude/hooks/memory_gate.sh --stale-state` — джерело лежить ПОЗА git, тож жоден гейт репозиторію цього не бачить.
- **чи не лишився я в `🚦 Critical Path`:** `ruby scripts/tracker_report.rb --critical-path` — рядок несе писаний інваріант «закритий елемент ВИПАДАЄ з рядка», і його не читає ЖОДЕН гейт. 🔴 **Звіт ЗАНИЖУЄ, бо не бачить слеш-стиснених списків** (у `INF.11/12/13/14/15/16` пʼять ID існують лише як `/12`, `/14`… — ані звіт, ані per-ID свіп їх не торкаються): після архівації грепни ще й `/<номер>` та `, <номер>` по секціях `🚦`/`⛓️` й продивись очима. **Питання до КОЖНОГО рядка `🚦`/`⛓️` після будь-якого присуду: він описує ВІДКРИТЕ — чи переказує закрите?** ⚠️ Це advisory НАЗАВЖДИ: сира точність осі 29 %, звужена форма виведена in-sample — звіт дає КАНДИДАТІВ, вердикт дають очі. Інстанси й числа → памʼять `log_task_execution_workflow`.
⚠️ ЖОДЕН із трьох не має CI позаду — за побудовою, — тож єдиний носій це ця стрічка (перелік вище і є множиною; числа тут свідомо немає — воно вже раз протухло, коли свіпів стало три). ⛔ **Прочитати файл на початку сесії — НЕ означає прогнати свіпи:** виміряно на собі, що прочитаний ЦІЛКОМ файл і нуль прогнаних свіпів співіснують в одній сесії.

**Червоні лінії founder'а** (порушення будь-якої = зіпсована сесія):
- 🔴 **grep-hit ≠ канонізовано.** Перед схлопуванням ЧИТАЙ (`Read`, не grep-snippet) **повну** секцію в **КОЖНОМУ** доку, де факт живе, + звір код↔канон. «Все ідеально, нічого не знайшов» — **підозріло**, копай глибше. Ця червона лінія стоїть **без гейта позаду** (⚠️ не «єдина» — цей файл нижче називає ще дві ungated-осі; унікальність знято 08-22, бо саме вона й протухла): zero-loss set-diff сам grep-based, тож факт, present-по-токену але вихолощений по суті, проходить зеленим.
- 🔴 **ОДНА Edit → верифікуй → наступна.** Жодних батчів, навіть non-glue.
- 🔴 **Присуд/вердикт і живі `[ ]`-residual не зрізати.** Done-ID тоншити до однорядкового визначення, **не** видаляти (канон реферить по ID; verify-inbound ПЕРЕД видаленням).
- 🔴 **Дзеркала** (⛓️ Гейт-кластери · skill / memory / CLAUDE.md) синкати в **ТОМУ Ж** проході per-ID, не на фінальному sweep.
- 🔴 **Переносячи текст МІЖ КОНТЕКСТАМИ (таблиця ⊥ проза ⊥ код-блок ⊥ blockquote), грепни діф на осиротіле ЕКРАНУВАННЯ — воно калічить речення МОВЧКИ, і жоден наш гейт цього не бачить за побудовою.** `git show <sha> | grep -nE '^\+.*\\$'` плюс око на `\|` · `\_` · `\*` · `` \` `` у перенесених рядках. 🔴 **І контекст-ціль буває НЕ markdown — а відколи Фаза 4 наказує мігрувати спростування в МІСЦЕ ДІЇ, найчастіша ціль є кодом.** У C-блок-коментарі речення обриває будь-яка пара `*` `/` усередині токена (виміряно 2026-09-08: рядок ніс `MX_` з зіркою й слешем перед іменем функції — коментар закрився, файл перестав компілюватись); у shell-heredoc те саме роблять `$` і зворотні лапки, у YAML — двокрапка з пробілом. **Рефлекс: мігруючи прозу в код, назви СИНТАКСИС цілі й прогони компілятор/парсер ЦІЛІ, не гейт доків** — `docs:check_refs` тут зелений за побудовою, бо предмет уже не його. ⊕ **Друга половина класу — ВІДНОСНІ рефи всередині перенесеного** (`§6`, «вище», «нижче»): їхня ціль лишилась у ДЖЕРЕЛІ, тож у новому домі вони резолвляться хибно або нікуди, і `docs:check_refs` мовчить обабіч — він судить лише форму `NN_NN §X`. 🔑 Дзеркало правила «міграція пише твердження СИЛЬНІШЕ за джерело»: тут вона пише МЕНШЕ, синтаксисом, і саме тому невидима. Інстанс → памʼять `log_verify_canon`.
- 🔴 **✅-БЛОК — НЕ ДОКАЗ ЗАКРИТОСТІ: відкритий дефект уміє жити ОДНИМ реченням усередині нього, і саме там trim його не бачить.** **Рефлекс: перш ніж тоншити ✅-блок, витягни з нього КОЖНЕ твердження й спитай не «чи це зроблено», а «чи це ТВЕРДЖЕННЯ ПРО ЗРОБЛЕНЕ» — усе інше (⚖️ · ⛔ · «лишається» · «але») винось ногою ДО зрізу, не після.** ⚠️ Ловиться лише читанням ТІЛА: грошовий дефект, чиє єдине входження в дереві — фраза всередині ✅-булета, для гейта не існує. Інстанси → памʼять `reference_deep_archival_prompt`.
- 🔴 **Гард на ДОВЖИНУ вирізаного блоку рятує від зрізу того, що вже стисле — постав його ДО заміни, не після.** Того ж дня обидва блоки, які «виглядали» кандидатами на trim, виявились 1225 B і 2690 B із 27 KB пункту, і саме в них сиділи дві тези без іншого дому. Скрипт зупинився на `abort unless len.between?(…)` двічі поспіль. **Ціна відмови називається вголос: пункт лишається великим — але його вага є ратифікованими присудами, не хронікою, і «27 KB» саме по собі підставою для зрізу не є.** ⊕ Дзеркало: справжня користь того проходу була не в байтах, а в бездомному дефекті й хибній адресі (`00_04 §5` замість `§2`), яку обидва блоки цитували роками — тобто **trim окупається ПОШУКОМ, і коли він дає нуль байтів, це не невдача проходу**.
- 🔴 **У де-дуплікаційному проході найдорожча помилка — прийняти за дубль те, що є ЄДИНОЮ АДРЕСОЮ.** **Спільна прикмета всіх спійманих інстансів: вони ВИГЛЯДАЛИ дзеркалом, бо стояли в чужому домі — а чужий дім і відсутність дому статично не розрізнити.** ⛔ Тож перед КОЖНИМ зрізом грепай предмет по ВСЬОМУ дереву, не лише в гаданому домі: нуль зовні = дірка, і зріз там є ВТРАТОЮ, а не прибиранням. ⊕ Дзеркало для позитивного боку: коли предмет справді бездомний, лік не «зрізати», а ПЕРЕСЕЛИТИ. Інстанси → памʼять `project_doc_t98_perimeter_sweep`.

Повний по-пунктний чек-ліст для звірки **перед комітом** — «✅ Чеклист на пункт» у самому кінці файлу.

---

## 🎯 Місія

Кампанія цементації SSOT: **одночасно цементувати код + канон-доки + 00_07**. На кожен ID, який «дозріваєш», пройти **всі** місця в коді й документах, де він зустрічається, переконатися, що (1) важливе **канонізоване** (не губиться), (2) в доках узгоджено, (3) в коді узгоджено, (4) сміття підчищене. Це задача, яка робить дерево SSOT справді єдиним джерелом істини.

**Дух роботи (цитати власника):**
- «Йдемо по одному за раз, не спішачи. Працюємо й з тими, де **багато зробленого**.»
- «Там всюди **кролячі нори** — ретельно копай. Глибоко рийся, без обмежень, ми не спішимо.»
- «Там всюди є **нитки як в лабіринті**, є **хлібні крихти** щоб тобі допомагати.»
- «Уважніше і ретельніше по SSOT шукай — не зупиняйся на одному документі.»
- «Інформація повинна бути **канонізована**, щоб важливу не втратити.»

---

## 📐 Принципи (НЕ порушувати)

1. **One-Home (одна річ — один дім).** Кожен факт має ОДИН канонічний дім (`00_06 §2` registry); решта доків **реферять**, не дублюють. Дубль = drift surface (саме так народжуються колізії).
2. **Migrate-first, zero content-loss (обидва напрями).** Канонізуючи: наповни канон-дім ПОВНОЮ субстанцією + verify present **ПЕРШ** ніж різати джерело. **Тоншаючи/колапсуючи 00_07-блок: ПЕРШ перевір (читанням канону), що факт УЖЕ канонізований** — інакше видалиш незбережене. ⚠️ Founder-правило (2026-06-09): «перед тим як колапсувати **обов'язково перевір чи канонізоване**, а то наудаляємо те що не треба». Стосується й **Присуду/вердикту** (аналітичний висновок власника) — його essence лишай у тонкому резюме + канон-pointer, не зрізай мовчки.
   - 🔴🔴 **grep-HIT ≠ канонізовано (founder 2026-06-09b — НАЙВАЖЛИВІШЕ):** «ти якимсь пошуком дивишся чи є згадка … ти хоч читаєш? чи просто схлопуєш?» + «углублятись в кролячі нори ти сильно не захотів». Знайти ОДНУ згадку в ОДНОМУ доку й схлопнути — **НЕ** verify. **ЧИТАЙ ПОВНУ секцію** (`Read`, не grep-snippet) **через ВСІ доки, де факт живе** (зазвичай >1: канон + sibling + код) + **звір КОД↔канон** + підтверди **ПОВНОТУ кожного нюанса** + cross-doc консистентність. Не схлопуй на першому hit. **Якщо все «ідеально, нічого не знайшов» — це підозріло → копай глибше.** (R16-доказ: rigorous re-read знайшов 2 пропуски — §4.3 hardcoded `ρ+17` vs derived; SEC.12 «4 host-тести» volatile — які grep-confirm проґавив.)
3. **Ruthless Pruning (зворотний бік).** Мертве/спростоване/чисту історію реалізації (конкретні фікси, CI-run#, volatile counts) → **git тримає, не канон**. Живе *design-justification* («чому X, а не Y») → лишається в каноні (1-2 рядки + pointer). Межа: мертвий артефакт vs живе обґрунтування.
4. **00_07 = тонкі вказівники.** Кожен пункт: meta-line `**PN** · WHO · STAGE · → канон-реф` (**WHO** = хто робить, **STAGE** = лайфсайкл) + 1 рядок Присуду. 🔴 **Повний перелік токенів обох осей НЕ дублюється тут — він живе в `00_07`-інтро §«Форма пункту», і читати треба ТАМ.** Тут доти стояв власний перелік, і він протух, бо **ніщо не звʼязує правку `00_07` із правкою його опису** — рівно та форма, яку описує анти-патерн про дзеркала. cement-trim жирного host-done пункту → STAGE `🟢`, bench-residual у `[ ]`. Інстанс → памʼять `log_tracker_item_form`.
5. **Проза ховає недороблене.** `[ ]` + STAGE `⚪`/`🟡`/`🟢`/`🔗` = відкрите (не done). Розрізняй: **done** → канон + тонкий pointer; **відкритий residual** → лишається в 00_07. НЕ архівуй пункт, у якого жива частина (`[ ]`-residual). WHO-тег у residual = хто його закриє.
6. **cement-trim vs archive.** Частково-done (реальний residual) → **cement-trim**, форма (**Universal-лід, founder 2026-06-14**): тіло ЗАВЖДИ веде з **`- **Стан:**`** = Присуд/вердикт essence + канон-pointer (НЕ зрізати; **НЕ «✅ X»-лід** — однорідність трекера) + **збережені відкриті `[ ]` residual** (вертикально; НЕ verbose `[x] ✅ 🤖` done-блоки — done-impl живе у git+каноні; ≥2 інлайн `· [ ]` ловить HARD guard `inline_residual_runon`, а сам Стан-лід — HARD guard `verdict_lead_violations` [DOC-T.19, обидва guards HARD з 2026-06-14 — трекер однорідний]) (**top-level, flat — БЕЗ «Лишається:»-обгортки чи відступу**; сам `[ ]` = що лишилось; list-форма стандартизована 2026-06-14). Прибери volatile counts. Повністю done (єдиний `[ ]` = optional/deferred, не зобов'язання) **АБО orphan-ID** (resolved+cited у коді/каноні, але без 00_07-рядка) → **archive** (рядок у §🗄️ + для #### видали з §модуля; для orphan просто додай §🗄️-рядок — крос-реф цілісність, OBS.1-патерн).
7. **Хірургічність — ОДНА правка за раз.** Сміття чистимо точково, не масово. **Роби ОДНУ Edit → ВЕРИФІКУЙ → тоді наступну** — навіть коли non-glue (founder 2026-06-09 спіймав 6-edit BLOCKER-4 батч в одній відповіді: «наголосив багато раз робимо по одному, ти вирішив робити одразу шість»). Glue-механіка (чому ще небезпечно): leading-`\n` removals (`new=''`) склеюють сусідні рядки → елементи зникають з парсера → гейт хибно зелений (non-empty replacements безпечніші, але дисципліна — все одно по одній).
8. **Без volatile counts.** Не лишати «30 RSpec / 17 host / 264 рядки» — дрейфують щокоміту. Заміни на «RSpec+host покрито» (реф на suite). Wire/spec-константи (21-байт, DR0..DR19, 0.47F) — design-факти, лишаються.

---

## 🔬 Процес (на кожен ID, по одному, не спішачи)

### Фаза 0 — Інструкції (раз на сесію)

- ⛔ Стартовий ритуал тут НЕ дублюється (`00_06` · скіл `ssot-maintenance` · `doc_structure_map.rb` — усе вже в наборі, а перші 90 рядків `00_06` є оголошеним входом: реєстри нижче відкривають ЗА АДРЕСОЮ, не скануючи). **Не пройдено — мінімум перед першим пунктом: зелений `ruby scripts/docs_band.rb`** (baseline знімається ТІЄЮ Ж формою, що й фінальний вимір, інакше порівнюєш різні прилади) **+ відкритий реєстр домів `00_06 §2`**, бо без нього Фаза 4 не має входу.

### Фаза 1 — Вибір пункту

🔴 **ДИСКРИМІНАТОР ВИБОРУ — не розмір, а чи СТАЛА робота.** Подвоюються за дві доби рівно ті пункти, де робота ЙШЛА; закриті-й-гейтовані лишаються байт у байт. **Отже трим гарячого пункту оплачується назад за 48 годин, а трим пункту, чия робота СКІНЧИЛАСЬ, тримається.** ⚠️ І це не привід не тримати гарячі — це привід **не міряти успіх проходу байтами**: у гарячому пункті зріз купує ЧИТАБЕЛЬНІСТЬ на час роботи, у зупиненому — довговічний дім. Ціль обирай так: найбільші серед тих, чия робота щойно ЗАКІНЧИЛАСЬ і лишила по собі хроніку; пункти з живими ⚖️, які ще судяться, тримай легко або не чіпай. Вимір регроуту → памʼять `reference_deep_archival_prompt`.

🔴 **ДРУГА ПОЛОВИНА ДИСКРИМІНАТОРА, без якої перша обирає не те: «робота стала» ⊥ «зміст ЗМІГРОВАНИЙ», і читаються вони з різних місць.** Стан роботи видно з гліфів residual'ів — дешево й спокусливо; чи має зміст другий дім, видно лише з ЧИТАННЯ канону, коду й скілів. **Причина структурна: свіжа робота ще не встигла осісти в домах, тож чим НЕДАВНІШЕ пункт зупинився, тим ГУСТІШИЙ він незмігрованим — і саме такі пункти дискримінатор «робота стала» вибирає першими.** 🔑 Отже порядок обовʼязковий: **аудит покриття → міграція → аж тоді трим**, а якщо на міграцію немає бюджету — **не тримай узагалі**: пів-мігрований трим є найгіршим виходом із трьох, бо знищує рівно те, заради чого пункт вели. Вимір → памʼять `reference_deep_archival_prompt`.

🔴 **І найдорожчий бездомний — це не зміст, а СПРОСТУВАННЯ; трим тоді не просто губить факт, а лишає ЧУЖИЙ дім активно брехати** (виміряно 2026-08-31 на `OPS.28`). Пункт ніс переміряне звуження власного класу — «CI не є носієм для власних інструментів» виявилось зашироким, бо `pre-push` таки ганяє rubocop, а `docs.yml` має `scripts/**` у своєму фільтрі. Саме твердження при цьому стояло в `guard-craft` **як виміряний факт усередині портативного рефлексу**, і спростування жило лише в трекері. Зріз лишив би скіл вчити хибного — тобто не «втрата», а РЕГРЕС: читач дістав би впевнене неправдиве правило замість жодного. ⚠️ Клас невидимий для звичайного аудиту покриття, бо той питає «чи є в цього факту дім», а тут дім Є — і саме він і є дефектом. **Тож до питання «що живе ЛИШЕ тут» додай друге: «чи не спростовує щось із цього пункту твердження, яке ДЕІНДЕ стоїть як чинне»** — і мігруй спростування ПЕРШИМ, ще до контенту. 🔴 **І це не «втрата на полицю», а ПРОГРАМУВАННЯ хибного фіксу, бо клас має вбудовану асиметрію ЧИСЛА: виправлення завжди ОДНЕ, а виправлюване — МНОЖИННЕ.** Знявши звірку, ти лишаєш читача з рештою згадок — а та решта СТАРІША за перемір, тож наступний, хто «наведе лад за домінантою», впевнено поверне спростоване значення. Виміряно 2026-08-31 на `INF.27`: мій же трим зняв рядок «HTTP-статус тут `401`, а `403` стоїть ЛИШЕ в ТІЛІ відповіді» — і вже наступний читач запропонував «виправити» `401`→`403`, тобто заключити детектор на рядок, який ніколи не приходить. **Тому спростування мігрує не в найближчий канон-абзац, а в МІСЦЕ ДІЇ** — туди, де стоїть саме те число, що спокушає «виправити»: сусідній дім гасить факт, але не гасить спокусу. ⚠️ Практична форма: аудит покриття добре паралелиться read-only агентами (по пункту на агента, питання «що живе ЛИШЕ тут»), а от вердикт і правки — ні; і памʼятай режим брехні саме цього брифа — **домінує хибний негатив «дому немає»**: з трьох перевірених мною тверджень одне було вигаданою бездомністю (правило справді стояло в каноні), тож кожне «NOWHERE ELSE» пінуй грепом сам, перш ніж на нього мігрувати. ⊕ Перемір 2026-09-02 (пʼять агентів по 3-4 пункти, ~20 заяв NOWHERE): вигаданих ДВІ (ціна форми canopy — стояла в самому маніфесті; «природний виконавець» S6.1 — у скілі бракувало лише ВКАЗІВНИКА), тобто точність ~90% і перевірка читанням лишається обовʼязковою; зате в ЗВОРОТНИЙ бік агенти не помилились жодного разу — усі девʼять знайдених спростувань (канон/код-коментар, що стверджує вже зняте) підтвердились, і саме вони були найдорожчим урожаєм проходу.


- За схемою (зазвичай firmware `FW.*` по черзі). Бери де **багато зробленого** (`✅`-щільні) — cement-trim або archive.
- НЕ «за номером наосліп» — дивись на сам пункт + його хрест-рефи.

### Фаза 2 — ШИРОКИЙ аудит (не зупиняйся на одному документі!)
- **ID repo-wide:** `grep -rn "FW\.NN\b" docs/ firmware/ app/ spec/ scripts/ lib/ .github/`.
- **Семантичні терміни** (не лише ID — робота розкидана!): назви сервісів/класів, константи, opcode, wire-маркери, HKDF-info — по **ВСЬОМУ** `docs/` + код + тести. Канон часто перефразовує — grep по токену це пропустить. ⚠️ Тут доти стояв діапазон, обірваний на тодішньому верхньому канон-доці, і він **виключав два файли, що реально несуть tracker-ID** — `00_00_SSOT_Index.md` (10 згадок) і `00_08_Beyond_TRL9_Planetary_Roadmap.md` (14): звуження перетворювало «широкий аудит» на систематичний хибний негатив рівно там, де Фаза 2 обіцяє ловити dangling-рефи. Не пиши діапазон узагалі — множина канон-файлів росте, а діапазон гниє мовчки.
- **Forward-рефи:** на що пункт посилається (канон-дім + sibling-доки).
- **Inbound-рефи (додано):** хто посилається **НА** цей ID (інші 00_07-пункти, канон-доки, код-коментарі, `[`00_07` — ID]`). Trim не сміє лишити dangling-реф деінде.
  - 🔴 **Але ЛІЧБА inbound-рефів систематично ЗАВИЩУЄ легітимність цілі, і завищує тихо: реф резолвиться, тому виглядає доказом, що ціль потрібна — а він може бути про ІНШЕ.** Виміряно 2026-09-04 (`DOC-T.98`): секцію канону тримали десять вхідних рефів, і **сім із них були осиротілі ще до будь-якого зрізу** — усі цитували її по обґрунтування, якого там не було ЖОДНОГО разу (ключових слів у секції — нуль), а справжній дім весь час стояв в іншій секції того самого файлу. Тобто зріз не «осиротив би сім рефів» — він **виявив** сім давно осиротілих. 🔑 Асиметрія, що робить це живучим: хибний реф ніхто не переміряє, бо резолвер зелений, а автор наступного рефа копіює адресу в сусіда — тож помилка **розмножується сама**, і чим більше копій, тим переконливіше ціль виглядає потрібною. **Команда, а не окомір: для кожного inbound-рефа грепни ПРЕДМЕТ, по який він прийшов, у самій цілі** (`sed -n '<span>p' <ціль> | grep -i "<ключове слово рефа>"`) — і читай нуль хітів як «реф не про цю адресу», ніколи як «я погано грепнув». ⊕ Наслідок для рішення: «кого можна стоншити» вирішує не КІЛЬКІСТЬ рефів, а скільки з них справді приземляються.
- **Крос-рефи крос-рефів** — транзитивне замикання в обидва боки.
- Семантичний near-dup (де grep сліпий): `ruby scripts/content_dup_audit.rb --near`. ⚠️ **Його сліпота ДВОВИМІРНА:** cross-doc + line-Jaccard, тож він не бачить (а) дублі ВСЕРЕДИНІ одного документа й (б) крос-мовні EN↔UK пари. Зелений прогін означає «нема cross-doc verbatim», ніколи «нема дублів». ⛔ Крос-мовну гілку виміряно ВУЗЬКОЮ (точність ~37 %, нуль на парі, заради якої будувалась) — тож другий інструмент тут не анкери, а ЧИТАННЯ: полюй за спільним рідкісним іменником (персона · джерело · тех-термін), не за токеном ID. ⚠️ Самі ID у прикладах не цитуй: `code_tracker_id_check` сканує `.claude/**` і не відрізняє ілюстрацію від живого рефа. Інстанси → памʼять `feedback_dedup_id_search`.
- 🔴 **Два способи, якими ID-свіп тихо промахується, обидва відтворювані:** (а) **межовий регекс `(^|[^0-9A-Za-z])ID` мовчки падає на двоцифрових ID у BSD-grep** (macOS) — бери `grep -F`, а не «розумну» межу; (б) **скорочені списки через слеш/кому обходять `\bID\b`** — реф `UNI.1/8/9` несе `UNI.8` лише як `/8`. Після літерального проходу окремо грепни форми `/8`, `8/`, `, 8` і продивись очима. **Остаточний all-clear = grep повертає ЛИШЕ канонічні доми самого ID** і нічого більше. Інстанси → памʼять `feedback_dedup_id_search`.
- **Orphan-ID sweep (додано 2026-06-09):** для родини префіксів (`SEC.`/`FW.`/`E.`…) — `comm -23 <(референси з коду/доків `sort -uV`) <(00_07-доми: лише `#### ID` + `\| ID \|` table-rows, НЕ проза)`. Referenced+resolved фікс **без** 00_07-рядка = **orphan** → додай §🗄️-рядок (крос-реф цілісність). Так спіймані **SEC.5** (Chainlink HMAC) + **SEC.8** (ECB Restoration). ⚠️ Проста `rg 'ID' 00_07` ловить і ПРОЗОВУ згадку (хибний «дім») — фільтруй на `####`/`\| ID \|`.
- **Re-audit раніше-зацементованих (founder 2026-06-09):** періодично перепроходь УЖЕ-закомічені пункти цим самим глибшим стандартом — перший прохід міг пропустити (так знайшлись SEC.5/SEC.8 orphans + **DOC-T.1** stale-resolved-«blocked»).

### Фаза 3 — VERIFY у реальному коді (додано: git-verify)
- **Читай реальний код**, не лише ID/прозу. Кожне `✅` твердження звір з кодом (логіка, не коментар).
- Verify wire-format / контракти **byte-accurate** docs↔code.
- **git-verify DONE:** `git log --oneline -- <file>` — переконайся, що `[x] DONE` справді **закомічено** (не «проза каже done, а коміту немає»).
- **Bug-hunt заодно:** notice defect (будь-де) → фіксь хірургічно (мінімальна нота, краще фікс); out-of-scope → явно познач.
- **«⏸️ Заблоковано Y» може бути УЖЕ resolved (додано 2026-06-09):** перевір, чи залежність ще реальна. **DOC-T.1** «blocked on SEC.9» виявився resolved — його doc-суперечність уже знята (§3.1-rework + WeakKeyDetector), docs key-agnostic → не залежали від key-replace; stale line-refs (цитовані рядки тепер містять інший зміст) = сигнал. → archive як resolved.

- 🔴 **ЗВОРОТНЯ нога, без якої правило працює лише в один бік: архівуючи ID, спитай не «на кого чекаю Я», а «ХТО ЧЕКАВ НА МЕНЕ»** — пускача з боку того, хто блокування ЗНІМАЄ, не існує, а саме він у цю мить єдиний знає, що залежність померла. 🔴 **Гейт не спіймає за побудовою: inbound-guard стереже, щоб реф не став dangling, а тут він резолвиться ідеально (ID живий, просто в §🗄️). «Резолвиться» ≠ «ще актуальний»** (`ssot-maintenance` §Guard-craft #50). **Команда — `grep -n "<ID>" docs/00_07*.md` із читанням КОНТЕКСТУ кожного хіта** на слова блокування (`⛔` · `ЗАБЛОКОВАНО` · `до присуду` · `чекає на` · `blocked`): кожен такий хіт після архівації ПРОТУХЛИЙ, доки не доведено інше. 🔴 **Але словник ловить не все: чекбокс `- [ ] 🔗 <ТвійID> — …` не містить жодного з цих слів. Шукай ДВІ форми — слова блокування ⊕ гліф `🔗` у рядку, що називає твій ID**, і для другої читай `**Стан:**` того пункту. ⚠️ Ціна асиметрична: хибне блокування ніхто не переміряє, воно виглядає як обережність. Інстанси → скіл `ssot-maintenance` · памʼять `log_tracker_self_errors`.

### Фаза 4 — CANONIZE (migrate-first)
- Знайди факти, що живуть ЛИШЕ в 00_07-прозі (не в каноні) → перенеси в канон-дім (`00_06 §2`) ПЕРШ ніж трим. Чесні обмеження/design-спостереження документуй прозою в body канону (як сусідні секції роблять).
- Drift, знайдений по нитці (значення в двох місцях, колізія opcode/константи, stale-ім'я) → виправ у **домі**, решту перепни на реф.
- 🔴 **Якщо результат пункту — ГЕЙТ, звір реєстр гейта з КОДОМ гейта ПЕРЕД архівацією.** Механізм: `guard_registry_sync` порівнює `run:`-команду, а вона не змінюється, коли перевірка додає новий вердикт — тож гейт росте осями при зеленому CI, а його рядок лишається на першій редакції. Заархівувати так означало б покласти в `§🗄️` пункт, чий носій ніде не оголошений. Конвенція рядка — **один датований буліт на вісь**; клас → скіл `ssot-maintenance` §Guard-craft #34.

### Фаза 5 — TRIM / ARCHIVE 00_07
- 🔴 **ЗОНД ПЕРШИМ, і він одноразовий: відкрий КАНОН-ДІМ пункту й пошукай речення, що оголошує МЕЖУ САМОГО ПУНКТУ.** Канон регулярно пише про трекер («стан → `00_07` X», «тут не дублюємо», «у трекері лишається лише ВІДКРИТА половина»), і таке речення є ратифікованим мандатом на зріз — сильнішим за будь-яку оцінку обсягу. ⚠️ Жоден гейт цієї пари не бачить **за побудовою**: обидва твердження локально істинні, ламається ВІДНОШЕННЯ між ними (клас → памʼять `feedback_self_negating_document`). Виміряно 2026-09-08: [`06_08 §1.3`](../../docs/06_08_Resilience_and_Failover_Policy.md) прямим текстом казав «у `00_07` лишається лише ВІДКРИТА половина SILENCE-1», а пункт тримав ТРИ ⚖️-присуди повними тілами — 18.4 kB, з яких 12.4 пішли одним рухом, і підстава була не в байтах, а в тому, що дві копії присуду розходяться тихо. **Команда:** `grep -n "00_07\|трекер" <канон-дім>` і читай кожен хіт як можливий мандат.
- 🔴 **ТРЕТІЙ вихід зонда, виміряний 2026-09-12 на `HW.34`, і він не є ні мандатом, ні забороною: канон шле сюди по стан, якого пункт НЕ МАЄ.** Вказівник «→ `00_07` X» резолвиться ідеально (пункт живий), тож жоден гейт не бачить, що по той бік порожньо — дзеркало до ноти про мертвий дім нижче, але **вхідне**, і тому невидиме з обох сторін: канон не знає, що не отримав відповіді, а трекер не знає, що його питали. Інстанс: [`01_01 §3`](../../docs/01_01_Coaxial_Gyroid_Topology_and_PEEK.md) адресує в `HW.34` «порядок · метод · приймання шва», а пункт про шов не ніс **жодної** ноги — при тому що звар ратифікований, а шов сідає в точку максимального згинального моменту. **Команда та сама, що для мандата, але читається ІНАКШЕ: грепнувши `00_07|трекер` у канон-домі, розділи хіти на три кошики — мандат (ріж) · «шле по стан, і стан Є» (несуче, лишай) · «шле по стан, і стану НЕМА» (заведи ногу).** Третій кошик і є найдорожчим уловом зонда, бо це не переміщення тексту, а робота, якої ніхто не веде.
- ⊕ **Дзеркало того ж зонда, і воно рятує від зайвого зрізу: канон буває сказав протилежне.** Якщо дім пункту сам шле ПО стан у трекер («умова перегляду — `00_07` X», «YAGNI-резерв — `00_07` X»), то ця половина в пункті **несуча** й лишається, хоч би як виглядала закритою. Виміряно того ж дня: `03_05 §2.2а` називає `00_07` SEC.21 домом YAGNI-резерву C-доповнювача — зріз там був би втратою.
- **cement-trim:** товстий блок → **`- **Стан:**`-лід** (Universal, founder 2026-06-14) = Присуд/вердикт essence + канон-pointer (**verify-canon ПЕРШ** — Принцип 2; **НЕ «✅ X»-лід**) + **вертикальні `[ ]` residual** (НЕ verbose `[x] ✅ 🤖` блоки). Прибери volatile counts. Реф — лінк-форма `[`NN_NN §X`](DocName)`.
- **archive:** перенеси рядок у §🗄️ Архів-таблицю (`| ID | Пункт | Канон |`, поряд тематичних сусідів) + видали `#### ID` з §модуля (захопи наступний heading як якір, щоб не лишити подвійний порожній рядок).
  - ⛔ **`⚫`-пункт (vacuous) до §🗄️ НЕ їде — ратифіковано ⚖️ founder 2026-09-05, тіло `BIZ.13`.** Інтро `00_07 §Розмітка` каже це прямо: item лишається НА МІСЦІ як closed-canon нотатка, residual хіба переоцінка. Підстава — цінністю такого пункту є САМЕ СПРОСТУВАННЯ преміси, а однорядковий архівний запис його не переживе; знята преміса повертається ПЕРЕвиведенням, не копіюванням, тож греп по історії її не спіймає. ⚠️ **Спокуса точна й повторювана, бо правило `archive` вище дає на неї ХИБНЕ «так»:** єдина `🌿`-нога `⚫`-пункту виглядає рівно як «optional/deferred, не зобовʼязання». **Дискримінатор: питай не «чи лишилась робота», а «чи лишилось СПРОСТУВАННЯ» — пункт, що існує, аби хтось не відбудував знятого, свій дім уже має.** Виміряно 2026-09-08: read-only розвідка запропонувала архів чотирьом `⚫`-пунктам, бо `BIZ.13` не відкривала — відповідь на її питання стояла в прозі сусіда три доби. ⊕ Дефектом на такому пункті буває інше й вужче — **гліф ВАГ на нозі-переоцінці**: він обіцяє доступний присуд там, де оживлення гейтоване подією; лік — `🌿`, не архів.
  - 🔴 **РОЗВОДЯЧИ залишки по домах, перевір, що дім ЖИВИЙ — архівний рядок теж є домом, тож маршрут у нього резолвиться ідеально й помирає мовчки.** Це дзеркало зворотної ноги Фази 3, і воно **гірше**: там вказівник переживає ціль, тут він мертвий **у мить написання**. Жоден гейт цього не бачить за побудовою (`tracker`-guard питає «чи ID резолвиться», не «чи він ще приймає роботу»). ⚠️ Спокуса виникає рівно тоді, коли розводка виглядає як акуратність. **Перевірка стрілки `→ ID` ДВОКРОКОВА:** (1) `grep -n "^#### <ID>" docs/00_07*.md` — порожньо означає «дім мертвий»; (2) якщо ціль в архіві — **ПРОЧИТАЙ її §🗄️-рядок**: можливо, відповідь уже там, і тоді лік протилежний переселенню — вказівник ЗАКРИВАЄТЬСЯ віднайденим присудом. Якщо ціль заархівована, а питання живе — питання лишається ТУТ або дістає власний `####`, і ніколи не їде в архів услід за адресатом. Інстанси → памʼять `log_verify_canon`.
  - 🔴 **АРХІВАЦІЯ — ТЕЖ ЗРІЗ, і краде вона тихіше за видалення, бо виглядає як упорядкування.** Архівний рядок несе ПРИСУД і його підставу — і більше нічого за побудовою. Перед злиттям перечитай `Стан`-рядки й спитай не «чи все закрито», а **«що з написаного тут не є станом ЦЬОГО пункту»**: метод-урок, відкинута альтернатива, куплений виміром рефлекс мають доми ПОЗА трекером (памʼять · скіл · шапка скрипта), і рядок їх не переживе. ⚠️ Migrate-first вище читається як «канонізуй», але напрямок тут інший — у канон таке не йде взагалі. 🔴 Найгірший підвид: урок, доданий і заархівований у межах ОДНІЄЇ сесії, не має навіть коміту. Дім класу → памʼять `feedback_verify_canon_before_delete`.

  - 🔴 **Третій напрямок — ВХІДНИЙ: архівуючи пункт, спитай, ХТО ШЛЕ СЮДИ ПО СТАН.** Обидві ноти вище дивляться з трекера назовні; цю не бачить жоден гейт репозиторію, бо джерело лежить **поза git** — памʼять роками пише «стан звіряй у `00_07` X», і клауза гниє в мить архівації, тобто від правки в ІНШОМУ файлі. Команда — `bash .claude/hooks/memory_gate.sh --stale-state`; лік **не** у видаленні ID (він і є провенанс), а в називанні закриття (`00_07` §🗄️ X) або в переадресації на живого спадкоємця. ⚠️ Тримай межу рівня й фронту (клас → `ssot-maintenance` §Guard-craft #82): детектор — **термометр**, він показує борг, коли той уже накопичено, тож питання ставиться ТУТ, у момент переходу.

### Фаза 6 — GATE + ZERO-LOSS
- `ruby scripts/docs_check.rb refs` + `tracker` → зелені. ⚠️ Це **два кроки** джоби `docs_check`, а не смуга: перед комітом ганяй `ruby scripts/docs_band.rb` (OPS.25 — читання зеленого `docs_check` як вердикту про смугу клало `main` тричі).
- Якщо чіпав heading → `bin/rails docs:toc`, потім check_refs.
- Якщо чіпав код → `make -C firmware/test` (firmware) / `bin/rspec` (backend); `bash -n` для shell-скриптів.
- **Zero-loss set-diff:** грепни кожен видалений з 00_07 факт і перевір, що він present. 🔴 **Периметр — УСЕ ДЕРЕВО, не «канон»**: носій буває коментарем над методом, шапкою гейта, рядком конфігу — саме туди Фаза 4 і радить його ставити. ⚠️ Помилка периметра штовхає в небезпечний бік: «втрачене» хочеться повернути в канон, тобто завести ДУБЛЬ рівно там, де One-Home щойно розвели. ⊕ І перед тим як «повертати», спитай, чи факт узагалі мав лишатись: звіт про власний прогін відсутній КОРЕКТНО (`00_06 §1`). 🔴 **Друга сліпота того ж кроку — він КРОС-МОВНО глухий:** трекер пише українською, дім факту часто англійською, тож зонд повідомляє ВТРАТУ там, де є лише переклад — і штовхає в той самий бік. 🔑 Рефлекс: перевіряй не фразу, а **найрідкісніший НЕПЕРЕКЛАДНИЙ токен** — число, ідентифікатор, імʼя файлу, змінну. Вони не мають другого написання, тож їхній нуль справді означає втрату. Inbound-рефи мають резолвитись. Калібрувальні інстанси → памʼять `log_verify_canon`.

### Фаза 7 — COMMIT (кожен пункт окремо)
- Commit **кожен ID окремим комітом** одразу як закінчиш (власник дозволив). Якщо в working tree кілька переплетених пунктів у 00_07 — розділи hunks через `git add -p` (`printf 'n\ny\nn\n' | git add -p docs/00_07...`), перевір `git diff --cached` перед кожним commit.
- Практика кампанії: коміт прямо в `main` **і push разом із комітом** — власник дав standing-авторизацію (памʼять `feedback_commit_push_authorization`); питати щоразу НЕ треба. Незворотні/деструктивні дії (force-push, видалення гілки, реврайт історії) авторизація НЕ покриває.
- Кінець меседжа: Co-Authored-By за конвенцією `CLAUDE.md §2` (поточна модель) — не хардкодити ім'я тут.

---

## 🧵 Хлібні крихти / нитки лабіринту

Слідуй за цими маркерами — вони ведуть до дому й викривають drift:
- **`[DOC.N]`** — canon-block ID (frozen, slug-baked). Напр. `[DOC.4]` = opcode-карта = дім `03_01 §4.5а`.
- **`[FW.N]` / `[SEC.N]` / `[ARCH.N]` / `[E.N]`** — теги в коді+доках; grep їх, щоб зібрати всю поверхню пункту.
- **`> **Cross-ref:**`** рядки + **«📐 КАНОНІЧНЕ ДЖЕРЕЛО / SSOT / дім»** — вказують, де факт живе.
- **`00_06 §2`** canonical-home registry — таблиця «який факт у якому домі».
- **`00_06 §3`** drift-guard table — які лінтери вже стережуть (і куди додати новий).
- **Опкоди** `0x99..0x9F` — дім `03_01 §4.5а [DOC.4]`; будь-який інший список = підозра на дубль/колізію.
- **Callout-и «⚠️ SUPERSEDED → §X»** — реверсовані рішення (напр. ATECC→SE050); реферований § = чинний дім.

---

## 🧰 Інструменти (`scripts/`, no Rails boot)

| Команда | Призначення |
|---|---|
| `ruby scripts/docs_band.rb` | **Уся смуга `CI · Docs`** (~1.5 хв) — перелік кроків читається з `docs.yml`, тож не протухає. Це те, що ганяти ПЕРЕД комітом; решта рядків цієї таблиці — швидкі підмножини (OPS.25). |
| `ruby scripts/docs_check.rb refs` | Омнібус УСЕРЕДИНІ свого кроку (~0.3с): dangling-лінки, §-drift, TRL, blockers→00_07, ToC, RTC reg-map, Lorenz/GP формули, opcode, deprecated-терми, xref-форма, external-paths. ⚠️ «Омнібус» тут про категорії ОДНОГО кроку, не про смугу — кроків 24. |
| `ruby scripts/docs_check.rb tracker` | 00_07: dup-IDs, #3-conformance, canon-ref + §-section resolution, section-home, inbound-refs, prose-ID-refs, CHEM.N. |
| `ruby scripts/doc_structure_map.rb` | Структурна мапа 00_01→00_02 (🎯/TRL/doc-links/секції) — навігація без читання всього. |
| `ruby scripts/content_dup_audit.rb --near` | Семантичний near-dup (Jaccard) — там, де grep по токену сліпий. |
| `ruby scripts/crossref_audit.rb` / `normalize_crossrefs.rb` / `linkify_bare_refs.rb` | Таксономія + bulk-нормалізація крос-рефів / лінкіфікація bare-рефів. _(Разовий `fix_stale_anchors.rb` знято 2026-08-09: його мапа одноразових фіксів давно застосована, клас тепер тримає HARD-гейт `#anchor resolution`, а мертвий запис усередині робив запуск неможливим — надгробок, не інструмент.)_ |
| `bin/rails docs:toc` | Регенерувати auto-ToC після зміни h2-заголовків. |

> Якщо знайдено **новий клас drift** — за рецептом `00_06 §3` додай pure-функцію-лінтер у `lib/docs_linter.rb` (+ spec, + wire у `docs.rake`, + рядок у `00_06 §3` guard-table). Defenses ростуть; skill — ні.

---

## 🚫 Anti-patterns (з реальних граблів сесії)

- ❌ Архівувати «за номером», не прочитавши код → пропустиш, що `[x]` насправді placeholder.
- ❌ Зупинятись на домі-документі → робота розкидана по 9 доках; drift живе на периферії (опкод-колізія знайшлась у `05_02`, не в домі).
- ❌ Trim, не канонізувавши спершу → втрата живого факту (а він був лише в 00_07-прозі).
- ❌ Канонізувати **невалідоване** рішення (no-premature-canon) → unvalidated pick трекай у 00_07/окремому треку, не в baseline-каноні.
- ❌ Батчити кілька Edit в одній відповіді (**навіть non-glue**) → роби по ОДНІЙ, верифікуй. Механіка glue-бага й founder-інстанс — Принцип 7 вище.
- ❌ Колапсувати/тоншати **Присуд/вердикт БЕЗ verify-canon** → видалиш незбережений аналіз. Механіка й founder-цитата — Принцип 2 вище; тут лише нагадування, що анти-патерн саме цей.
- ❌ **Вписувати в канон-файл тимчасовий маркер, щоб через крок зрізати його скриптом** (живий ID із дописаним суфіксом `-REMOVED-MARKER` у `####`-заголовку, спіймав founder 2026-08-03: «оце крінж»; сам ID тут НЕ цитується — див. застереження Фази 2, і до 2026-08-09 він таки стояв, бо `code_tracker_id_check` був сліпий до дефісних родин і порушення ніхто не бачив). Спокуса виникає рівно тоді, коли блок веде гігантський `- **Стан:**`-рядок, який не хочеться цитувати в `old_string` цілком. Ціна: між двома викликами трекер РЕАЛЬНО несе item з неіснуючим ID — обрив сесії лишає фантом у SSOT, а гейти або падають, або (гірше) приймають його за живий. Правильно: якір для span-replace бери з тексту, який **уже там є** (сам `#### ID —`), ріж діапазон від нього до наступного heading, і став guard на розмір блоку + на «чи не захопив чужий `#### ID`». Ширше: канон-файл — не чернетка, проміжних станів у ньому не існує.
- ❌ **Дописувати новий клас/знахідку ОКРЕМИМ рядком замість дописати в наявний СПИСОК** — так активно-ведений пункт за добу стає хронікою проходів: класи розсипаються по кількох рядках, кожен зі своєю датою й своїм «✅ прогнано», а читач більше не бачить, скільки їх усього. 🔑 Прикмета в момент письма: ти починаєш рядок словами «РАМКУ ПРОГНАНО ВДРУГЕ» / «НОВІ КЛАСИ, яких у переліку не було» — обидва означають, що перелік Є і дописувати треба В НЬОГО. **Правило: один пункт — один список класів; прохід дописує в нього рядок, а свій звіт лишає в git.** ⊕ Дзеркало для чекбоксів: закривши ногу, знімай її ТИМ САМИМ комітом — «зроблю чистку потім» і є механізм, яким пункт набирає брехню. Інстанс → памʼять `log_tracker_item_form`.
- ❌ Лишати volatile counts → дрейф щокоміту.
- ❌ **«Machine-half ✅ / вичерпано»-мантра у Стан-ліді** (founder 2026-07-05: «🤖 і 👤 self-explanatory enough») — WHO-теги residual-рядків + STAGE вже несуть «що зроблено машиною/лишилось людині»; лід веде з СУТІ (Присуд/корінь/рішення), не з поділу праці.
- ❌ Тягнути out-of-scope drift у поточний коміт (напр. ATECC→SE050 при FW.23) → відзнач у його **власному** треку, не змішуй.
- ❌ **Cement-trim gated-residual БЕЗ оновлення synergy-мапи** (`00_07` «⛓️ Гейт-кластери»): коли implicit «лишок = deploy» стає ЯВНИМ gated-residual, мапа «одна дія відкриває кластер» не показує НОВЕ зчеплення ID→кластер. 🔑 Несуче тут те, чого червона лінія про дзеркала не має: `⛓️` є **ЧЕТВЕРТИМ** дзеркалом, internal-00_07, окремим від skill/memory/canon — і старіє так само, тож sync у ТОМУ Ж проході.
- ❌ **Видаляти ID-визначення (CHEM.N / SEC.N / FW.N…), на яке посилається канон** → orphan-ref + `tracker:check` FAIL. ID-родини в 00_07 = **реєстр визначень** (канон реферить по ID; guard вимагає, щоб кожен cited-ID мав визначення) — done-`[x]` тонкий однорядковий ≠ bloat. **Verify-inbound ПЕРЕД видаленням** (`grep -rn 'CHEM\.N' docs/` поза 00_07 + прогнати guard); done-ID **тонши до однорядкового визначення, НЕ видаляй**. ⛔ **Межа: archive безпечний ЛИШЕ для вербозних done-РОБОТА-блоків без ID-ролі.** Інстанс, що купив правило: зріз done-CHEM.N P0-блоку дав **11 канон-рефів dangling**, спіймав `tracker:check`. «Не фанатично» = саме про це.

---

## ✅ Чеклист на пункт

```
[ ] Широкий аудит: ID + семантика по ВСЬОМУ docs/ (⛔ БЕЗ діапазону `01..07`: він виключає крайні номери, а ті несуть живі ID — свого часу так випали `00_00` і тодішній `00_08`. Перелік бери з дерева: `ls docs/[0-9][0-9]_[0-9][0-9]_*.md`, бо номери зʼявляються й розчиняються) + код + spec + .github
[ ] Forward + inbound рефи (хто на нього посилається) + крос-рефи крос-рефів
[ ] Реальний код прочитано; кожне ✅ verified; wire byte-accurate; git-verify DONE
[ ] Bug/anomaly hunt — фікс або явна нота
[ ] Важливе канонізовано в дім (migrate-first); drift виправлено в домі
[ ] 00_07 → тонкий вказівник (cement-trim) АБО §🗄️ Архів (повністю done); volatile counts прибрано
[ ] Гейти зелені — **УСЯ смуга `ruby scripts/docs_band.rb`** (25 кроків), НЕ пара `refs+tracker`
    ⛔ Та пара є двома кроками джоби `docs_check` із багатьох, і читання її зеленого
       як вердикту про смугу вже ТРИЧІ реддило `main` (OPS.25). Метод сторінки —
       `module_restructure.md` крок 5, там же ⚠️ про `git add` нових файлів ДО прогону
[ ] Zero-loss set-diff: кожен видалений факт present У ДЕРЕВІ (не лише в каноні — носій буває коментарем у коді); inbound-refs резолвляться
[ ] Commit окремим комітом (git add -p якщо переплетено); Co-Authored-By
```
