# Deploy — gotchas (тіла)

> **Companion-файл скіла `deploy`. Відкривай його, перш ніж запускати деплой, імпорт, drift-план,
> консольний зонд чи будь-який прилад на живому слоті.** Однорядковий індекс у `SKILL.md` §Gotchas є
> НОСІЄМ; тут — механізм, інцидент і межі. Правила правити ТУТ — індекс генерується
> (`ruby scripts/guard_craft_index.rb --write`). Номери — адреси (`6a` — окремий пункт), append-only;
> цитуй `deploy §Gotchas #N`.

1. **jemalloc через `LD_PRELOAD` у Docker-образі не прибирай без бенчмарку** (`libjemalloc.so`) — менше пам'яті
   й латентності.
2. **`SENTRY_DSN` задається at deploy time, і без нього Sentry інертний — нуль crash-репортів** (`.kamal/secrets-common`).
3. **Старт через Thruster — дефолт, і він overridable at runtime** (`thrust ./bin/rails server`).
4. **WIF рантайм = ТРИ GCP API, і `sts` та `iamcredentials` вмикаються лише ЯВНО** — `iam` (default-on) + `sts` + `iamcredentials`. Пропущений `sts.googleapis.com` → перший keyless CI-run падає `SERVICE_DISABLED` (STS робить OIDC→federated exchange ПЕРЕД impersonation), а `terraform validate`/local-apply це НЕ ловлять (STS не викликається при create pool).
5. **keyless AUTH ≠ terraform-apply CAPABILITY — CI імперсонує least-privilege deploy-SA БЕЗ IAM/WIF/serviceusage-admin, тож рефреш IAM/WIF-ресурсів дає 403.** ✅ **Розвилку ЗАКРИТО ⚖️ founder 2026-08-29: `apply` лишається founder-local, і конфіг це виконує — джоби `terraform` у deploy-воркфлоу БІЛЬШЕ НЕМА** (разом із нею знято `TF_VAR_*` і `terraform/` з path-фільтра canopy; `deploy` висить на `needs: verify-secrets`). ⛔ Не читай цей пункт як відкрите питання «apply в CI чи ні» — SA-privesc відхилено з підставою (він вимагав би чотирьох GCP-адмін-ролей, тобто god-credential проти самої мети keyless-WIF). 🔴 **І це не було косметикою:** доти `deploy` залежав від тієї джоби через `needs:`, тож у день деплою, ЩОЙНО заведуть WIF-Variables, вона діставала б 403 і `kamal deploy` не побіг би ЖОДНОГО разу; невидиме доти лише тому, що Variables порожні. Що лишалось: 403 у **weekly `terraform_drift.yml`** (він і далі робить `plan` з рефрешем) — оголошений негатив детектора, не блокер релізу. 🔴 **Але «оголошений негатив» ЗНЯТО ⚖️ founder 2026-09-08, і підстава — вимір, а не втома: детектор, що завжди червоний, дорівнює ВИМКНЕНОМУ**, тобто ціна була не в шумі, а в тому, що справжній дрейф не мав би як про себе сказати. Бракувало РІВНО двох READ-дозволів, і жоден не є привілеєм: `resourcemanager.projects.getIamPolicy` (рефреш 11 `google_project_iam_member`) + `servicenetworking.services.get` (рефреш `google_service_networking_connection`). Роль обрано за РОЗМІРОМ ПОВЕРХНІ, не за назвою — **`roles/browser` = 6 дозволів** чистого read ієрархії, проти 2535 у `iam.securityReviewer` і 6083 у `viewer`; другий дозвіл предефайненої read-ролі не має ВЗАГАЛІ (`servicenetworking.networksViewer` НЕ ІСНУЄ), тож несе однодозвільна custom-роль `driftSvcNetRead`. ⛔ Це НЕ послаблення присуду «apply founder-local»: підстава відмови була в чотирьох АДМІН-ролях, а тут нуль записів. Обидві прив'язки оголошені в `terraform/iam.tf` (⚖️ founder: «browser і в iam.tf» — тобто НЕ ручним `gcloud`, щоб детектор не залежав від ґранту поза IaC), отже вмикає їх **founder-local `apply`**. ✅ **Той apply ВИКОНАНО 2026-09-08 — не читай цю ногу як роботу, що чекає:** ґранти в GCP, `workflow_dispatch` прогнано, нуль 403, план доходить до кінця, і перший здоровий прогін одразу спіймав справжній дрейф (сім IAM-прив'язок жили в GCP, бо apply пройшов раніше за пуш `iam.tf`). 🔑 Урок для наступного такого ланцюга: **перебирати дозволи по одному з 403-помилок є біговкою ЗА ПОБУДОВОЮ** — `plan` обриває рефреш ресурсу на ПЕРШІЙ відмові, тож кожен ґрант відкриває лише наступний шар; лік — вивести потрібне з КОНФІГУ (перелік типів ресурсів кореневого модуля) і брати ЦІЛОСЕРВІСНІ viewer-ролі, які Google супроводжує в міру появи нових read-дієслів. → 00_07 INF.22 · `06_07 §1` · `06_01 §IAM`/Фаза 0.
6. **`gh run watch --exit-status` бреше (exit 0 on fail / 1 on empty) — щоб перевірити, чи Deploy·Canopy/Production реально пройшов, довіряй `gh run view --json conclusion`, не `watch`.** ⊕ `gh run list --commit` матчить лише ПОВНИЙ SHA — короткий віддає ∅ мовчки (09-02). ⊕ І детектор INF.9 (`Deploy · Canopy` → джоба `changes`) до 2026-09-02 судив файли ОДНОГО HEAD-коміту, не пушу: чотирикомітний пуш із docs-хвостом скіпнув деплой коду за зеленим `success`; тепер база = SHA останнього успішного `Kamal Deploy (Canopy)` (compare API), а «що зараз ЖИВЕ» — саме те питання, яке цей фільтр і має ставити.
6a. 🔴 **`conclusion: failure` теж бреше — не про факт, а про ПРИЧИНУ, і саме ця брехня маскує справжній червоний** (OPS.23). Хрестик «джоба не добігла» (раннери не видались: «The job was not acquired by Runner of type hosted…») і хрестик «код зламано» виглядають у панелі ІДЕНТИЧНО, а перший ховає другий — виміряний випадок: інфраструктурний червоний накрив червону підлогу покриття, і її не побачив ніхто. **Рефлекс на червоний `main`: питай спершу не «що зламалось», а «чи джоба СТАРТУВАЛА»** — `gh run view <id> --json jobs --jq '.jobs[] | select(.conclusion=="failure") | {name, steps: [.steps[].name]}'`; порожній `steps` = інфраструктура → **re-run**, бо під тим хрестиком може стояти другий. Дзеркальна половина класу закрита машинно: агрегат тепер стверджує результат САМОГО path-фільтра (доти незадеклароване `skipped` резолвилось у «OK», і `CI passed` зеленів, не виконавши жодної джоби) і його помилка явно каже «infrastructure failure, not a code failure» → `06_07 §2`. Ця ж, друга, нерозрізненна за побудовою — збій передує запуску нашого коду.
7. **`gh attestation verify` рендерить TTY-only → piped/`tail`/`grep` захоплюють ПОРОЖНЄ; довіряй EXIT=0 або `--format json`.**
8. 🔴 **Живі прогони `import.rb` знайшли вже ШІСТЬ дефектів, яких `--dry-run`/`--verify` не бачать ЗА ПОБУДОВОЮ** — вони судять ФОРМУ артефактів, а всі шість були про ФІТ із реальним API. Четвертий і пʼятий (2026-08-30) — ОДИН клас, найпідступніший, бо його не бачить навіть УСПІШНИЙ імпорт: **Ruler API ЗБЕРІГАЄ правило, яке Mimir потім відмовиться рахувати** (валідація запису слабша за evaluation). (4) SSE-вираз не парситься: `type: threshold`-model без ключа `expression: "A"` (classic-форма `conditions.query.params` його НЕ заміняє) — 57 правил сіли зеленими, перший тік дав `[FIRING:57] DatasourceError` у щойно задротований contact point. (5) `relativeTimeRange` > ~45 год пробиває Mimir-ліміт 11 000 точок/серію — лік: довгий lookback живе в PromQL (`increase(x[30d])`), вікно запиту 600s; гейт вікон тепер у `grafana_alerts_spec`. **Рефлекс: після імпорту дивись не на успіх POST, а на стан правил за хвилину-дві — і мірка = ВСІ оцінені (`lastEvaluation` != нульового) І нуль error: health свіжозбереженого правила до першого тіку стоїть «ok» порожнім дефолтом, і двоє таких «ok» ховали майбутні DatasourceError.** 🔴 **(6) І САМА ЦЯ МІРКА виявилась неповною 2026-09-03: вона правдива лише над метриками, що МАЮТЬ дані.** `threshold` приймає лише ЗВЕДЕНЕ число, а датасорс-запит віддає часовий ряд — тож правило без вузла `reduce` падає в evaluation, але ЛИШЕ коли метрика ожила; на порожній воно звітує `nodata` й читається як здорове. Тому вердикт «57/57 evaluated, 0 error» був правдивий у мить виміру й помер від першого живого job-контейнера: **29 із 42 перевірених правил стали Error, 9 із 10 `p0-critical`** — включно з тим, що стереже сам скрейп. Форма — `A(запит) → B(reduce,last) → C(threshold)`, анотації на `$values.B`; ⛔ `instant: true` відхилено (редактор малює `Type: Instant`, бекенд біжить range). Структурний носій — `grafana_alerts_spec` «no expression reads a raw datasource query». 🔑 **Рефлекс, ширший за Grafana: вердикт про алерти, знятий над порожніми метриками, є твердженням про ПОРОЖНЕЧУ — перевіряй його ще раз у день, коли зʼявились дані.** Решта три (2026-08-29):
   · **datasource-автовиявлення бере is-default, а за неоднозначності ВІДМОВЛЯЄТЬСЯ.** Grafana Cloud віддає кілька prometheus-джерел, і біллінгове `grafanacloud-usage` стоїть у списку ПЕРШИМ. Стара `.find` привʼязала б усі правила до бази, де `silkennet_*` не буде НІКОЛИ: резолвляться, виглядають здоровими, не спрацьовують ніколи. ⛔ Імʼя НЕ дискримінатор — денилист на `usage` протух би на першому перейменуванні вендором. **Регресія (повернення до `.find`) не ловиться жодним гейтом.**
   · **uid alert-правила ≤ 40 символів** — межа ВЕНДОРСЬКА, і відмова приходить лише з живого API (`400: UID is longer than 40 symbols`) ПОСЕРЕД черги: частина правил уже записана, решта не поїде. Тепер це валідує `--dry-run` (`RULE_UID_MAX`).
   · **`interval` групи — секунди-ЦІЛИМ в API проти рядка-тривалості (`1m`) у provisioning-файлі.** Порівняння `60 != "1m"` завжди нерівне, тож warn «інтервал не виставився» був хибною тривогою про ПРАВИЛЬНИЙ стан — і саме тому ховав справжній дрейф (`p2-info` стояв на 60 с замість 300). Тривога, що звучить завжди, не звучить ніколи.
   ⊕ **Дашборд і правила РОЗЧЕПЛЕНІ:** Grafana Cloud скоупить RBAC по теках, тож 403 на дашборді обривав імпорт до першого правила — часткові права давали НУЛЬ замість більшої половини. Тепер провал гучний і несе `exit 1`, але правила їдуть. Побачив частковий провал — це очікувана поведінка, не регресія.
   ⊕ **Звірка живого стека — `ruby deploy/grafana/import.rb --verify`** (READ-ONLY; оголошені стелі в шапці самої гілки): чи всі правила сіли · чи привʼязаний datasource · чи немає правил, СТВОРЕНИХ ПОВЗ РЕПО (зворотний дрейф, якого upsert не чіпає) · чи збігаються інтервали. ⛔ Він судить НАЯВНІСТЬ, ніколи правильність порогів, і не читає ані silences, ані contact point.
9. 🔴 **`terraform fmt -diff` РЕНДЕРИТЬ `terraform.tfvars` — тобто друкує `db_password` у відкритому вигляді.** Спіймано на собі 2026-08-31, за хвилини до першого `apply`. Форма підступна тим, що команда виглядає як лінтер: думаєш «перевіряю відступи», а отримуєш вміст файлу, який навмисно тримають поза git. **`.gitignore` тут не захищає — він стереже РЕПОЗИТОРІЙ, а витік іде в ЛОГ/транскрипт/CI-вивід**, тобто в поверхню, про яку gitignore нічого не знає. ⚠️ І це не про terraform: клас — будь-яка команда, що рендерить ВМІСТ файлу з секретами (`fmt -diff` · `diff` · `cat` · `plan` з `-var-file` у verbose). **Рефлекс: перш ніж запускати щось із `-diff`/`--show`/`cat` у каталозі, де лежить tfvars або env-file, скоупни ціль явно — `terraform fmt *.tf`, не `terraform fmt`.** 🔑 Дешевий лік, коли вже сталось: якщо `apply` ще не було, ротуй значення — воно ще не є креденшелом ні до чого, і ротація коштує один рядок; після `apply` це вже зміна пароля живої БД.
10. 🔴 **Console-доступ: дорогу ВИМІРЯНО 2026-09-02 — read-only `rails runner` через `gcloud compute ssh --tunnel-through-iap` + `sudo` `docker exec` працює з ноутбука, `kamal app exec` звідти падає на posix-акаунті SA (`06_01` ops-блок); інтерактивна консоль ще не виконувалась** — історія нижче лишається як підстава: 🔴 і преміса ЗВУЗИЛАСЬ 2026-08-31: «не задеплоєно нічого й ніде» більше не так (інфра розгорнута), 🔴 **а 2026-09-01 звузилась ВТРЕТЄ: застосунок ДЕПЛОЇВСЯ** (девʼять прогонів; контейнер стартував і почав Rails-бут), тож контейнера, у якому виконують консоль, не існувало до 2026-09-02 (виміряно 2026-07-29; природа зафіксована `OPS.37` 2026-08-29). Доти діра читалась як «рецепт для НЕ-ТОГО таргета» (Kamal задокументований, живим вважався Akash); з платформою знято й Akash-таргет — **тепер таргет один, рецепт один** (`kamal app exec --interactive --reuse "bin/rails console"`, `06_01 §DEPLOY-DAY`), а асиметрія не закрилась — вона змінила ПРИРОДУ: контейнер тепер СТАРТУЄ, але не ЛИШАЄТЬСЯ живим — `web3_network_guard` валить бут, тож консоль інертна не через відсутність деплою. 🔴 **Преміса звузилась УЧЕТВЕРТЕ 2026-09-01, і цього разу атрибуція теж зсунулась: Фаза 2t ВИКОНАНА** (шість контрактів живі на Amoy), тож тримали консоль уже не невиконана фаза, а **незаведені в env слоту адреси** — пʼятирухова нога `INF.27`, закрита 2026-09-02 (§🗄️): canopy бутнувся й живе, і саме на ньому цю дорогу виміряно. ⚠️ І «на плейсхолдерах» тепер занижує гард: після фіксу того ж дня ЗНЯТА змінна відмовляє бут так само, тобто прибирання плейсхолдерів більше не купує чистий бут — рівно той важіль, який фікс і знешкодив. Наслідок несе не той документ, де діра: **інертними лишаються всі console-рецепти репо**, зокрема два money-path (`manual_review`-резолюція та ескалація Field-Audit C→A, що відчиняє ворота необоротного слешингу). Тож пишучи новий рецепт «виконай у консолі», не вважай доступ вирішеним питанням; спільна нота стоїть одним домом у шапці `06_07 §3` — читай і вирівнюй, не пиши третю редакцію. ⛔ `coap` для консолі не брати (звільнений від master-key-перевірки). → `00_07` OPS.20. ⊕ **КЛАС, що пережив і платформу, і рецепт (00_07 §🗄️ OPS.20):** «інструмент доступу недоступний саме в ІНЦИДЕНТІ — рестарт/краш-луп, — коли він потрібен» — платформо-незалежний; з ноутбука `kamal app exec` недоступний ЗАВЖДИ, не лише в інциденті (posix-акаунт SA), тож дорога = `gcloud compute ssh --tunnel-through-iap` + `docker exec`, а надійність саме ЦІЄЇ дороги в краш-лупі не міряна — тригер: перший рестарт під навантаженням.
11. 🔴 **`gcloud sql instances clone` після ~10 хв друкує «failed … taking longer than expected» — це клієнтський таймаут, не вердикт:** операція живе (DR-drill 2026-09-02: `CLONE` 15:57→16:08Z, RTO 11:00 до RUNNABLE), тож судити `gcloud sql operations describe <op>` (`status`/`startTime`/`endTime`) — саме ця пара і є RTO. Сусідня брехня того ж роду: `gsutil` під корпоративним TLS-проксі зависає мовчки (свій CA-стор, не gcloud-конфіг) → `gcloud storage ls/cp` (з `#<generation>` для версій стану). Клон успадковує `deletionProtectionEnabled` — видалення = `patch --no-deletion-protection` + `delete`.
12. 🔴 **Скрипт анкера живе в `metadata.startup-script` з 2026-09-03 (⚖️ founder B, INF.17): зміна скрипта чи піна образу відтоді in-place + `reset`.** ⚠️ **Але `reset` потрібен НЕ на кожну правку шаблона, і межа коштує бонсу живого інтейку:** heredoc пише `coap.env` під гардом `if [ ! -f … ]`, тож на ЗАПОВНЕНОМУ анкері зміна цієї половини не доїжджає НІКОЛИ — `reset` там не купує нічого, крім рестарту PRIMARY CoAP-демона (виміряно 09-03: рядок довелось дописати в живий файл руками). `reset` несе сенс там, де скрипт має ПЕРЕвиконатись: пін образу, HAProxy-конфіг, systemd-юніт. Доти він був `metadata_startup_script` (ForceNew у google-провайдері), і кожна правка = replace VM (`1 add / 1 destroy`) — саме тому фікси 09-02 на VM не доїхали, а крок рунбука «apply оновлює startup-script» був мертвий; урок: крок IaC-рунбука судить `terraform plan`, не читання HCL. Сама міграція коштує один останній replace (атрибут іде в null), і вікно = разовий `gcloud compute instances update … --no-deletion-protection` + один `apply`: Terraform не може зняти захист зі СТАРОЇ VM у тому ж replace (виміряно 09-03 — окремий прапор-вікно дав «Deletion Protection is enabled» і був знятий як YAGNI), а нова VM народжується вже захищеною; після replace `app-host-ip` знову сентинел → `add-metadata` + `sudoedit coap.env` + `reset`. 🔴 **Пін `coap_daemon_image` — ЗАВЖДИ, і його дім із 2026-09-08 — `posture.auto.tfvars` (комічений), НЕ `terraform.tfvars`.** Доти тут стояло «лише у вікні replace, інакше кожен apply спотикається» — ⚖️ founder 2026-09-08 сказав «зняти пін взагалі», і буквальне виконання поклало б PRIMARY-інтейк: дефолт `variables.tf` є `:PIN_ME`, і це не запасне значення, а **fail-closed сентинел [INF.21]**, який іде дослівно в `ExecStart` systemd-юніта та в `docker pull` → перший же `reset` анкера дав би «CoAP daemon NOT started: image ABSENT». 🔑 **Причиною тижневого фантомного дрейфу був не пін, а його ДІМ:** `terraform.tfvars` gitignored, тож `Ops · TF Drift` рендерив startup-script із `PIN_ME`, а founder-local `apply` — із реальним тегом, і розбіжність ДВОХ РЕНДЕРІВ читалась як дрейф інфри. Після переносу `terraform.tfvars` містить рівно ті три змінні, що їх воркфлоу передає через `TF_VAR_` — асиметрію знято структурно. ⚠️ **Ширший рефлекс: побачив «фантомний дрейф» — питай не «що змінилось в інфрі», а «чи обидва рендери мали ОДНАКОВІ входи»**; змінна, видима лише одному з двох приладів, дає вічну розбіжність, у якій жоден із них не помиляється.
13. 🔴 **Детектор «чи змінилось релевантне» судить БАЗУ, не HEAD — і в ОБОХ воркфлоу:** `deploy.yml` (INF.9, 09-02) і `mirror-ghcr.yml` (INF.17, 09-03) читали `commits/${HEAD_SHA}` = файли ОСТАННЬОГО коміту пушу; база = head останнього успішного `Kamal Deploy (Canopy)` / `Build & Push to GHCR` через compare API, носії `deploy_relevance_base_spec` / `mirror_relevance_base_spec`. Симптом mirror-діри: живий коміт canopy без `sha-<7>` тега — пінити нема що; закрита вісь у сусідньому воркфлоу = перший кандидат на ту саму діру. ⊕ Grafana «горить»: читай `alerting/history`, не список правил — 30.08 усі правила пройшли NoData/Error у день імпорту (мітки `datasource_uid`/`ref_id` = підпис цього стану), а canopy-дашборд «No data» був ціною присуду OPS.37 до 2026-09-03 — відтоді `config.alloy` несе два canopy-таргети зі `slot` НА ТАРГЕТІ, розкатано того ж дня dispatch-входом `deploy.yml -f alloy=reboot` (гоча 15).
14. 🔴 **Три deploy-day факти, кожен виміряний 2026-09-03, і жоден не видно з зеленого воркфлоу:** (а) `kamal deploy` НЕ стрімить stdout post-deploy хука — exit 0 і є весь вердикт про склад `governance:bootstrap`, тож «зелений деплой» про bootstrap не свідчить (OPS.38); (б) `db:prepare` сіє лише ПОРОЖНЮ базу — фікс сіда не доходить до живого слоту без дропу трьох баз (`06_06 §5.6`), і canopy добу малював старий сід після виправлених координат (ex-UI.19); (в) крок рунбука, виконаний РУКАМИ (бакет для дампів, DR.1), тим самим проходом оновлює рядок рунбука — інакше наступний читач створює ДРУГИЙ ресурс або, гірше, вірить опису, що розійшовся з реальністю (трекер писав «EU multi-region», `buckets describe` сказав `europe-west1`).
15. 🔴 **Чотири гейти дня 2026-09-03 були зелені на речі НЕ ТОГО РОДУ — і кожен закрито формою, не значенням.** (а) `gh secret set` за промптом поклав Grafana-токен у `CANOPY_REDIS_URL`; `verify-secrets` судив присутність → три деплої `/ready` 503 `redis: false`, старий контейнер жив на попередньому значенні. Діагноз — runner у СТАРОМУ контейнері з новим процесом (`p Sidekiq.redis { |c| c.call("PING") }` друкує URL у помилці, тобто засвітить секрет — читай вивід сам, не вставляй у чат). Лік — гейт форми `*_URL` ⇒ `scheme://` в обох воркфлоу + `gh secret list` (час оновлення) після КОЖНОГО `secret set`. (б) `deploy_timeout` 30 с: холодний entrypoint canopy = 4,5 хв, теплий ≈ 30 с + проба раз на 10 с → kamal-proxy убиває контейнер із ДОПИСАНОЮ роботою; canopy — `deploy_timeout: 360`. (в) Kamal-лок від убитого прогону валить наступний на `proxy reboot`; concurrency-група серіалізує деплої, тож лок на старті = труп → крок `kamal lock release` перед `proxy reboot`. (г) Alloy ставить `job` = id компонента (`prometheus.scrape.<label>`), а правила селектують `job="silken_net_scraper"` → `job_name` у scrape-блоці; перша серія в Grafana Cloud (`up{instance="canopy-web:80",slot="canopy"}` = 1) довела: мітка ТАРГЕТА бере гору над `external_labels`. Розкатка accessory — лише dispatch-вхід `deploy.yml -f alloy=reboot` (файл монтується при boot; `boot` існуючий контейнер пропускає).

16. 🔴 **`spl-token` фізично не працює за TLS-перехоплювальним проксі, і це властивість БІНАРЯ, не мережі — а сусідні інструменти в тій самій оболонці працюють, тож симптом читається як «зламався Solana»** [DEPLOY-1 Фаза 2t, виміряно 2026-09-03]. `spl-token-cli` зібраний на **rustls із ВШИТИМИ Mozilla-коренями**, тож корпоративний CA він не бачить ЗА ПОБУДОВОЮ і віддає `InvalidCertificate(UnknownIssuer)`; ⛔ `SSL_CERT_FILE`/`SSL_CERT_DIR` його **НЕ лікують** — це змінні для openssl-probe, а не для webpki. Дискримінатор дешевий і його варто зробити ПЕРШИМ: `curl` і Ruby/`Net::HTTP` ходять системним стором, `solana` CLI — теж, тож якщо ті троє беруть той самий ендпоінт, а `spl-token` ні, питання не в мережі й не в RPC. **Лік — прибрати TLS зовсім там, де він не потрібен:** локальне plain-HTTP реле на `127.0.0.1`, що приймає JSON-RPC і форвардить його назовні через Ruby (`Net::HTTP`, системний стор), після чого `spl-token --url http://127.0.0.1:<port>` робить усе штатно. ⚠️ Реле мусить закривати зʼєднання коректно: наївна форма `Connection: close` дає CLI `IncompleteMessage` на **поллінгу підтвердження** — транзакція при цьому вже пройшла, тож помилка косметична, але виглядає як провал; **суди результат станом у чейні, не кодом виходу CLI**. 🔑 Ширший рефлекс: побачив TLS-помилку в ОДНОГО інструмента зі стеку — питай не «що з мережею», а **«який у нього TLS-бекенд і чи він узагалі вміє читати системний стор»**; відповідь ділить інструменти на два класи, і всередині класу вони поводяться однаково.

17. 🔴 **Діагностичний зонд через `bin/rails runner` на ЖИВОМУ слоті не є read-only щодо каналу помилок: його raise стає issue в Sentry під ПРОДОВИМ тегом релізу** [2026-09-03, спіймано на собі]. Виміряний інстанс — розвідка cron'а на canopy: `require "sidekiq_scheduler/redis_manager"` упав `LoadError`, і власник дістав нову issue `SILKEN-NET-8` під релізом того ж коміту, тобто мій зонд став неможливо відрізнити від дефекту застосунку, доки хтось не відкриє стек. **Рефлекс: усе, що ти пробуєш «просто подивитись» на живому слоті, обгортай так, щоб воно не могло кинути — і памʼятай, що вартість помилки тут не твій час, а хибний сигнал у чужому каналі тривоги.** ⊕ 🔴 **І гард, який я поставив, НЕ спрацював, бо в Ruby `rescue => e` ловить `StandardError`, а `LoadError` є `ScriptError` — інша гілка `Exception`.** Тобто `begin … rescue => e` довкола `require` є гардом ЛИШЕ на вигляд: він компілюється, читається як обачність і пропускає рівно той виняток, заради якого його писали. Для `require`/`load` у зонді бери `rescue LoadError, StandardError => e`. 🔑 Ширший рефлекс, і він не про Sidekiq: **побачив `rescue` довкола завантаження коду — спитай не «чи є гард», а «чи ловить він КЛАС, який тут кидається»**; сімʼя `ScriptError` (`LoadError`, `SyntaxError`, `NotImplementedError`) лежить поза `StandardError` за побудовою.
18. 🔴 **Двічі поспіль я міряв cron не тим приладом, і обидва рази отримав хибне «немає»** [2026-09-03]. (а) Питав `Sidekiq::Cron::Job` — у нас `sidekiq-scheduler`, інший гем і інші класи; (б) питав із `bin/rails runner`, а той **окремий процес і `config/sidekiq.yml` не читає** — блок `:scheduler:` вантажить сам sidekiq-СЕРВЕР при буті, тож `Sidekiq.schedule` у runner'і порожній ЗАВЖДИ, і на здоровому слоті теж. **Чесний зонд питає Redis і сам процес:** `Sidekiq.redis { |c| c.call("KEYS", "sidekiq-scheduler*") }` (є `pushed:`-мітки) + `HGETALL sidekiq-scheduler:last_times` (коли яка джоба бігла) + `docker inspect --format "{{.Config.Cmd}}"` (чи справді `-C config/sidekiq.yml`). Три незалежні свідки; жоден із них не в runner-процесі. **Рефлекс: питаючи «чи X живий», спершу назви, ЯКИЙ процес тримає X — інакше ти виміряєш процес, у якому його не буває за побудовою.**
19. 🔴 **Рунбук може бути роззброєний власним ЗАГАРТУВАННЯМ, і тоді він бреше саме в аварії** [ARCH.118/INF.11, виміряно 2026-09-04]. `05_01 §8` наказував операторові в інциденті зняти `WEB3_STRICT_MODE=false`, щоб розблокувати KYC-гейт мінтингу (секцію знято 2026-09-04 — присуд живе в `06_08 §2.2б`, і разом із ним ⛔ на ручне проставляння `hadron_kyc_status`). Умова в коді — `ENV["WEB3_STRICT_MODE"] == "true" || Rails.env.production?`, тобто у проді другий диз-юнкт істинний ЗАВЖДИ і `raise` лишається: важіль інертний. Двері закрив INF.11 (2026-07-10) свідомо й ПРАВИЛЬНО — забутий прапорець не сміє відкривати fake-KYC-мінт, — а рунбук, писаний під старе розуміння, ніхто не свіпнув. 🔑 **Ціна саме в моменті:** оператор під тиском тисне важіль, нічого не стається, і він робить хибний висновок «зламано щось інше», витрачаючи найгірші хвилини. ⚠️ Клас ширший за прапорці: **правка, що ЗВУЖУЄ дозвіл, лишає всі рунбуки, які той дозвіл використовували, і жоден гейт цього не бачить** — greп по правилу їх не знайде, бо рунбук описує ДІЮ, а не правило. **Рефлекс: звузивши будь-який дозвіл (прапорець, роль, ENV, feature-gate), грепни рунбуки й emergency-таблиці на ДІЄСЛОВО («зняти», «вимкнути», "set … false", "override") — і перевір, чи вони ще щось роблять.** ⊕ Дзеркало до п.14в і до `00_05 §4` осі «дозвільна мова».

20. 🔴 **`import.rb --verify` судив НАЯВНІСТЬ правильно, але текст навколо неї брехав про ПОХОДЖЕННЯ — і не міг інакше, бо імпорт ОДНОСТОРОННІЙ** [S2.4, ⚖️ founder 2026-09-05]. Рядок «у стеку, але НЕ в IaC» звав кожен такий запис «створеним руками»; насправді `import.rb` лише додає/оновлює правила ЗА uid, тож правило, ЗНЯТЕ з репо (файл видалено чи перейменовано), лишається живим у стеку й потрапляє в ТУ САМУ множину — форма не відрізняє «людина натиснула Save в UI» від «git rm». Виміряно на `sn-alert-cluster-entropy` (знятому 2026-09-05): він був IaC-керованим, а звіт приписав би його рукам. **Рефлекс: розбіжність «поза IaC» перевіряй git-історією (`git log -- deploy/grafana/alerts/`), а не текстом самого репорту** — текст тепер каже чесно («або створені руками, або ЗНЯТІ з IaC»), але скрипт джерела ЗА ПОБУДОВОЮ не знає (немає снапшота попереднього імпорту, з яким звіряти). ⛔ Це НЕ привід заводити `--prune`: автоматичне видалення лишається відсутнім свідомо — форма запису, справді створеного руками, від знятого з IaC не відрізнити, тож автопрун стер би легітимну ручну діагностику разом із дрейфом (`00_07` S2.4). Носій — коментар над `unless extra.empty?` у `deploy/grafana/import.rb` (гоча 8, те саме `--verify`). 🔴 **І в того ж `--verify` була ЧЕТВЕРТА сліпа зона, дорожча за походження — ТЕКСТ, ЯКИЙ ЧИТАЄ ОПЕРАТОР** [2026-09-06]. Ратифікована форма звірки порівнювала `spec.expressions.{refId}.model.expr` побайтово й давала «✅ паритет», тоді як живий стек ніс `description` ДВОХ ПОКОЛІНЬ давнини: фікс 09-05 («нуль ≠ вичерпання») не імпортували НІКОЛИ, тож алерт і далі казав «вичерпує кошти» на гаманці, куди не клали монет. **Вираз ламає СПРАЦЮВАННЯ й помітний; речення ламає ДІЮ ЛЮДИНИ і мовчить.** Вісь додано (анотації · мітки · `noDataState`/`execErrState`/`for`), і `for` порівнюється через `interval_seconds`, бо Grafana НОРМАЛІЗУЄ тривалість (`0m`→`0s`) — наївна рівність рядків червонила 9 правил на ЧЕСНОМУ дереві. ⚠️ **Рефлекс: правка в РЕПО ≠ правка в СТЕКУ, і гейт, що звіряє лише машинну половину артефакту, підтверджує паритет, якого немає.** Питай не «чи звірка зелена», а «яке ПОЛЕ вона порівнює». ✅ **ДЗЕРКАЛЬНУ ПОЛОВИНУ ЗАКРИТО 2026-09-06 — пʼята вісь: ВИРАЗИ.** Четверту (текст) додали саме тому, що вираз-звірка пропускала речення; при переїзді в `--verify` вираз-половину не перенесли ВЗАГАЛІ, тобто лік від однобокості відтворив однобокість на другому боці. Ціна виміряна на собі: зняття `> 0` з `sn-alert-tree-sweep-stale` — правка, що РОЗСЛІПЛЮЄ дед-мена — пройшла звіркою зеленою. Тепер `--verify` червонить із обома боками в тексті. ⚠️ Стеля лишається: звірка судить РІВНІСТЬ рядка, ніколи доречність виразу. **І незалежно від неї став третій вимір — «збережено» ≠ «оцінюється»:** `/api/prometheus/grafana/api/v1/rules` → `health` мусить бути `ok` на ВСІХ, `lastError` порожній; жодна звірка IaC цього не каже, бо це властивість ОЦІНКИ, не збереження.

21. 🔴 **Після `drop` + `reseed` УВЕСЬ DeadSet стає недійсним ЗА ПОБУДОВОЮ, і «Retry All» перетворюється з марного на ШКІДЛИВИЙ** [S2.4, виміряно на живому canopy 2026-09-05]. Аргументи мертвих джоб адресують СТАРУ базу: `FilecoinArchiveWorker(audit_log_id)` робить `AuditLog.find(audit_log_id)`, тож після ресіду той id або не існує, або належить **ІНШОМУ** рядку — і ретрай запінив би в IPFS не той запис, а пін незворотний. Те саме з будь-яким воркером, чий аргумент є PK. ⛔ **Після руйнівної операції єдиний чесний хід — ВИДАЛИТИ, ніколи не ретраїти**; справді незавершену роботу поточної бази підбирають ре-арм-воркери за власними скоупами (`FilecoinReconcileWorker`: `ipfs_cid` NULL, вікно 30 днів, старші за 2 год). ⚠️ **І дискримінатор тріажу «чи DeadSet РОСТЕ» дає хибний сигнал у вікні ресіду:** він РІС — чотири періодичні воркери впали на `ActiveRecord::NoDatabaseError: Database not found`, поки бази не існувало, — але продюсер не дефект, а наша ж руйнівна дія. Питай не «чи росте», а **«що саме додалось і коли відносно останньої руйнівної операції»**. 🔴 **І поза вікном ресіду вирок ПОРОДИННИЙ, а не гуртовий — виміряно 2026-09-06.** У DeadSet лежали дві родини: `TimeSyncDownlinkWorker` (ACK timeout у Королеву, якої на canopy НЕМАЄ за конструкцією — не вдасться ніколи, скільки не ретрай, а причина появи була артефактом рестарту симулятора) і `CeloRewardWorker` (падає на ЖИВІЙ чинній умові — порожній гаманець; сервіс date-scoped і ВІДТВОРЮВАНИЙ, тож після поповнення ретрай справді довершить винагороду). Перших видалено, других лишено. **Питання до кожної родини окремо: чи може ця джоба вдатися ПІСЛЯ дії, яку ми плануємо — чи вона неможлива за побудовою слоту.** Гуртове «clear the DeadSet» стерло б рівно ту роботу, заради якої поповнення й робиться. ⊕ 🔴 **І клас ШИРШИЙ за DeadSet — він однаковий на БУДЬ-ЯКОМУ списку, що має bulk-кнопку; переміряно 2026-09-07 на Sentry.** Три unresolved issue: `AASM::InvalidTransition` (441 подія) — полагоджено й задеплоєно, ✅ ресолвити; `[Celo] Оракул НЕ ПРОВІЖИНЕНО` — гаманець і досі нуль, ⛔ сигнал живий; `insufficient funds for gas` — **персистований свідок ВІДКРИТОЇ ⚖️** про поріг балансу, ⛔ ресолв стер би єдиний слід того, що зелений `ratio` світив над непрацездатним money-шляхом. Тобто «Resolve All» тут коштував би рівно того ж, що «Delete All» вище. 🔑 **Дискримінатор ОДИН на обидві поверхні: чи може цей запис вдатися/зникнути після ДІЇ, яку ми плануємо — чи він неможливий за побудовою слоту.** ⚠️ І третій рід, якого в чергах не буває: запис може бути не роботою, а ДОКАЗОМ відкритого присуду — такий не закривають ані ретраєм, ані поповненням, лише разом із самим присудом. Перш ніж bulk-дія: розклади список по родинах, дай кожній власний вирок, і назви вголос, чому кожна отримала свій.

22. 🔴 **`gh secret set` доїжджає НІКУДИ, доки змінна не проведена ПʼЯТЬМА поверхнями — і три з них знаходить гейт, не автор** [S2.4/UI.19, двічі за один день 2026-09-05]. Повний ланцюг: `config/deploy.yml` `env.secret` · `.kamal/secrets-common` (`VAR=$VAR`) · `RUNTIME`-перелік кроку verify · env-дзеркало САМОЇ джоби деплою · **обидва** воркфлоу (`deploy.yml` ⊥ `deploy-production.yml`). Пропустиш будь-яку — `gh secret list` покаже зелене, а в контейнер приїде **порожня змінна**. Суддя — `spec/deploy/env_fetch_declaration_spec.rb` [B1/INF.19], і його текст каже це дослівно: «`.kamal/secrets-common` resolves them from the CI shell, so they arrive EMPTY in the container». ⚠️ **І окремо про `env.clear` як «дім не-секрету»:** `scripts/deploy_secret_scan.rb` тримає інваріант «`*_KEY` не несе літерала в clear», тож публічний-за-природою ключ (CARTO basemap: їде в URL тайла, захищений referrer'ом) усе одно кладеться в `env.secret` — **щоб не осісти в ПУБЛІЧНОМУ git, а не тому, що він таємний**. Канон `04_04 §7.2` називав домом `env.clear` і був виправлений саме цим гейтом: носій виявився правішим за присуд.
23. 🔴 **`timeout N docker exec …` НЕ ставить стелі процесу — вона вбиває КЛІЄНТА, а робота всередині контейнера їде далі** [виміряно 2026-09-05, симулятор проганяв **39 хвилин** після «таймауту»]. `docker exec` це RPC: клієнт від'єднується, демон процес не чіпає. Наслідок дорожчий за зайвий час — прогін, який ти вважаєш зупиненим, ДАЛІ пише в живу базу (у нас він тим часом насипав телеметрії й запалив критичний fraud-канал). Стеля мусить стояти ВСЕРЕДИНІ: `docker exec … timeout N <cmd>` (не зовні), або команда запускається з власним лімітом.
    🔴 **І половина, яка зробила діагностику гіршою за її відсутність: `docker exec … ps aux | grep -c` віддав НУЛЬ — не тому, що процесу не було, а тому, що в контейнері немає `procps`.** `ps` не знайдено → порожній stdout → `grep -c` чесно рахує 0, і **exit-код теж «успішний»**. Тобто прилад відсутній, а відповідь виглядає як вимір: класичний хибний негатив від невстановленого інструмента, і він каже «чисто» саме тоді, коли брудно. ⚠️ **Лік — питати ГОСТА, а не контейнер:** `docker top <container>` бачить процеси ззовні namespace'у й від наявності пакетів усередині не залежить; ним і знайшлась жива PID, яку далі знято `docker exec … kill`. Ширший рефлекс: перш ніж вірити нулю, спитай, чи інструмент, що його дав, узагалі існує в тому середовищі — `command -v ps` коштує один рядок.

24. 🔴 **«Тривога, що звучить ЗАВЖДИ, не звучить ніколи» — і на демо-слоті це не метафора, а щоденний механізм** [S2.4/E.64, тричі за один день 2026-09-05]. `bin/forest_simulator` брав `bio_status = rand(0..3)`, а DCI питає, чи збігся заявлений стан із серверним Z — розбіжність гарантована, тож КОЖЕН демо-прогін запалював критичний `Telemetry fraud detected`, істинно-позитивний для механізму: «шахраєм» був наш власний симулятор. ✅ **ЦЕЙ інстанс закрито 2026-09-06 [E.64]:** вузол тепер РАХУЄ статус власним Lorenz-ланцюгом (`Hil::SoldierNode`), тож канал знову здатен стати зеленим. ⚠️ **Закон від того НЕ ослаб, і саме тому інстанс лишається тут у минулому часі, а не викреслюється** — він є доказом, а не хронікою: два інші вічно-червоні канали цього слоту (`Queen offline` · `Silent trees` після ре-сіду) стоять досі, і кожен новий демо-механізм народжується кандидатом у ту саму пастку. Так само вічно-червоними стають `Queen offline` і `Silent trees` після ре-сіду (у базі є вузли, передавати нікому). ⚠️ **Питання при кожному firing не «чи горить», а «ХТО саме це стверджує і чи є в нього вимірювач»** — і якщо канал не може стати зеленим на цьому слоті за побудовою, він уже не алерт, а фон. Той самий закон ратифіковано для production-silence (silence з ДАТОЮ, не з умовою) і для DeadSet.
25. 🔴 **Діф каже «розбіжність у 100 % елементів» → підозрюй ПАРСЕР, а не предмет** [S2.4, 2026-09-05]. Звірка репо ⟷ живий Grafana дала «58 розбіжностей із 58» — не тому, що стек інший, а тому, що мій парсер шукав `data[]` (форма РЕПО), тоді як provisioning-API віддає `spec.expressions{}` (МАПА, ключ = refId). Нуль від зламаного приладу виглядає як вимір і вказує в найгірший бік — «нічого не імпортовано». Сестра гочі 23 (там прилад ВІДСУТНІЙ, тут — не тієї форми); спільне те, що обидва дають упевнену відповідь без жодного червоного.
26. 🔴 **Ключ ≠ баланс: presence-чек секрету зелений при непрацездатній нозі** [DEPLOY-1, 2026-09-05]. `ORACLE_CELO_PRIVATE_KEY` заведений і `verify-secrets` зелений — а на адресі нуль монет, тож `CeloRewardWorker` рейзить щодня на кожен NaaS-контракт живого кластера. Заведення КЛЮЧА і поповнення АДРЕСИ ведуться різними домами, і другого не веде ніхто. ⊕ 🔴 **І ПРАВИЛО ТУТ ІНВЕРСОВАНЕ ВИМІРОМ 2026-09-06 — доти цей абзац сам був його жертвою.** Стояло: «Sentry показав ОДИН непоповнений оракул, а Grafana — ТРИ», з висновком «вузьке джерело дає вузький периметр». Історія станів обох правил за 09-03→09-06 не містить ЖОДНОГО переходу в `Alerting` поза `network=celo, signer=rewards` — тобто **Grafana теж показувала одного, а «три» було переліком ІНСТАНСІВ правила, прочитаним без поля `state` кожного.** Множина інстансів справді йде з міток `signer=…`, тобто з ТОПОЛОГІЇ КОДУ, і саме тому вона не є множиною горінь. **Рефлекс лишається, але з протилежним знаком: питай не «чи джерело ширше», а «що формує його множину — предмет чи топологія», і для алерта читай `alerting/history` (гоча 13), НІКОЛИ перелік інстансів.** ⚠️ Найдорожче тут — напрямок: ширше джерело дало ХИБНІШИЙ периметр, і саме воно було записане як «виправлення» вузького. ✅ **ПРИЛАД ЗʼЯВИВСЯ 2026-09-06 — `rake treasury:balances`** (адреса · баланс · `nonce` · вирок `НЕ ПРОВІЖИНЕНО` ⊥ `НИЖЧИЙ ЗА МІНІМУМ` ⊥ `НЕ ПРОЧИТАНО`); на canopy: `kamal app exec -d canopy -r job "bin/rails treasury:balances"`. Доти єдиним читачем балансів був `TreasuryMonitorWorker` усередині Sidekiq — тобто рішення просили, а міряти не було чим. ⛔ **Зонд СВІДОМО не є обгорткою над `Treasury::MonitorService.call`:** той у тому ж проході пише ґейджі, детектує mint-аномалію, **створює** `EwsAlert` і **закриває** одужалі, тож «подивитись баланс» його викликом означало б мутувати те, про що звітуєш. ⊕ Перший же прогін знайшов **ТРЕТІЙ стан ключа: ПРИСУТНІЙ, АЛЕ НЕПРИДАТНИЙ** — `resolvable?` судить НАЯВНІСТЬ, тож плейсхолдер проходить зеленим, а деривація падає `Secp256k1::Error`. ⊕ І дозакрито периметр присуду «нуль ≠ вичерпання»: ніг було ЧОТИРИ, свіп 09-05 накрив три, а `Ethereum::StateAnchorService` — саме `ethereum/anchor` із трьох, що горять — далі стверджував ВИТРАТУ й ще й ПЕРСИСТИВ це в `anchor.error_message`. ⚠️ **І текст самого алерта брехав про НАШ прилад:** казав «метрика розрізнення не несе», тоді як `ratio` ставиться лише при додатному порозі, отже `ratio == 0` ⟺ `balance == 0`; правку в репо ще треба довезти 👤-імпортом — `import.rb` односторонній (гоча 20). ⊕ 🔴 **ЧЕТВЕРТИЙ стан, сильніший за всі три: ключ ПРИСУТНІЙ, придатний, гейт зелений — і нога все одно мертва, бо її відкидає ВЕНДОР** [INF.22, виміряно 2026-09-07]. `FILECOIN_API_KEY` заведено 09-05, `verify-secrets` зелений, значення валідний JWT — а `filecoin_archive_exhausted_total`=**134** при `filecoin_unarchived_depth`=**67** і `repin_total`=**67**: reconcile двічі підняв ті самі 67 записів, і всі 134 спроби осіли в DeadSet, бо Pinata вичерпала квоту плану за два дні демо-трафіку. **Драбинка станів ключа тепер повна: ВІДСУТНІЙ ⊥ ПРИСУТНІЙ-непридатний (плейсхолдер) ⊥ придатний-без-РЕСУРСУ (нуль монет) ⊥ придатний-із-ресурсом, але відкинутий КВОТОЮ.** Presence-чек сліпий до всіх трьох останніх, і кожен наступний дорожчий, бо виглядає зеленішим. 🔑 **Прилад, що розводить їх, — не наш: ним виявився ЛИСТ ВЕНДОРА про ліміт плану.** Ми не мали жодного сторожа на «нога працює, але марно», хоча стеля була ОГОЛОШЕНА в коді (`filecoin_reconcile_worker.rb`, «re-populate money-path Dead-Set… фікс при першому live-трафіку») і тригер її настав 09-03. **Оголошена стеля без сторожа — це передбачення, яке справдиться тихо.**

27. 🔴 **Мертвий писач НЕ прибирає ряд — він його МОРОЗИТЬ, тож «серія є» ніколи не є доказом живого приладу** [S2.4, 2026-09-06]. Prometheus-ґейдж віддається на КОЖНОМУ скрейпі, доки живий процес, тож `count_over_time(m[7d])` рахує скрейпи, а не записи: воркер, що помер тиждень тому, дає такий самий безперервний ряд останнім значенням. Свідчить цей вимір рівно в один бік — **нуль семплів ⇒ писач не спрацював жодного разу** (так і знайшовся `cluster_tree_count_drift`, чий `.set` стоїть за `return if cached == live`, тобто відсутність там є ЗДОРОВʼЯМ). Хочеш знати, чи ряд мусить існувати — читай ФОРМУ `.set` у коді, не крон воркера й не графік. ⊕ **Смертей дві, і `noDataState` не розводить їх у принципі:** метод кинув і `rescue` ковтнув ⊥ воркер не біг узагалі. `NoData` спрацьовує лише коли ряд ЗНИКНЕ (рестарт процесу), а голодування черги лишає його замерзлим — тож правило з `noDataState: NoData`, назване «зонд не біжить», накриває смерть ПРОЦЕСУ, не голодування ВОРКЕРА. Один лік на обидві: штамп `*_timestamp_seconds`, поставлений ПІСЛЯ повного проходу (`06_03 §2.9`). ⛔ **І НЕ дописуй `> 0` до штампа у виразі правила — цей фільтр робить дед-мена сліпим саме до того, що він ловить.** Безлейблову серію `prometheus-client` реєструє зі значенням 0.0 від СТАРТУ процесу, тож `> 0` викидає її → порожній вектор → NoData → при `noDataState: OK` правило ЗЕЛЕНЕ; рестарт контейнера + писач, що не піднявся жодного разу, лишаються невидимими НАЗАВЖДИ. Легітимний фільтр рівно тоді, коли каденс писача ДОВШИЙ за вікно `for` (добовий `partition-sampler` — так; `*/5` і `*/15` — ні: там Pending резолвиться сам). Носій — ратчет `spec/deploy/grafana_alerts_spec.rb` з allow-list на uid. 🔑 Клас ширший за Grafana: **токен скопійовано разом із ФОРМОЮ, а його ПІДСТАВА не поїхала** — питай не «як роблять сусіди», а «що робило це правдивим у сусіда, і чи воно правдиве тут». 🔴 **Але «після проходу» ЗАМАЛО, якщо всередині є ковтальний `rescue` — і тоді штамп бреше переконливіше за відсутність.** У `Treasury::MonitorService` три підпроходи ковтають СВІДОМО (спостережність не сміє валити money-цикл), тож безумовний `set` у кінці `perform` просувався б після проковтнутого збою: прилад свідчив би «воркер СТАРТУВАВ» там, де питання «прохід ДІЙШОВ ДО КІНЦЯ». **Перш ніж ставити штамп — випиши КОЖЕН `rescue`, `return`, `next` між початком проходу й ним, і спитай про кожен: чи він означає ЗБІЙ, чи ЗАВЕРШЕНО-без-серії.** Перше мусить знімати штамп (підпрохід повертає булеве, штамп на кон'юнкції); друге — НІ, інакше дістанеш дзеркальний дефект: вічно замерзлий штамп на стенді, де опційна нога просто не заведена, і новий алерт, що горить завжди (⊂ гоча 24). Обидві половини пінуються окремо, і ліхтар на другу обовʼязковий — усі піни на першу негативні, тож її регресію вони не бачать. ⚠️ **І свій власний grep переміряй теж:** `grep -c "silkennet_<m>[^_a-z]"` дає 0 на рядку `expr: silkennet_gateways_faulty`, бо клас вимагає символу, а рядок там і закінчується — писач мало не випав із периметра через дужку, а не через стек (лік — `\b`).

28. 🔴 **Вимикаєш ногу свідомо — перечитай ОПИСИ її алертів: вони написані для світу, де вимкнення є ІНЦИДЕНТОМ, і накажуть оператору скасувати щойно ухвалене рішення** [INF.22, 2026-09-07]. Знявши `FILECOIN_API_KEY` (activation-gate, ⚖️ founder), ми лишили два правила, чиї `description` казали «Перевір Pinata API status + **filecoin_api_key**» і «**Звір Pinata-креденшли**». Обидва спрацьовують саме тоді, коли гейт працює ПРАВИЛЬНО — і обидва ведуть до заведення ключа назад. ⚠️ Це не шум, а **людський носій із хибним приписом** (клас `feedback_human_executed_surface`): алерт читають о третій ночі, і його текст сильніший за трекер, бо він у місці дії. 🔑 Лік — НЕ silence: сайленс ховає й правдиву половину сигналу, потребує поновлення і має власний строк; **лік — перший рядок опису, що називає ⚖️, стан гейта і ТРИГЕР повернення**, після чого число в тривозі перестає бути збоєм і стає ГЛИБИНОЮ ВІДКЛАДЕНОГО БОРГУ — тобто мірою роботи при реактивації. ⛔ **І перевір, чи сайленс узагалі потрібен, перш ніж його робити:** у нас маршрутизація `slot=canopy` веде в контакт-точку-заглушку `empty` (`/api/v1/provisioning/policies` + `/api/v1/provisioning/contact-points`), тож вічно-палаючий алерт не будить нікого — сайленс був би ліками від неіснуючої хвороби. **Питай про алерт-втому ЧИСЛОМ маршруту, а не відчуттям.** ⚠️ Правка опису живе в репо й НЕ діє, доки не поїде 👤-імпорт (`import.rb` односторонній — гоча 20), тож між правкою і імпортом стек і далі несе стару інструкцію.
