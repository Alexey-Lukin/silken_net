{
  "ignored_warnings": [
    {
      "fingerprint": "965acb5849720e216bd24db753af56c9366521d4a6a12665f21b038b3b6f1e33",
      "note": "Kredis.lock is a Redis distributed lock, not SQL. String interpolation is used for the Redis key name in blockchain burning service (INF.22: dedicated ORACLE_SLASHER_PRIVATE_KEY, fallback chain removed; the key derives from Eth::Key.address, never user input). [SEC.17] The fingerprint moved because the address now comes through the Web3::OracleSigner seam instead of a direct Eth::Key call — the expression changed, the grounds for the ignore did not."
    },
    {
      "fingerprint": "5b88435adb1e5165d1804612b6bca650e9109d69b60d01b268e493a41d9d0386",
      "note": "Kredis.lock is a Redis distributed lock, not SQL. String interpolation is used for the Redis key name in blockchain minting service; the key derives from Eth::Key.address, never user input. [ARCH.106] The TTL now lives in the MINT_LOCK_TTL constant rather than a literal — deliberately no number here: this fingerprint is keyed on the expression, so a note that restates the value goes stale in exactly the pass that re-fingerprints it (the previous note still said '30s to 120s' after the value moved). [SEC.17] Re-fingerprinted again when the signer moved behind the Web3::OracleSigner seam — this is the second refactor to invalidate these five entries, and both times the breach surfaced only in CI, since no local hook runs brakeman."
    },
    {
      "fingerprint": "5491eff8051a01293f4c9b63079b4a7788e87cb672680521d74759f15fabad50",
      "note": "Kredis.lock is a Redis distributed lock, not SQL. String interpolation is the Redis key name in Celo reward service (ARCH.50: chain-prefixed lock; INF.22: fallback chain removed, dedicated ORACLE_CELO_PRIVATE_KEY only; the key derives from Eth::Key.address, never user input). [SEC.17] The fingerprint moved because the address now comes through the Web3::OracleSigner seam instead of a direct Eth::Key call — the expression changed, the grounds for the ignore did not."
    },
    {
      "fingerprint": "c4a3e2bb3ffbea7d31691399123b3ea7b2128dc61dcf6c56cad8a23dd79525cb",
      "note": "Kredis.lock is a Redis distributed lock, not SQL. String interpolation is the Redis key name in Etherisc claim service (ARCH.49 nonce-serialization; INF.22: dedicated ORACLE_ETHERISC_PRIVATE_KEY replaced the shared base-EOA; the key derives from Eth::Key.address, never user input). [SEC.17] The fingerprint moved because the address now comes through the Web3::OracleSigner seam instead of a direct Eth::Key call — the expression changed, the grounds for the ignore did not."
    },
    {
      "fingerprint": "6e90929d0f2b6bd0e77546601121fb893de2f10d903e1c375edef0e544dc6a89",
      "note": "Kredis.lock is a Redis distributed lock, not SQL. String interpolation is the Redis key name in PuroEarth passport service (ARCH.49 nonce-serialization; INF.22: dedicated ORACLE_PURO_PRIVATE_KEY replaced the shared base-EOA; the key derives from Eth::Key.address, never user input). [SEC.17] The fingerprint moved because the address now comes through the Web3::OracleSigner seam instead of a direct Eth::Key call — the expression changed, the grounds for the ignore did not."
    }
  ],
  "updated": "2026-08-26 21:00:00 +0000",
  "brakeman_version": "8.0.6"
}
