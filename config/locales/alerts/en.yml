---
en:
  alerts:
    index:
      filter_all: all
      filter_aria_all: Show all alerts
      filter_aria_severity: Filter alerts by %{severity} severity
      filter_critical: critical
      filter_low: low
      filter_medium: medium
      subtitle: Monitoring live telemetry for anomalies and baseline deviations.
    index_title: Alerts Command
    messages:
      actuator_fault: 'Actuator ‘%{name}’ failed (%{endpoint}). Reason: %{reason}. A patrol visit is required.'
      actuator_fault_unknown: Actuator ‘%{name}’ failed (%{endpoint}). Cause unknown (no CoAP reply). A patrol visit is required.
      actuator_stuck: 'Actuator ‘%{name}’ (%{endpoint}) has been recorded active for %{stuck_for_min} min past the window of command #%{command_id}. Rails lost track of it and reset the record. The physical state is unknown — verify the device on site.'
      actuator_stuck_stop_queued: 'Actuator ‘%{name}’ (%{endpoint}) has been recorded active for %{stuck_for_min} min past the window of command #%{command_id}. Rails lost track of it, queued a STOP for the next gateway poll and reset the record. The physical state is unknown — verify the device on site.'
      attractor_destabilised: "🌀 ATTRACTOR: Destabilisation (Z: %{z_value})."
      burn_failure: 'Critical burn failure for %{amount} SCC. Possible loss of control over the contracted balance. Error: %{error}'
      burn_failure_ambiguous: 'Critical burn failure for %{amount} SCC — the transaction may have landed; DO NOT retry automatically, reconcile on the explorer first. Possible loss of control over the contracted balance. Error: %{error}'
      canary_trip: "🐦 CANARY: __stack_chk_fail on the node (stack frame overwritten — possible attack on the LoRa/AT parser; the node reincarnated itself). DID: %{did}, seq: %{seq}"
      chainsaw_detected: "🪚 LOGGING: Acoustic anomaly without a thermal signal. DID: %{did}"
      chainsaw_detected_panic: "🪚 LOGGING: Acoustic anomaly without a thermal signal (PANIC-TX). DID: %{did}"
      cluster_data_blackout: 'Cluster-wide data blackout (%{target_date}): possible gateway failure / Starlink blackout (force-majeure). Slashing NOT applied — Field Audit required (Category C, 05_05 §5).'
      cluster_small_sample_degradation: 'Small-sample degradation (%{target_date}): %{critical_count} of %{total_trees} trees critical. The percentage threshold degenerates below 5 trees — a single tree crosses it — so NO auto-slash; Field Audit decides (Category C, 05_05 §5).'
      emergency_response_all_unavailable: 'Emergency response NOT dispatched: no %{device_type} actuator in this cluster is reachable right now. Installed: %{installed}; behind a silent gateway: %{silent_gateway}; not in a working state: %{out_of_service}. This protocol step was not executed; the other steps are unaffected.'
      emergency_response_no_actuator: 'Emergency response NOT dispatched: this cluster has no %{device_type} actuator at all — the hardware this protocol step needs was never installed. The step was not executed; the other steps are unaffected.'
      emergency_response_over_ceiling: 'Emergency response NOT dispatched to actuator ‘%{name}’ (%{endpoint}): it is rated for %{limit_s} s of continuous action, but a single command in this protocol runs %{chunk_s} s. No command was issued to THIS actuator; the rest of the cluster is unaffected.'
      emergency_response_too_slow: 'Emergency response NOT dispatched to actuator ‘%{name}’ (%{endpoint}): the gateway fleet is polled at worst every %{cadence_min} min, while this response stays meaningful for only %{relevance_min} min. The command would expire before it was ever asked for. No command was issued to THIS actuator; the rest of the cluster is unaffected.'
      entropy_pre_stress: "🎲 PRE-STRESS SIGNAL: Z-distribution entropy of cluster %{cluster_name} dropped to %{entropy_score} (threshold: %{threshold}). Analysis of %{sample_count} measurements over the last 24 hours points to homogenisation of tree responses — forest stress may be starting."
      field_audit_escalated_c_to_a: 'Field Audit %{date}: node %{uid} enclosure breached — act №%{act}, photo %{photo}. Escalation C→A: direct Category-A evidence recorded on site.'
      fire_detected: "🔥 CATASTROPHE: Temperature %{temperature_c}°C (threshold: %{fire_limit}). Fire risk!"
      firmware_fault: "⚙️ FIRMWARE FAULT: mruby VM error / device unprovisioned — re-flash/OTA required. DID: %{did}"
      firmware_reverted: "⏮️ FIRMWARE REVERTED: node on embedded baseline, OTA version %{burned_version} burned by fallback — re-issue only with a version above it. DID: %{did}"
      fraud_telemetry_detected: "🚨 FRAUD: fraudulent telemetry detected for %{target_date}."
      gateway_freezing: "❄️ FREEZING: Queen %{uid} (%{temperature_c}°C). LiFePO4 offline risk — charging below 0°C is unsafe."
      gateway_hardware_fault: "🛠️ Hardware fault on Queen %{uid}. Inspection required."
      gateway_overheat: "🔥 OVERHEAT: Queen %{uid} (%{temperature_c}°C). Risk of SIM7070G degradation."
      gateway_uplink_degraded: "📵 UPLINK: Queen %{uid} accumulated %{fail_count} failed flush conversations — LTE/Starlink is degrading, telemetry is holding on retry."
      gateway_weak_signal: "📡 LINK: Weak signal on %{uid} (CSQ: %{csq}). Risk of batch loss."
      global_blackout: "🛰️ GLOBAL BLACKOUT: no telemetry arrived for %{target_date}. Possible Starlink outage or mass gateway failure — Field Audit (Cat-C), slashing NOT applied."
      hardware_decay: 'Hardware Decay: node %{did} requires replacement.'
      hydrological_stress: "💧 DROUGHT: Hydrological stress."
      insurance_candidate_armed: 'Insurance candidate #%{id}: the AI oracle sees %{percentage}% critical trees — INDEPENDENT confirmation (dClimate satellite / Field Audit) is required before payout (dual-trigger, 05_05 §6).'
      insurance_no_data: 'Insurance oracle #%{id}: zero AiInsight for %{target_date} while trees are active (possible catastrophe / destroyed sensors). Payout NOT zeroed — Field Audit required (Category C, 05_05 §5/§6).'
      insurance_reserve_hold_aggregate_cap: 'Insurance reserve-gate HOLD [INS.2] for #%{id}: 24h internal insurance emission %{window_sum} %{token_type} exceeds the cap %{cap}. Payout stopped — reconcile exposure before a manual resolve.'
      insurance_reserve_hold_eval_error: 'Insurance reserve-gate HOLD [INS.2] for #%{id}: the reserve could not be evaluated, so the gate failed closed. Error: %{error}'
      insurance_reserve_hold_reserve_inadequate: 'Insurance reserve-gate HOLD [INS.2] for #%{id}: 30d internal insurance emission %{outstanding} exceeds reserve %{reserve} × %{ratio}. Payout stopped — top up DAO_TREASURY before a manual resolve.'
      mint_volume_anomaly: 'Mint-volume anomaly [ARCH.62] %{token_type}: %{volume} minted in the last %{window} exceeds the configured ceiling (%{ceiling}). Possible firmware/pipeline bug or misused MINTER key. Verify recent mint tx before topping the ceiling.'
      non_fire_peril: 'The peril behind alert #%{alert_id} cannot be adjudicated by the fire satellite (FIRMS sees fire only) — neither confirmed nor refuted. Payout and verdict stay frozen until physical inspection: a site visit is required.'
      obscured_critical_fire: "🔥 Critical fire alert #%{alert_id} is obscured by cloud/canopy — the satellite can neither confirm nor refute it. IMMEDIATE physical inspection required: waiting 48 h for an orbital pass is unacceptable here (life-safety)."
      oracle_balance_low: "%{network} %{signer} Oracle wallet balance (%{balance} %{currency}) is below the minimum threshold (%{min_threshold} %{currency}). Ratio: %{ratio}x. Blockchain transactions will fail without a top-up."
      power_loss: "🚨 CRITICAL: Power loss (%{voltage_mv} mV)! DID: %{did}"
      queen_ota_stuck: "👑 Queen %{uid} was stuck mid-OTA for %{stuck_for_h} h (firmware #%{firmware_id}). The campaign has been withdrawn and the gateway released, so it can receive future updates again; the firmware version is unchanged. Re-deploy manually after checking the link."
      queen_silent: "👑 Queen %{uid} has been silent for %{silent_for_min} min (last contact %{last_seen_at}). Cluster without uplink — tree telemetry is not arriving."
      queen_uplink_lost: "🆘 Queen %{uid} lost ALL uplinks (%{reason}; vcap=%{vcap_mv}mV, uptime=%{uptime_min}min). The cry arrived via Helium — telemetry is buffering, a visit is required."
      queen_uplink_lost_reported: "🆘 Queen %{uid} lost ALL uplinks (%{reason}; vcap=%{vcap_mv}mV, uptime=%{uptime_min}min, reported_at=%{reported_at}). The cry arrived via Helium — telemetry is buffering, a visit is required."
      slash_evasion_cluster: 'Slash evasion (cluster #%{cluster_id}): Category A evidence exists, but the holder moved out all SCC before the verdict was executed (requested %{requested_burn} SCC, on-chain balance ≈0). Burning is impossible — a legal/manual track is required (05_05 §3.2).'
      slash_evasion_tree: 'Slash evasion (tree %{tree_did}): Category A evidence exists, but the holder moved out all SCC before the verdict was executed (requested %{requested_burn} SCC, on-chain balance ≈0). Burning is impossible — a legal/manual track is required (05_05 §3.2).'
      slash_frozen_indeterminate_cluster: 'Slashing blocked (cluster #%{cluster_id}): Category A evidence exists, but the MAGNITUDE of the damage is indeterminate (no AiInsight data for the date). Nothing burned — Field Audit required (Category C, 05_05 §3.2/§5).'
      slash_frozen_indeterminate_tree: 'Slashing blocked (tree %{tree_did}): Category A evidence exists, but the MAGNITUDE of the damage is indeterminate (no AiInsight data for the date). Nothing burned — Field Audit required (Category C, 05_05 §3.2/§5).'
      slash_frozen_no_evidence_cluster: 'Slashing blocked (cluster #%{cluster_id}): no direct Category A evidence. Nothing burned — Field Audit required (Category C, 05_05 §3.2/§5).'
      slash_frozen_no_evidence_tree: 'Slashing blocked (tree %{tree_did}): no direct Category A evidence. Nothing burned — Field Audit required (Category C, 05_05 §3.2/§5).'
      tree_silent: "🌳 Node %{did} has been silent for ~%{silent_for_h} h (last transmission %{last_seen_at}, threshold %{threshold_h} h). Silent health and death/theft are indistinguishable until a heartbeat — go and listen."
    resolutions:
      gateway_returned: Queen %{uid} is back on the air (%{seen_at}).
      maintenance_restored: 'Restored by maintenance (record #%{record_id}).'
      mint_volume_detector_disabled: The mint-volume detector is disabled (`mint_volume_hourly_max_scc` = 0) — the alert is closed as moot, not as remedied.
      mint_volume_recovered: Mint volume of %{token_type} is back under the ceiling (%{volume} <= %{max} SCC).
      non_fire_peril: The FIRMS fire satellite does not adjudicate a non-fire peril — Field Audit (Cat-C); no real drought oracle exists yet.
      obscured_critical_fire: Critical fire alert obscured (cloud/canopy) — immediate Field Audit instead of the 48h orbital retry (life-safety).
      operator_closed: Closed by the operator.
      oracle_balance_recovered: 'Balance %{network}/%{signer} recovered: %{balance} %{currency} (threshold %{threshold} %{currency}).'
      orbital_exhausted: Orbital verification failed after prolonged canopy/cloud cover. Manual DAO audit required.
      system_closed: Closed by the system.
      tree_left_active: Node %{did} left active (%{status}) — a human/slashing case owns it now; silence is no longer anomalous.
      tree_returned: Node %{did} is back on the air (%{seen_at}).
    row:
      acknowledge: Acknowledge & Resolve →
      resolve_aria: 'Resolve alert #%{id}'
      resolve_confirm: 'Confirm threat localisation for alert #%{id}?'
      resolved: "✓ Resolved"
      severity_aria: 'Severity: %{severity}'
    severities:
      critical: critical
      low: low
      medium: medium
    show:
      assignment:
        claim: Take it
        claim_aria: 'Take alert #%{id}'
        release: Release
        release_aria: 'Release alert #%{id}'
        since: since %{at}
        title: Assignee
        unassigned: Nobody has taken this alert yet
      sop:
        domain:
          pending: Partner SOP content for this alert type is not available yet — domain playbooks are owned externally and arrive via the academic channel.
          title: Domain tactics (partner SOP)
        steps:
          acknowledge:
            body: Confirm the alert is being handled. Resolving it records the operator and timestamp in the audit trail.
            title: Acknowledge and resolve
          field_audit:
            body: If the alert affects a contract or the evidence is ambiguous, dispatch a field audit — physical inspection outranks any remote signal.
            cta: Log a field audit
            title: Field audit
          verify:
            body: Cross-check the event against satellite/climate data (dClimate) before physical action — the sensor is one witness, not the verdict.
            title: Verify with an independent witness
        title: Response protocol
    show_title: 'Alert #%{id}'
    table:
      alert_type: Alert Type
      command: Command
      message: Event / Message
      severity: Severity
      source: Source
      timestamp: Timestamp
    types:
      actuator_stuck: Actuator Stuck
      chainsaw_detected: Chainsaw Detected
      emergency_response_undeliverable: Emergency Response Undeliverable
      entropy_anomaly: Entropy Anomaly
      field_audit: Field Audit
      fire_detected: Fire Detected
      firmware_canary_trip: Firmware Canary Trip
      firmware_fault: Firmware Fault
      firmware_reverted: Firmware Reverted
      queen_offline: Queen Offline
      queen_uplink_lost: Queen Uplink Lost
      severe_drought: Severe Drought
      system_fault: System Fault
      vandalism_breach: Vandalism Breach
