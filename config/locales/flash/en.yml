---
en:
  flash:
    actuators:
      command_in_flight: Actuator already has an active command. Wait for it to complete.
      command_queued: Command queued — the node will pick it up on its next poll.
      idempotency_required: Idempotency-Key header is required for actuator command execution.
    alerts:
      acknowledged: 'Alert #%{id} acknowledged.'
      already_resolved: 'Alert #%{id} was already resolved.'
      invalid_severity: 'Invalid severity filter: %{value}.'
      invalid_status: 'Invalid status filter: %{value}.'
      resolved: Threat localised.
    blockchain_transactions:
      invalid_status: 'Invalid status filter: %{value}.'
      invalid_token_type: 'Invalid token_type filter: %{value}.'
    firmwares:
      deployment_dispatched: Evolution order for v%{version} queued — Queens will fetch it on their next post-flush poll.
      deployment_no_targets: 'OTA rejected: no eligible gateways to deploy to.'
      deployment_oversized: 'OTA rejected: firmware exceeds the Queen assembly ceiling (16 chunks x 512 B = 8 KB).'
      deployment_rejected_stale: 'OTA rejected: version does not exceed the cluster high-water mark (anti-rollback). Create a new firmware record.'
      file_too_large: File size exceeds the %{limit} MB limit.
      invalid_target_type: 'target_type must be one of: %{allowed}.'
      target_all: Entire forest
      target_cluster: 'Cluster #%{id}'
      target_type_mismatch: target_type does not match the firmware hardware type (%{expected}).
      uploaded: New evolution v%{version} uploaded to the Ocean.
    maintenance:
      hardware_pulse_missing: No pulse from the unit after this maintenance yet — verification needs the hardware to report in first.
      hardware_verified: Hardware state verified. STM32 pulse acknowledged.
      idempotency_required: Idempotency-Key header is required for JSON maintenance-record submissions.
      invalid_date: 'Invalid ISO8601 date: %{value}.'
      photo_deleted: Photo removed from the Evidence Matrix.
      photo_evidence_locked: This photo is evidence for a repair or installation record and cannot be deleted. Add a corrected frame instead.
      photo_not_found: Photo not found.
      record_created: Healing record committed. Ecosystem updated.
      record_updated: Record updated.
    notifications:
      updated: Notification settings updated.
    organizations:
      context_switched: 'Working context switched: %{name}.'
    provisioning:
      invalid_uid: Invalid hardware UID %{uid} — expected 24 hex chars (three 32-bit words of the STM32 silicon UID).
      node_initiated: 'Node initiation complete. DID: %{did}. Hardware UID: %{uid}'
      uid_taken: Device with UID %{uid} is already registered in the system.
    sessions:
      invalid_credentials: Invalid access coordinates.
      logout_success: Logout successful. The Gate is closed.
      neural_link_established: Neural Link Established.
      neural_link_severed: Neural Link Severed.
      rate_limited: Too many login attempts. Try again in a minute.
    settings:
      org_updated: Organization settings updated.
    telemetry:
      payload_too_large: Payload exceeds %{limit} KiB ceiling.
    tree_families:
      created: New species DNA woven into the network.
      updated: Biological constants recalibrated.
