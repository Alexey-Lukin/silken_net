---
en:
  ui:
    action_badge:
      actions:
        acting_organization_switched: Organization context switched
        actuator_bulk_cancelled: Actuator queue cancelled
        factory_flash: Factory flash
        hardware_key_rotated: Hardware key rotated
        maintenance_photo_purged: Evidence photo purged
        slash_verdict_burn: 'Slash verdict: burn'
        slash_verdict_evasion: 'Slash verdict: evasion'
        slash_verdict_frozen: 'Slash verdict: frozen'
        stream_epoch_rotated: Stream epoch rotated
        system_parameter_changed: System parameter changed
        user_role_changed: User role changed
      transitions:
        actuator_command: Command → %{state}
        blockchain_tx: Transaction → %{state}
        naas_contract: Contract → %{state}
    measurement:
      coverage: "%{measured} of %{total} measured"
      not_measured: Not measured
    photo_card:
      confirm_remove: Remove this photo from the evidence record?
      remove_photo: 'Remove photo: %{filename}'
      view_photo: 'View photo: %{filename}'
    relative_time:
      ago: "%{time} ago"
    skeleton:
      loading: Loading…
    status:
      active: active
      aria_label: 'Status: %{status}'
      breached: breached
      cancelled: cancelled
      confirmed: confirmed
      deceased: deceased
      dormant: dormant
      draft: draft
      failed: failed
      faulty: faulty
      fulfilled: fulfilled
      idle: idle
      maintenance: maintenance
      maintenance_needed: maintenance needed
      manual_review: manual review
      offline: offline
      pending: pending
      processing: processing
      removed: removed
      sent: sent
      updating: updating
    web3_address:
      copied: Address copied
      copy: Copy address
      copy_aria: Copy address %{address} to clipboard
      copy_failed: Copy failed
      not_provisioned: NOT_PROVISIONED
