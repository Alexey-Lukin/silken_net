# Code ownership → auto-requests review from the owner on a PR touching any path.
#
# Solo repo: one owner, one rule. A per-area split would be nine copies of the
# same name — org-chart shape with no org behind it. Split it the day a second
# person actually reviews something, not in anticipation of one.
#
# NB: this only *requests* review until branch protection turns on
# "Require review from Code Owners" (off today — owner uses admin-bypass).

*                          @Alexey-Lukin
