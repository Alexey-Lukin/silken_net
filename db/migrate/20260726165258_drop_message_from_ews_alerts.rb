# SPDX-License-Identifier: AGPL-3.0-or-later
# frozen_string_literal: true

# Замикання переходу: усі 19 писальників (плюс фабрики й сіди) переведені на
# `message_key` + `message_params`, тож колонка `message` лишилась єдиним
# другим джерелом істини для однієї й тієї ж фрази — і йде геть.
#
# Перехід свідомо йшов ДВОМА міграціями, а не однією: 19 сайтів у 15 файлах не
# перевіряються одним блобом, і зелена сюїта після кожного кластера — єдиний
# спосіб знати, що кожен переведений правильно. Колонка на цей час працювала
# фолбеком у перевизначеному читачі `EwsAlert#message`; фолбек знімається
# разом із нею, бо «тимчасові» шляхи, які нікуди не ведуть, переживають усіх.
class DropMessageFromEwsAlerts < ActiveRecord::Migration[8.1]
  def change
    # `safety_assured`: StrongMigrations резонно боїться drop_column на живій
    # БД (старий код у процесі деплою ще пише в неї). Тут прод-БД не існує
    # (pre-launch), а писальників не лишилось — перевірено грепом і сюїтою.
    safety_assured { remove_column :ews_alerts, :message, :text }
  end
end
