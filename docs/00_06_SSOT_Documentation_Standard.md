# 00_06: SSOT Documentation Standard & Drift Prevention

## 🎯 Мета

Канонічний дім **стандарту самих SSOT-документів**: як писати й супроводжувати `docs/NN_MM_*.md`, щоб не накопичувався SSOT-drift. Фіксує doc-skeleton, реєстр канонічних домів (*одна річ — один дім*), CI-enforced drift-tooling та метод реструктуризації/виносу теми в нову сторінку. Це той стандарт, на який спирається скіл `ssot-maintenance` (він — операційний HOW, цей документ — самі правила). Поширюється на всі модулі.

---

## ✅ Статус

- **Поточний TRL:** TRL 8 — стандарт впроваджено та **CI-enforced** (`docs:check_refs` + `tracker:check` як HARD-гейти у `docs.yml` — єдиний дім, §3). Усі roadmap-гейти §3 реалізовані (останній — TRL range-consistency); ref-graph лишається advisory/on-demand **за дизайном**, не як борг (magic-marker → HARD 2026-07-18, DOC-T.46).

---

## 🔗 Cross-references

| Ресурс | Опис |
|--------|------|
| [`00_00` — SSOT Index](00_00_SSOT_Index) | Reading-order + повний реєстр сторінок |
| [`00_02` — AI Native Engineering and TRL](00_02_AI_Native_Engineering_and_TRL) | AI-Native філософія + Wiki-First (no code until spec approved) |
| [`00_03` — TRL Matrix HIL and Beyond](00_03_TRL_Matrix_HIL_and_Beyond) | Per-module TRL matrix — канон-дім (§2 registry посилається сюди) |
| [`00_05` — GitHub Projects and IaC Automation](00_05_GitHub_Projects_and_IaC_Automation) | Workflows, що ганяють ці гейти (`docs.yml`, `ssot_guard.yml`); Projects/Labels SSOT |
| `lib/docs_linter.rb` · `lib/docs_toc.rb` · `lib/tasks/docs.rake` · `lib/wiki_link_normalizer.rb` | Engine'и drift-tooling (§3) — pure-функції, unit-tested |
| [`00_07` — Action Plan Tracker](00_07_Action_Plan_Tracker) | Канонічний дім блокерів (§1) + open backlog |

## 📑 Зміст

<!-- TOC:AUTO:START -->
- [1. Canonical doc skeleton](#-1-canonical-doc-skeleton)
- [2. Canonical-home registry (одна річ — один дім)](#-2-canonical-home-registry-одна-річ--один-дім)
- [3. Drift-prevention tooling (CI-enforced)](#-3-drift-prevention-tooling-ci-enforced)
- [4. Module-restructure / extract-to-new-page](#-4-module-restructure--extract-to-new-page)
<!-- TOC:AUTO:END -->

---

## 📐 1. Canonical doc skeleton

```
# NN_MM: Title
## 🎯 Мета
## ✅ Статус            — власний (member) TRL доку + rationale; агрегат-матриця лише в 00_03 §1; +1 рядок «відкриті → 00_07 §NN»
## 🔗 Cross-references  — ОДИН раз, угорі зразу під Статус: sibling-доки + ключові файли + 00_07-link
## 📑 Зміст             — авто-ToC між <!-- TOC:AUTO --> маркерами (h2 контенту); regen `bin/rails docs:toc`
## <Content>           — інженерна суть; поточні обмеження документуються прозою тут
```

- **Блокери — НЕ в каноні.** Канон-док **не тримає** секцій `🛑 Блокери` чи `✅ Архів вирішених блокерів`. **Усі** блокери — і відкриті, і закриті — живуть **тільки в `00_07`** (відкриті → §модуль-реєстр як тонкі вказівники → канон; закриті → `00_07 §🗄️ Архів`). Канон описує дизайн і **відоме обмеження прозою в body** («як воно є»); чому member-TRL не вищий — 1 рядок у Статус із рефом `→ 00_07 §NN`. `docs:check_refs` ловить будь-яку лишкову блокер-секцію (§3).
- **Cross-references — угорі, DRY.** ОДНА секція зразу під Статус: sibling-доки + ключові файли + `00_07`-link (канонічний дім блокерів). Прибрано дубль: окремий список «Пов'язані модулі» в Статус та окрема References-секція внизу більше НЕ використовуються — це й була основна дуплікація.
- **Крос-рефи — один стандартний формат.** Інлайн-посилання на канон: видима мітка веде з тим самим `NN_NN`, що й ціль href, а `§X` — **реальний номер секції** (`§4.1`, `§1A`, `§6.3`), НЕ описове слово. **Мітка ЗАВЖДИ веде з code-span `` `NN_NN` ``** — це ЄДИНИЙ дозволений діалект (HARD-enforced 2026-06-01), у трьох формах: `` [`05_05`](05_05_Slashing_and_Risk_Policy) `` (на весь док) · `` [`05_05 §3`](05_05_Slashing_and_Risk_Policy) `` (секція, `§X` = реальний номер) · `` [`05_05` — Slashing and Risk Policy](05_05_Slashing_and_Risk_Policy) `` (directory-рядок: title ПОЗА code-span). Заборонено plain `[05_05 …](Doc)`, escaped `[05\_05\_…](Doc)`, full-name-in-codespan `` [`05_05_Full_Name`](Doc) `` — це той самий реф у другому написанні. Описовий контекст (назва концепту, напр. «Web3CircuitBreaker») — у прозі/після em-dash ПОРУЧ, не в `§`-слоті й не всередині code-span. Prose-фразовий лінк (мітка без doc-id, напр. «`[живе тут](Doc)`») — легітимний. `docs:check_refs` (§3) флагає §-мітку без заголовка, label↔href-розбіжність і не-code-span мітку: **стандартизуй реф, а не послаблюй гейт** (один формат < багато форматів). Bulk-нормалізація — `scripts/normalize_crossrefs.rb`; таксономія/аудит — `scripts/crossref_audit.rb`.
- **Статус — lean.** Лише member-TRL + rationale (+ «відкриті → `00_07 §NN`»). БЕЗ build-state таблиць «Компонент | Стан» — стан компонентів живе в body + code-рефах Cross-references (інакше дубль body/00_07 → drift).
- **Без волатильних лічильників.** Не хардкодити в прозі к-сть тестів / рядків коду — дрейфують на кожен коміт (у 03_01 знайдено `264` vs `511` vs `83/105` в одному доку). Рефати на джерело (`make -C firmware/test`, suite) або генерувати (як метрики `06_03 §2.8`). Spec/wire-константи (21-байт фрейм, DR0..DR19, 0.47F) — design-факти, лишаються.
- **Зміст — авто.** `## 📑 Зміст` зразу під Cross-references; тіло між `<!-- TOC:AUTO:START/END -->` ГЕНЕРУЄТЬСЯ з h2-заголовків (`bin/rails docs:toc`), `docs:check_refs` падає при дрейфі. Потрібен бо Wiki не має авто-outline; ручний список заборонено (дрейф). Анкори = GitHub-слаги (`lib/docs_toc.rb`).
- **File Map** — опційно; згортається у Cross-references.

---

## 🏠 2. Canonical-home registry (одна річ — один дім)

Кожен факт має **ОДИН** SSOT-дім; усе інше — реф, не re-statement. Дзеркало позначати: «значення тут — дзеркало SSOT, правити там».

| Факт | SSOT home |
|---|---|
| Per-module TRL матриця | `00_03 §1` (агрегат-дім). Модульний док = власний member-TRL у Статус (це джерело, не дубль); заборонено відтворювати всю матрицю поза §1 |
| Beyond-TRL-9 / SRL-MRL R&D-агенда (Planetary Intelligence gaps + фрактальне масштабування) | `00_08` |
| AI-Native метод (AI Pipeline + 🚦 Validation Gate, Intent-First, Triple Stream, Agent Handoff) + AI-ростер (інстанси) | `00_02` (§2 ростер-знімок ролей frontier-LLM/coding-agent + §3–§4 метод; операційний потік→кластер mapping → `00_04`). Інші доки реферять **роль**, не вендора (guard §3) |
| AES per-channel modes | `03_05 §6` (зведена channel-таблиця) + `§3.7` (ключі/ARCH.42). Firmware-доки `03_01 §9` / `03_02 §8` реферять цю таблицю, не дублюють (DOC-T.14) |
| Двоключова LoRa key-model — session KEYL (per-device) · cluster control-plane KEYB (FW.2 (в)) | `03_05 §3.1` (модель/слоти/ротаційна семантика; ухвала — §2.1 flip-checklist). Фабрична деривація/запис → `03_06 §2`; інші доки реферять модель, не перелічують ключі |
| DePIN trust-origin ladder (L0→L1→L2) | `05_02` (Trust-origin ladder — рунги/гейти/статус; L2-механізм §E.60). SE/крипто-частина → `03_05 §3.7`. Інші доки реферять, не дублюють ladder |
| Merkle leaf-формула + anchor-структура (ARCH.12/E.60) | `05_02 §E.60` (canonical leaf = Z-based per-record CID; hash=sha256; паралельні якорі Polygon `archive_root` ‖ Eth-L1 `state_root`). Eth-L1 anchor-структура → `05_04 §Merkle`; L2 clawback-policy → `05_05 §3.3`. Інші доки реферять, не дублюють leaf (guard §3 — `telemetry_logs.chain_hash` ≠ leaf) |
| Secure Element part (SE050) + slot-map | `03_05 §3.7` (SE = **SE050**; AES-128 = вибір, не SE-constraint; slot-map; ladder → `05_02`). Міграція — `00_07` SE050-MIGRATION |
| L1 QATT batch-конверт (wire + повідомлення підпису + anti-replay) | `03_05 §2.2` (firmware-дзеркало розкладки: `firmware/common/queen_attest.h`; ladder-статус → `05_02`). Інші доки реферять, не дублюють формат |
| Lorenz константи | `03_04 §4.1` |
| Tokenomics rate / slashing thresholds / insurance pool / **Dynamic Tax** (ставка+поріг, S6.17 — governance-aware via `SystemParameter` ← `ProtocolParameters.sol`) | `05_03` · `05_05 §3/§4` |
| Carbon (2000 SCC = 1 tCO₂) | `05_03` + `07_01 §3` (business view) |
| Financial constants (business) | `07_01 §3` (з рефами на 05_03/00_01) |
| **Cost-домен — spec-at-source → aggregate-in-07_02 → price.** Component-spec (моделі + per-unit $) живе у технічному домі підсистеми; 07_02 АГРЕГУЄ (rollup→CAPEX→OPEX→ROI) через реф, не дублює spec; ціна — tokenomics | ⬇ розклад нижче |
| ├ Soldier BOM (component-моделі + per-unit $ + Electronics TOTAL) | `02_01 §3` (component-spec дім). Node-rollup/CAPEX — 07_02 §1.2; інші реферять, не дублюють |
| ├ Queen BOM (component) | `02_05 §7`. Node CAPEX — 07_02 §4 |
| ├ Анкер виробничий cost (DMLS друк / HIP) · EBFC biochem cost | `01_02 §6` · `01_03 §5`. Rollup — 07_02 §1.2/§3 |
| ├ Node $/вузол (Soldier/Queen) · cluster CAPEX (waterfall) · OPEX · ROI/payback (waterfall) | `07_02` §1.2/§4 (node) · §5.3 (cluster CAPEX) · §6 + §8a (OPEX) · §7.3 (ROI). Агрегує component-доми через реф; §5а/§7.2/§8a.4/§9 = дзеркала кривої |
| └ Vendor-landscape / DMLS-хаби / Frame Agreement (production-procurement) | `07_02 §8` (`protocols/procurement/rfq_registry` реферить рядок, не дублює) |
| Центральна провідна шина анкера (топологія · матеріал · ізоляція каналу · термінус · реалізація) | `01_01 §1.4` — monolithic = сплав анода (HW.24-gated); Ø → frozen-dims `01_01 §1`; термоміст → `01_01 §4.1` + in-silico `54`; механіка → in-silico `55`; матеріал/реалізація-рішення → `00_07` HW.34. Інші доки реферять, не дублюють |
| Slashing penalty formula | `05_05 §3` ↔ `BlockchainBurningService` |
| Money-path crash-window idempotency (intent-marker + `in_flight` guard) | `04_02 §4/§10` ↔ `BlockchainBurningService` / `Solana::BatchPayoutService` |
| Lorenz Z↔health ground-truth / de-risk протокол | `05_05 §8` (калібрує пороги §3/§7; партнерський ростер ФОТІУС/ЧНУ → `07_03 §1.1`) |
| Governance / DAO params (Governor, Timelock, ProtocolParameters) | `05_06` |
| Secrets inventory | `06_04` (canonical = `config/deploy.yml env.secret`) |
| Prometheus metric registry | `06_03 §2.8` (regen з `SilkenNet::Metrics::REGISTRY`) |
| DR / backup posture | `06_06` (config SSOT = `terraform/database.tf`) |
| CI/CD workflows + runbook index | `06_07` |
| Supply-chain hardening (Actions SHA-pin · harden-runner · Scorecard · IaC misconfig-scan (Trivy) + TF-drift · build-provenance signing · CLI-tool hash-pin) | `00_05 §2.7` (IaC policy); інвентар workflow → `06_07 §1`; signed-release verify-команда → `SECURITY.md`; дія/стан → `00_07` OPS.10 (supply-chain) / INF.22 (IaC-scan/drift) |
| GCP Cloud KMS keyring/IAM (disk-CMEK + mint-signer custody + tfstate-CMEK [SEC.22]) | `06_04 §5.6` (3 isolated keyrings `silken-disk-ew1`/`silken-sign-ew1`/`silken-tfstate-ew1` + separation-rules); mint-custody design → `06_04 §5.5`; стан → `00_07` SEC.17/SEC.22/INF.22. Інші доки реферять, не дублюють keyring-архітектуру |
| Security assurance case (threat model · trust boundaries · OWASP map · residual risks) | `SECURITY_ASSURANCE_CASE.md` (synthesis-дім — аргументує + реферить canon, **не** новий source-of-truth: AES-режими лишаються в `03_05`, slashing у `05_05`, тощо) |
| Публічний манифест (жанр: standalone маркетинг-нарратив) | `manifest.md` (genre-дім, DOC-T.41 — **не** source-of-truth: кожен факт у нім = дзеркало канону. Exempt з форм-лінтерів (bare-ref/line-ref) і rate-guard **за дизайном** — жанр несе числа в прозі без реф-апарату; лінію тримають (а) value-гейти без manifest-exempt (gp-clamp/deprecated/thermal/…), (б) rate-anchor, що при ре-прайсі явно називає manifest у чеклісті дзеркал (§3), (в) семантичні дрейфи — σ/ρ-інверсія, вигадані числа, 12 CONFIRMED у свіпі 07-16 — регекс НЕ ловить за природою → ручний свіп методом DOC-T.41 на цемент-сесіях. Живе в `docs/` свідомо; wiki-sync його не публікує (дзеркалить лише `NN_NN_*`)) |
| Академічний ростер (5 ВНЗ: партнер → що валідує → канон-дім) | `07_03 §1` (Реєстр Партнерів; §1.1 ЧНУ · §1.2 ЧДТУ · §1.3 ЧІПБ · §1.4 ЧМА · §1.5 СЄУ) |
| План публікацій + IP-рамка + бренд-архітектура | `07_03 §2` (публікації) · `07_03 §3` (IP) · `07_03 §5` (бренд) |
| B2G-гейткіпери Tier 1 (лісівники Дзюбенко/Сегеда) | `07_03` (ноти §1.2 ЧДТУ / §1.1 ЧНУ) |
| Certification/метрологія (Чорней, ЗВТ accuracy-нога) | `00_07` STK.5 (у §05) + `05_02` |
| Культурний шар (митці) | `cultural_layer.md` (ненумерований, поза wiki) |
| Firmware internals split | **Soldier** RAM/tests/lifecycle + RTC reg-map → `03_01`; **Queen** deep-dive (RAM `§9` · HAL `§10` · host-tests `§11` · `#define` `§0`) → `03_02`. Кожен реферить інший, не дублює (DOC-T.14) |
| Queen cluster capacity (Soldiers/Queen) | `02_05 §2.1` (capacity-math: baseline ~100, roadmap-стеля ~200; CIFO overflow). Business/FAQ-view `07_01` реферить, не дублює |
| Log-mel feature contract (FW.25) | `03_03 §3.4` (owner). Дзеркала: `firmware/common/logmel_contract.h` + `silken_ml.dsp.contract` (`tools/ml`); звірка через stamped `contract_hash` + `emit_c --check`. Таблиці генерує `silken_ml.codegen` |
| Lorenz mruby bytecode (FW.46) | джерело `firmware/bio_contracts/bio_contract.rb` (логіка — owner `03_04`); generated mirror `firmware/common/lorenz_bytecode.h` (mrbc); звірка — `tools/firmware/check_bytecode.py` (light sha-stamp) + `gen_bytecode.sh --check` (deep regen) |
| Metabolic-DCI канал (FW.57): wire несе **raw** `delta_t` + raw temp; backend декодує GP та бере raw temp для Z (НЕ calibrated `temperature_c` — той physical/display); точний GP↔`delta_t` recompute відкладено до FW.2 | `03_01 §13.6` (firmware→backend impact дім). Формула `metabolic_health`/GP-bands → `03_04 §4.3`; service-чеки (`check_metabolic_divergence!`) → `04_02`. Інші реферять, не дублюють |
| StatusByte wire-layout (post-FW.29: PanicFlag/status/growth_points = 1/2/5 біт, пак `(status<<5)｜gp`, маска `0x1F`) | логіка+пак-розкладка → `03_04` (owner); байт-позиція у wire-таблиці → `03_05 §2.1`. Ruby unpack (`03_01`/`05_02`) + Queen-парсер `03_02` + firmware-емітер реферять, НЕ рестейтять — 7 сайтів їдуть lockstep при wire-rev (FW.2 wire-rev2.1 рухає device_z). Layout value-guard = `DocsLinter.status_byte_layout_drift` (DOC-T.43, anti-retired — §3) |
| Firmware ARM build / toolchain pin / mruby build_config (FW.46) | `03_01 §12.4` (CMake + `firmware/cmake/arm-none-eabi.cmake` + `firmware/mruby/build_config.rb`; pinned submodules у `firmware/extern/`). Гейт — `ci.yml › firmware_arm_build` |
| Vendor / dependency pin-policy (FW.47) | `03_01 §12.5` — repo-wide external-dep pinning: firmware-native `extern/<dep>`@tag · contracts npm+lock · Python conda (`in_silico` → committed `conda-lock.yml`, гейт `lock_sync`; `ml` deferred — parity-self-guards). Інші доки реферять, не дублюють рішення |
| Solidity solc / pragma версія | `05_03` (Pragma-таблиця, «Dual Token System»). Код-SSOT = `contracts/foundry.toml` + кожен `*.sol` pragma; pin-policy → `03_01 §12.5`. Інші доки реферять, не дублюють (guard §3) |
| Factory flashing + provisioning ключів (HKDF per-device · Lorenz K_seed SEC.11 · OTA-HMAC FW.23 · factory-ops SEC.3) | `03_06` (виокремлено з 03_05 §3.4, 2026-06-14). Крипто-режими/SE050/ротація/IV лишаються в `03_05`; інші доки реферять конкретну `03_06 §N` |
| Procurement / RFQ-артефакти (vendor-specs · lead-times · hard-constraints · R&D-RFQ vs production-шар · IP/CDA-канал) | `protocols/procurement/rfq_registry.md` (concern-шар, як `paper/`/`outreach/`; per-domain `<domain>_<scope>_rfq.md` аркуші; vendor-landscape/BOM canon `07_02 §8`; IP-політика `07_03 §3`; академ-канал `07_03`; workflow-айтеми `00_07` HW.*/BIZ.*). Числа = дзеркало канон-домів; інші доки реферять рядок реєстру, не дублюють |
| §07 юр/бізнес робочі артефакти (MSA/ToS-каркаси · entity-структура · податкова постава · ™-бриф · securities-review · registry-matrix · E&O-спека · SLA-exhibit · enterprise/carbon/OSS-стандарт-ландшафти) | `protocols/legal/legal_registry.md` + `protocols/business/business_registry.md` (concern-шар, як `procurement/`; вхідні web-знімки — `protocols/research/`). **Особливість класу, якої не має жоден інший дім:** ці документи спираються на ЗОВНІШНЄ право/ринок, що рухається незалежно від repo → кожен несе `⏳ станом на <дата>` + статус чернетки + «не юридична порада»; **вік — не стиль, а валідність**. Наші факти всередині них (курси · ліцензійна мапа · entity-присуд · SLO) = **дзеркала** домів (`05_03` · `07_01 §3` · `07_03 §3` · `06_06 §3`/`06_08 §2.4`), правити там. Стан/рішення — `00_07` BIZ.*/UNI.* |

> **🔴 Міграційний залишок — найтихіший клас порушення One-Home** (`00_07 DOC-T.50`, архів). Коли інфраструктура/архітектура **півотить**, подія оновлює дім і `✅ Статус`, а **глибинні копії факту лишає жити** — і саме вони найнебезпечніші, бо сидять у файлах, з яких оператор тюнить систему. Доведений випадок: пів от web GCP→Akash видалив хост, але його сайзинг лишався живим у 9 сайтах, включно з `config/puma.rb` і `config/deploy.yml` (останній суперечив сам собі: один рядок казав «Rails runs on Akash», інший обґрунтовував воркери вже неіснуючою машиною). ⚠️ **Лік — не find-replace імені:** проста заміна назви створила б НОВУ брехню; правильно — перемітити копію як явний fallback-контекст або зняти. 🔴 **І сам фікс — теж джерело цього класу:** точкова заміна ОДНОГО числа створює суперечність із сусіднім рядком-підсумком, що з нього рахувався (доведено на `config/database.yml`: виправлений множник дав «84 на базу» поруч із незайманим «3 × 42 = 126») → після правки числа ПЕРЕЧИТАЙ арифметику навколо нього, а не лише сам рядок. Найстійкіший лік — **зняти число** й послатись на SSOT: те, чого нема, не розходиться. Тому після будь-якої migration-події: grep САМЕ ЗНАЧЕННЯ (розмір, тип машини, регіон, ім'я сервісу), а не лише назву дому, і окремо — конфіги, які читає людина. Інфра-факт за замовчуванням осідає в **3–12** домах; лічильникова нога цього ж класу → DOC-T.51.

> **Канон↔канон дубль-аудит — завершено** (`00_07 DOC-T.2`, архів). Єдина справжня ПОВНА ре-декларація факту (Lorenz-блок у `05_02`) усунена → owner `03_04 §4.1`; решта (AES/rate/carbon/slashing/insurance/gyroid/delta_t) — контекстні згадки, не spec-блоки, і стережені value-guard'ами (§3: lorenz/rate/carbon/solc/AES-roster). ⚠️ **Blind value-de-dup небезпечний** — колізія чисел (`02_03 «65%»` = MPPT-фракція VOC ≠ gyroid-пористість): масовий ref за значенням зіпсував би непов'язаний факт → рефати поза home лише по-кейсу з per-fact верифікацією. Автоматичний канон↔канон value-detect поза home відкладено (важко без doc-AST, FP-ризик).

---

## 🛡️ 3. Drift-prevention tooling (CI-enforced)

| Guard | Що ловить | Команда / місце |
|---|---|---|
| `docs:check_refs` | dangling `NN_NN` doc-links (hard) + §-section label drift (**HARD** з 2026-07-25, DOC-T.48 — був останнім advisory-пунктом із 34 категорій; 0 хітів, advisory з 2026-05-30, тоді як сусіди того ж тижня `bare_section_ref`/`bare_doc_ref` пішли HARD 2026-05-31 без записаної причини для винятку). **Value/owner-only гейти** (RTC/Lorenz/rate/solc/AI-vendor/anchor-dim/thermal/gp-clamp/deprecated/chain_hash) сканують і **розширену поверхню**: `docs/**/*.md` (protocols-піддерево) + `tools/cad/cem/*.json` як текст — DOC-T.42 ② закрив сліпу зону, де superseded `SF 3.4×` жив у `zone2_sleeve.json` підписаний «frozen» (structural/skeleton-гейти лишаються top-level: піддерево має власні реф-конвенції + не несе канон-скелета) | `bin/rails docs:check_refs` (docs.yml — required via `docs-ok`) |
| `tracker:check` | 00_07: dup-IDs (**whole-file global uniqueness** — кожен item-ID унікальний across усі секції вкл. 📌 Backlog / 🗄️ Архів; реюз через `all_item_ids`, єдиний span з inbound-guard'ом — закрив DOC-T.12 #### ↔ registry-table-row 2026-06-01 + `OPS.5` §07-heading ↔ 📌-backlog-row 2026-06-03), meta-line conformance (priority + **WHO**-виконавець + **STAGE**-лайфсайкл + canon-ref; WHO/STAGE = дві осі Projects-V2, моделюються окремо — DOC-T.18, раніше 🟡/🔗 хибно мапились як :blocked executor), canon-ref resolution + **§-section resolution** (a `NN_NN §X` pointer's §X must be a real heading — зловив 12 stale `§BLOCKER-N`/wrong-doc-id рефів, осиротілих blockers→00_07 sweep'ом; thread C 2026-05-31). Parser tolerant до emoji/✅-префікса і в `#### 🌿 ID`, і в table-row `\| ✅ ID \|` (раніше UNI.13a/BIZ.12 + status-prefixed backlog-рядки були невидимі усім чекам) | `bin/rails tracker:check` (docs.yml — required via `docs-ok`) |
| **section↔canon-home** | 00_07 canon-mirror One-Home: кожен `#### ` під `## §NN` має canon-ref модуля NN (`§NN/§MM`-заголовок декларує multi-module set; `§NNa/b/c` під-секція = той самий модуль NN — кілька секцій ділять модуль, курація за під-темою; 🔀/📌/🗄️ exempt). Закрив «§06-deploy-під-§04-DevOps» drift (15 S*/INF* айтемів ховалися під §04 за nav-нотатками) при canon-mirror реструктуризації 00_07 (2026-06-01) | `bin/rails tracker:check` (HARD; `lib/tracker/dashboard.rb`) |
| **inbound 00_07 item-ref** | reference з ІНШОГО доку виду `[`00_07` — <ID>]` (directory-link на 00_07) має резолвитись у реальний item (всі `####` + table-row IDs, **усі секції** вкл. 📌/🗄️ — `all_item_ids`). `tracker:check` раніше валідував лише ВЛАСНІ рефи 00_07, не inbound → renamed/removed item тихо протухав (зловив dangling `06_02 → 00_07 DOC.5`, оголений DOC.N-namespace роботою). Em-dash **опційний** (DOC-T.42 ③): діалект `[`00_07` ID](…)` без тире — той самий реф і не покривався НІЧИМ (prose-гейт бачить лише список у дужках ПІСЛЯ лінка). ID вимагає `.`/`-` сепаратора → directory-title лінк (`00_07 — Action Plan Tracker`) не FP | `bin/rails tracker:check` (HARD 2026-06-03, no-dash 2026-07-16; `lib/tracker/dashboard.rb`) |
| **prose 00_07 ID-ref** | ID, цитований у ПРОЗІ зразу після `00_07`-лінка (``→ [`00_07`](…) (S4.3, INF.4, S5.6)``), має резолвитись у реальний item. `inbound`-guard бачив лише em-dash `[`00_07` — ID]`, тож wrong-id (`S6.1` Redis замість GCS-bucket `S5.6`) і ref на ще-неіснуючий (`OBS.1` до появи рядка) тихо протухали — обидва зловлені 2026-06-03. Парсить повні ID-токени (digit-bearing → `X.*` wildcard без цифри пропускається), `/`-digit родини (`INF.3/4/6`) розкриває; token-shape-фільтр → прозові слова/`§X` не FP | `bin/rails tracker:check` (HARD 2026-06-03; `lib/tracker/dashboard.rb`) |
| **whole-file 00_07 §-ref** | КОЖЕН `NN_NN §X` ref БУДЬ-ДЕ у 00_07 (вкл. bare code-span у 📌 Backlog / 🗄️ Архів table-cells + прозу) резолвиться у реальний heading-anchor цілі. `section_dangling_refs` сканує лише `####` `it.canon` meta-рефи, а bare-§ guard 00_07-exempt → ~20 рефів, осиротілих Taxonomy-v3 §1.x-колапсом (08_01/08_02 §1.1–§1.8 → §1A/§1B / «Стаття N»), тихо протухли (E.50-53/E.59/ARCH.29-33 + UNI.4-5/STK.3/10 + 02_03 §4.А / 03_01 §11.5 / 04_02 §1.2 / 06_01 §6 / 08_01 §24a). Boundary-aware: `§1.3` НЕ резолвиться хибно проти `2.1.3`-заголовка (retired substring `include?` так робив); parent-group реф резолвиться через дітей (`§4а` ⇐ `4а.1..4а.5`); ranges спліт; lowercase `.x`-хвіст = wildcard placeholder (skip — реальні літерал-підсекції на `.X`) | `bin/rails tracker:check` (HARD 2026-06-09; `lib/tracker/dashboard.rb`) |
| **canon §-ref resolution** (DOC-T.28) | КОЖЕН numbered `NN_NN §X` ref у БУДЬ-ЯКОМУ канон-доці (00_01..08_02) резолвиться у реальний heading-anchor цілі — той самий boundary-aware `file_section_dangling_refs`, що `tracker:check` ганяє по 00_07, тепер і по всьому канону. Раніше канон cross-doc §-рефи мали лише слабший substring `section_label_drift` (ADVISORY) → тихо протухали `08_02 §1.x` / `05_03 §749` (номер РЯДКА!) / `02_03 §4.А`. `heading_anchors` тепер parent-aware для літерних підсекцій (`### A.`/`### А.` під `## 5.`/`## 4.` → `5.a`/`4.а`, Lat+Cyr) → точний `§5.A`/`§4.А` резолвиться сам; run-резолвер їсть `,;`+backtick → comma-список (`§2.9, §6`) перевіряє КОЖЕН член, не лише перший. Той самий резолвер живить report-only `scripts/code_doc_section_refs.rb` (app/spec/lib код-коментарі) + `scripts/protocols_ref_check.rb`. Exempt: 00_06 (цитує stale-рефи як drift-ПРИКЛАДИ) + 00_07 (tracker:check володіє, One-Home). Named (`§SLA`) рефи лишаються на `section_label_drift` — цей резолвер бере лише **секційну мітку**: digit-led АБО одна літера + `.` + цифра (`§A.2`, `§B.1.4`, `§E.60` — DOC-T.60, зняло сліпоту до цілого `04_06`); `heading_anchors` симетрично емітить такі мітки (строгий надмножинний набір) | `bin/rails docs:check_refs` (HARD 2026-06-16; `lib/tracker/dashboard.rb`) |
| **CHEM.N in-silico note-ref** | хімічні нотатки `HW.5.IS` — bulleted backlog, не `####` items — несуть власні `CHEM.N` ID (standardized 2026-06-06 з ad-hoc `note N`, що був unanchored і вже розповзся по 01_03/SUMMARY/L1/scripts+код → drift surface, founder-flagged). `chem_note_ids` бере ID-кластер ДО em-dash (compound `CHEM.22 + CHEM.5` / slash `CHEM.20/26`, checkbox опційний) → реф у ОПИСІ не плутається з def; guard скан `docs/**/*.md` **+ `tools/in_silico/scripts/*.py`** → кожен `CHEM.N` мусить резолвитись у визначену нотатку | `bin/rails tracker:check` (HARD 2026-06-06; `lib/tracker/dashboard.rb`) |
| **CHEM.N phantom-def hygiene** | голий `CHEM.N`-токен у CHECKBOX-bullet БЕЗ em-dash неоднозначний: `chem_note_ids` (em-dash-scoped) читає його як ref, а whole-line dup-scan — як 2-й *def* (саме так CHEM.14-dup проскочив committed-guard, поки строгіший локальний scan ловив). Status-bullet не сміє нести голий `CHEM.N` → reword або справжній `CHEM.N — …` def. Low-FP: лише no-em-dash checkbox-bullets з CHEM-токеном | `bin/rails tracker:check` (HARD; `lib/tracker/dashboard.rb`) |
| **00_07 item visibility** (DOC-T.49; `Tracker::Dashboard.orphan_item_violations`) | `parse` бачить `#### `-items ЛИШЕ всередині registry-секції (`## §NN` / `## 🔀`) → item **над** першою `## `-секцією (або під SKIP-секцією) невидимий парсеру, а отже й УСІМ іншим tracker-гейтам (dup-ID, meta-form, canon-ref, section-home, verdict-lead ітерують parsed-сет). Вони перевіряють наосліп і лишаються **зеленими на зіпсованому файлі** — та сама false-green форма, що дає склеєний table-row. Не гіпотеза: правка склеїла H1 і лишила `DOC-T.48` над усіма секціями цілий коміт (225 із 226 items розпарсено, обидва doc-гейти зелені). Mirror-guard: пересканує ТИМ САМИМ `####`-предикатом, що й `parse`, тож self-consistent із тим, що захищає. **HARD** з 2026-07-25 (0 порушень після фіксу) | `bin/rails tracker:check` (HARD; `lib/tracker/dashboard.rb#orphan_item_violations`) |
| **00_07 residual run-on** (founder 2026-06-14) | item-form: відкриті residual'и = **вертикальний список** (00_07 інтро); body-рядок з ≥2 інлайн чекбоксами (`· [ ] … · [ ]`) флагається — ховає residual'и від ока + glue-ризик у diff'ах. Skip: intro-blockquote приклади / fenced-code / table-rows. **HARD** (DOC-T.19 sweep §00/§07/§08 + named-таргети → 0 inline run-on 2026-06-14; guard тепер тримає лінію) | `bin/rails tracker:check` (HARD; `lib/tracker/dashboard.rb#inline_residual_runon`) |
| **00_07 verdict-lead** (DOC-T.19, founder 2026-06-14) | Universal-Стан: КОЖЕН registry `#### `-item body веде з `- **Стан:**` (присуд/essence + канон-pointer) — max однорідність, без «✅ X»-лід / prose-лід / bare-checkbox-лід (гетерогенність, яку founder «без сліз не міг бачити»). Флагає перший body-рядок після meta (`- **P?** …`), що не `- **Стан:**`; registry-scope як `parse` (REGISTRY_SECTION, не SKIP/📌/🗄️). **HARD** (DOC-T.19 sweep §00–§08 → 0 non-Стан ліди across усіх 138 items, 2026-06-14; guard тримає лінію — новий non-Стан лід валить CI) | `bin/rails tracker:check` (`lib/tracker/dashboard.rb#verdict_lead_violations`) |
| **00_07 meta-line form** (DOC-T.23, founder 2026-06-14) | item-form: meta-рядок = рівно `**P?** · WHO · STAGE · → канон-реф`. WHO ∈ {🤖, 👤, 🤖+👤} (канонічний AI-first combo — відхиляє `👤+🤖`/`👤/🤖`) + жодного `· tail` після канон-рефа (контекст типу `· ✅ ліцензія`/`· 🔗 UNI.16` живе в `**Стан:**`, не в meta). Executor-парсер на `include?` тихо толерував `👤+🤖` і tails (gate був зелений). **HARD** (DOC-T.23 sweep → 0 violations; guard тримає лінію). **`⚖️` decision-residual** (DOC-T.33, голова-vs-руки підтип `👤` — присуд, що НЕ зводиться до відомої дії): Ф1 (2026-07-11) = маркер на ЧЕКБОКСАХ (`- [ ] ⚖️ …`); Ф2 (2026-07-12) = `WHO_CANON` += `⚖️`-комбо → WHO ∈ {🤖, 👤, ⚖️, 🤖+👤, 🤖+⚖️, 👤+⚖️} (⚖️ сольне або trailing — decider не веде комбо), `EXECUTORS` += `⚖️`→`:decider` (сольний ⚖️-item не FP-ить «missing executor»); scan-on-section. **`⚫` vacuous-STAGE** (DOC-T.34 ②, 2026-07-12; founder-pick ⚫ замість ∅ — емодзі-ряд + ∅-діаметр-колізія): шоста STAGE-іконка «нема-що-завершувати» (premise спростовано/поглинуто; ≠ 🟢/🔗/🌿; пара ⚪↔⚫) — `STAGES`+`META_LINE` її знають, тож парсер розрізняє vacuous↔active, а item лишається на місці як closed-canon нотатка; STAGE-легенда = 00_07 §розмітка. | `bin/rails tracker:check` (HARD; `lib/tracker/dashboard.rb#meta_form_violations`) |
| **bench-session tag symmetry** (DOC-T.34 ①) | bench-сеанси = first-class: реєстр сеансів SSOT у `firmware/scripts/bench/RUNBOOK.md §6` (`\| [bench:slug] \| секції \| IDs \|` — рядок заякорено самим тегом, інші RUNBOOK-таблиці не FP-ляться), 00_07-item несе `[bench:slug]` на bench-чекбоксі. Двостороння симетрія: кожен тег ∈ реєстр + item у рядку сеансу; кожен ID рядка тегнутий. Закрив FW.8↔FW.20-клас cross-ref-асиметрії; grep тега = план стенд-дня. Header-скид current у tag-зборі (тег у 🔀-таблиці не липне до останнього #### — зловлено на першому прогоні) | `bin/rails tracker:check` (HARD 2026-07-12; `lib/tracker/dashboard.rb#bench_tag_violations`) |
| **00_07 дім-кластер marker** (DOC-T.34 ③) | координатор ⊃ важелі як ОДИН greppable маркер на item-заголовках: `[кластер:slug:дім\|важіль]` (формалізував ad-hoc «[дім X кластера]»/«[candidate-важіль Y]» — ARCH.8/E.59/FW.55/ARCH.26-кластери). Симетрія: рівно ОДИН `дім` + ≥1 `важіль` на slug (half-migrated кластер = drift); malformed-хвіст флагається. `[поглинув X]`-пара лишається вільною прозою | `bin/rails tracker:check` (HARD 2026-07-12; `lib/tracker/dashboard.rb#cluster_marker_violations`) |
| **00_07 stale WHO** (DOC-T.52, 2026-07-26) | meta-WHO = **об'єднання ВІДКРИТИХ** residual'ів (00_07 §розмітка), тож зашиплена машинна половина мусить ЗНЯТИ `🤖` з meta. Сліпа пляма: `meta_form_violations` валідує лише **форму** WHO-токена, ніколи — чи він ще ВІДПОВІДАЄ чекбоксам, тому item роками рекламує вільну 🤖-роботу, якої вже нема (2-й за частотою tracker-drift після STAGE 🟡→🟢; ловився лише очима, і саме він робить зріз «що зараз машинно-доступне» брехливим — знайдено 3 живих: `UNI.15`/`UNI.16`/`FW.20`). Флагає item, чий meta заявляє `🤖`, але жоден відкритий `- [ ]` його не несе. Два **емпірично виведені** винятки тримають FP на нулі (кожен був реальним хітом, що виявився чесним): `🔗`-лідований residual (делеговано в інший item — eventual-WHO живе ТАМ, напр. `BIZ.14` gated на SEC.1) і residual **без жодного WHO-гліфа** (`🌿`-лід — WHO не оголошено, з цього «машинна половина закрита» НЕ випливає: `ARCH.18`/`E.31` = справжня машинна робота, просто far-horizon). 🔴 **Друга вісь того ж контракту — `understated WHO`** (DOC-T.54, 2026-07-26): «об'єднання» двобічне, але перша реалізація ловила лише **завищення** (meta заявляє 🤖, якого нема) і була структурно сліпа до **заниження** — відкритий residual, чийого виконавця meta не оголошує. Ця вісь дорожча: meta-рядок **є scan-шаром**, тож чистий `👤`-meta над тілом із 🤖-residual'ами читається як «тут для машини нічого» (`HW.1` — P0 критичного шляху — ховав ШІСТЬ; **17** items корпусом, **7** із них у §01a — той самий шов, що метод описує як «ховається за 👤-фасадом». ⚠️ Число міряне ЗАШИПЛЕНОЮ версією проти до-фіксового трекера: перша, зламана, звітувала менше, бо власні дві помилки глушили частину її ж улову — тож не цитуй улов недороблених гейтів). Ті самі два винятки, і **третього не треба**: `⚖️ ⊂ 👤` в один бік (meta `👤` покриває відкритий `⚖️`, зворотно — ні), тому трійка `{🤖,👤,⚖️}` згортається в легальну пару `🤖+👤` і «об'єднання» здійсненне завжди. 🔴 **Перша версія цього гейта містила обидві помилки, які він мав ловити** (зняті adversarial-ревʼю того ж дня): (а) exempt на `kinds.size > 2` під приводом «фізично нездійсненне» — посилку спростовував рядок `⚖️⊂👤` трьома рядками нижче, а сам exempt глушив **7 із 17** реальних порушень, серед них три випадки тієї самої патології `HW.1` (чистий `👤`-фасад над машинною роботою), задля якої гейт писався; (б) виконавці збиралися `include?`-ом по всьому рядку, тож `👤`-чекбокс, що ЦИТУЄ закриту машинну роботу («§5.3 вже 🤖-verified»), читався як відкрита 🤖-робота — і «фікс» дописував у meta `🤖`, якого нема, тобто вироблявся рівно той DOC-T.52-дрейф, що стереже сусідня вісь. Обидві осі тепер якорять виконавця на **провідному токені** residual'а. 🔴 **Вісь overstate розширено з ОДНОГО гліфа на всі три + на порожню множину** (DOC-T.55, 2026-08-05; звідси й ім'я `stale_who` замість `stale_machine_who`): перша редакція читала буквально `include?("🤖")`, тож meta, що рекламує `⚖️`-присуд, якого вже ніхто не чекає — а це найдорожчий виконавець, — проходила зеленою назавжди; і вона виходила на `open.empty?`, тобто **порожня множина задовольняла її тривіально**, і закритий пункт зберігав повну WHO-вісь ні над чим. Покриття **асиметричне**, бо `⚖️ ⊂ 👤`: meta `👤` підкріплює відкритий `👤` АБО `⚖️`, meta `⚖️` — лише `⚖️`; виміряно на живому корпусі, що без цього правила 4 чесні пункти стають хибнопозитивами. Для нуля відкритих residual'ів чесні лише STAGE `🌿`/`⚫` (WHO називає МАЙБУТНЬОГО виконавця); `🔗` свідомо НЕ звільнено — заблокований пункт мусить називати тригер. ⚠️ Стеля, що подорожчала разом із розширенням: item-wide винятки (`🔗`-лід / без-гліфа residual) глушать **27 із 29** буквальних хітів — недетермінований виконавець підкріплює будь-який гліф, тож звузити його per-glyph не можна; зелений прогін тут — свідчення про пункти, чиї residual'и ВСІ оголошують виконавця, і ні про що інше | `bin/rails tracker:check` (HARD; `lib/tracker/dashboard.rb#stale_who` + `#understated_who`) |
| `ssot_guard.yml` | protected code змінено → docs мусять оновитись | CI PR gate, **advisory — не merge-blocking** (00_05 §2.3) |
| regen-from-code | _(on-demand — **не гейт**)_ enumerable lists (метрики) генеруються з SSOT, не вручну: `06_03 §2.8` несе `bin/rails runner`-однорядковик, що друкує таблицю з `SilkenNet::Metrics::REGISTRY`. Прогнати ПІСЛЯ зміни реєстру метрик. CI-гейта нема свідомо: він вимагав би Rails-boot у `docs_check`, який тримає pure-Ruby/no-DB профіль. ⚠️ Рядок стояв без маркера в таблиці, чия шапка каже «CI-enforced» — DOC-T.41-клас «заявлений gating без CI-виклику», що вижив УСЕРЕДИНІ реєстру (CHECK E не бачив: `06_03 §2.8` не матчить cmd-span-регекс → рядок мовчки пропускався) | on-demand (`bin/rails runner`, [`06_03 §2.8`](06_03_Prometheus_Observability)) |
| TRL presence | кожен док з `## ✅ Статус` декларує TRL (ловить 06_04-клас gap) | `bin/rails docs:check_refs` (hard) |
| TRL single-value | `00_03 §1` matrix-клітинки — одинарне 1-9, без діапазонів (00_05 §1.1) | `bin/rails docs:check_refs` (hard; `lib/docs_linter.rb`) |
| blocker-hygiene | канон-док не тримає `🛑 Блокери`/`✅ Архів` секцій — блокери лише в `00_07` (§1) | `bin/rails docs:check_refs` (**HARD** — sweep завершено 2026-05-30) |
| standard-conformance | кожен NN_NN-канон несе ✅ Статус + top 🔗 Cross-references + auto-ToC — **плюс H1** (DOC-T.49, 2026-07-25): H1 перевіряється **до** exempt-return, бо skeleton-винятки (`00_00`/`00_07`/appendix) існують через відсутній ✅ Статус, а от заголовка не сміє бракувати жодному. Це єдиний skeleton-дефект, невидимий решті гейтів, і він розякорює файл цілком: правка склеїла H1 `00_07` у попередній абзац (`#🔴 …` — без пробілу після `#`, тож CommonMark читає абзац, не заголовок) і всі doc-гейти лишались зелені цілий коміт. Перевіряє ПЕРШИЙ непорожній рядок (провідні порожні толеруються; `#x` без пробілу справедливо падає — це не заголовок) | `bin/rails docs:check_refs` (HARD; `lib/docs_linter.rb`) |
| ToC sync | docs з `TOC:AUTO` маркерами — зміст збігається з h2-заголовками | `bin/rails docs:check_refs` (HARD; writer `docs:toc`; engine `lib/docs_toc.rb`) |
| **fence balance** (DOC-T.45; `DocsLinter.unbalanced_code_fences`) | незакритий ``` code-fence (непарна к-сть fence-маркерів) лишає `in_fence`-toggle застряглим `true` до EOF → КОЖЕН з ~9 fence-aware guards (`next if in_fence`) німіє + auto-ToC обрізається (`DocsToc.content_headings` губить хвостові заголовки). Детермінований структурний інваріант (0 owner-винятків, 0 легіт-непарних доків) — рахує fence ТИМ САМИМ предикатом (рядок починається з трьох backtick), що й toggle-guards, тож self-consistent із тим, що захищає (tilde `~~~` / inline / indented ігнор — як і guards). Репортить рядок відкриття; сканує весь `docs/**/*.md` (top-level + protocols-піддерево) | `bin/rails docs:check_refs` (HARD 2026-07-18; `lib/docs_linter.rb`) |
| **RTC reg-map drift** | register availability (`DRn free/reserve`) живе лише в owner `03_01 §2`; інші доки не дублюють (зловив stale «DR15 резерв» у 03_02/00_07/03_03) | `bin/rails docs:check_refs` (HARD 2026-05-30; `lib/docs_linter.rb`) |
| **RTC phantom register** | будь-який `DRn` з n>19 — неіснуюче залізо (STM32WLE5JC має ЛИШЕ DR0..DR19 — `03_01 §2`/`05_02`); ловив phantom DR20-DR31 (Edge-RL у 00_08/03_03) + DR20-DR21 (ARCH.35 ring у 02_05/00_07) — клас, який allocation-drift проґавив (пропускає таблиці + потребує availability-слова; phantom сидів у table-row + blockquote). Skip fences + deprecation-маркери (DEPRECATED/не існу/фізично неможлив/слід читати/DR0..DR19); застосовується до ВСІХ доків (owner теж — DR20 не існує ні для кого) | `bin/rails docs:check_refs` (HARD 2026-06-07; `lib/docs_linter.rb`) |
| **Lorenz-formula drift** | β-assignment (`beta = 8.0/3.0`) живе лише в owner `03_04 §4.1`; інші доки реферять, не re-declare (зловив stale σ/ρ/β у 05_01 + старому tech-00_01, розчиненому в P1b, при 05/07-реструктуризації) | `bin/rails docs:check_refs` (HARD 2026-05-30; `lib/docs_linter.rb`) |
| **telemetry_logs.chain_hash drift** (ARCH.12) | `telemetry_logs` НЕ має колонки `chain_hash` (вона живе в `audit_logs`/`ethereum_anchors`) → Merkle-anchor leaf = Z-based One-Home (`05_02 §E.60`), не дерево над `TelemetryLog.chain_hash` (правдоподібний-але-хибний рецепт, що дрейфнув у 05_04 §Merkle). Ловить ПОЗИТИВНЕ ствердження; negation/пояснення (drift-fix мусить назвати хибний токен) exempt. Без owner — колонки нема ніде, тож її ствердження завжди drift | `bin/rails docs:check_refs` (HARD 2026-06-28; `lib/docs_linter.rb`) |
| **growth_points clamp drift** (`DocsLinter.growth_points_clamp_drift`) | retired pre-FW.29-PACK wire-clamp `clamp(…,10,63)` (6-біт) живе лише як history в owner `03_04 §4.3`; чинний FW.29-PACK wire = `(5 + m * 26).round.clamp(5, 31)` (метаболічна `m`, E.63; backend ×2 → stored 10..62) — ⚠️ `reward / 2` теж **retired** і банований цим самим guard'ом (owner-дім формули — [`03_04 §4.3`](03_04_mruby_Lorenz_Attractor)). Жоден інший guard його не бачив (Lorenz знає лише β; rate-guard exempt-ить manifest), тож stale-копія тихо misstate-ила token-emission у CLAUDE.md (R8) + manifest:78 (R9). **НЕ** table-skip — дрейф жив саме у table-cell. Exempt: owner 03_04 + standard 00_06 + tracker 00_07 (03_01 ВИЛУЧЕНО з exempt-сету при E.63 — `GP_CLAMP_OWNER_DOC`; стара згадка «firmware-mirror 03_01» тут була doc↔code-дрейфом, зловлено DOC-T.37-рев'ю) | `bin/rails docs:check_refs` (HARD 2026-06-03; `lib/docs_linter.rb`) |
| **StatusByte layout** (DOC-T.43; `DocsLinter.status_byte_layout_drift`) | retired pre-FW.29 6-біт розкладка поза owner — пак `status<<6` (було bits 7..6) · 6-біт growth-маска `0x3F` · «bits 7..6»/`[7:6]` · «6-біт growth». Post-FW.29 wire = `[PanicFlag:1｜Status:2 (bits 6..5)｜GrowthPoints:5 (bits 4..0)]`, пак `(status<<5)｜gp`, маска `0x1F`; owner логіки+паку `03_04 §4.3/§4.4`, wire-позиція `03_05 §2.1`; 7 сайтів (03_01/03_02/05_02/04_01/04_02) реферять, НЕ рестейтять (FW.2 wire-rev2.1 рухає пакет → lockstep). **Anti-retired** (не positive-consistency): проза «bits 6..5» без code-літералів → «мусить бути присутнє» FP-ило б усі 7 → банимо мертву signature (0 хітів у docs). Context-anchored на StatusByte-keyword (`0x3FFF`/`0x7F`/96-біт UID ігнор); historical «6→5»-нота exempt; **НЕ** table-skip. Ортогонально `growth_points clamp drift` (той — wire-діапазон, цей — біт-геометрія — регекси не перетинаються). Exempt 00_06/00_07 | `bin/rails docs:check_refs` (HARD 2026-07-18; `lib/docs_linter.rb`) |
| **cross-ref label single-form** | КОЖНА мітка doc-id-лінка має вести з code-span `` `NN_NN` `` — один дозволений діалект (`` [`NN_NN`](Doc) ``, `` [`NN_NN §X`](Doc) ``, `` [`NN_NN` — Title](Doc) ``). Plain `[NN_NN …](Doc)`, escaped `[NN\_NN\_…](Doc)`, full-name-in-codespan `` [`NN_NN_Full`](Doc) `` = той самий реф у 2-му написанні → flagged. Prose-фраза без doc-id лишається. Звело 445 лінків у 50 доках (`scripts/normalize_crossrefs.rb`); таксономія — `scripts/crossref_audit.rb` | `bin/rails docs:check_refs` (HARD 2026-06-01; `lib/docs_linter.rb`) |
| **link label↔href mismatch** | doc-link, де visible label веде з одним `NN_NN`, а href резолвиться на ІНШИЙ доку — renamed-doc residue, який dangling-check НЕ ловить (href валідний). Зловив 00_02 §3 (текст «00_06» → файл 00_05) + residue в 03_02 після renumber'у візії-доку (visible label-ID ≠ href-ID) | `bin/rails docs:check_refs` (HARD 2026-05-30; `lib/docs_linter.rb`) |
| **magic-marker hex** (DOC-T.46) | визначення 4-байтного ASCII-маркера (`"LZST" = 0xNNNN`) має дорівнювати BE/LE byte-packing власного імені — self-validating, без таблиці (firmware змішує endianness: `RITE`=LE, `LZST`=BE); ловить typo'd/stale magic-value (клас 9cb1d86). Hex-посилання за значенням (без сусіднього імені) не чіпається. **Флип advisory→HARD (DOC-T.46, 2026-07-18):** детермінований byte-packing, FP-free за конструкцією (потребує сусіднього quoted-4-letter імені + ASCII-range hex), scoped на `/magic/i`, 0 residual — advisory-мітка була stale-обережністю, яку дизайн уже усунув | `bin/rails docs:check_refs` (HARD 2026-07-18; `lib/docs_linter.rb`) |
| **bare §-ref → link** | code-span `NN_NN §X` поза markdown-лінком — non-standard + blind spot (`section_label_drift` валідує лише лінковані рефи). Стандартизувати у повний лінк (канонічна форма — §1). Exempt 00_00/00_06/00_07; skip fences + meta-плейсхолдери (`§NN`/`§X.Y`) | `bin/rails docs:check_refs` (HARD 2026-05-31; `lib/docs_linter.rb`) |
| **bare doc-id → link** | сиблінг bare-§ для **whole-doc** рефів: code-span `NN_NN` / `docs/NN_NN` / `NN_NN_FullName` **без** § поза лінком має бути повним лінком (канонічна форма — §1), не lone code-span (клікабельність + один формат). Ловить лише id, що резолвиться в поточний док (retired-`04_07` лишається прозою); skip fences + спани в лінках. Закрив 213-реф thread-A sweep (`scripts/linkify_bare_refs.rb`). Exempt 00_00/00_06/00_07/manifest | `bin/rails docs:check_refs` (HARD 2026-05-31; `lib/docs_linter.rb`) |
| **§-after-link → fold** (DOC-T.16) | code-span doc-id лінк, після якого ВИСИТЬ голий `§X` (`[NN_NN](Doc) §X`) — той самий реф, розщеплений надвоє: §X поза лінком (некликабельний) + невидимий обом сусідам (`bare_section_ref` хоче doc-id+§ в ОДНОМУ bare code-span; `crossref_label_form` дивиться лише мітку↔href, не текст ПІСЛЯ `)`). Згорнути → `[NN_NN §X](Doc)`. Href = абсолютний АБО relative `../` (бо `docs/protocols/` реферить канон relative-path); filename-мітки (`SUMMARY.md`) поза скоупом. Tree-wide: top-level `files` + окремий `docs/**` scan-pass (ширшу protocols ref-integrity тримає DOC-T.26). Skip fences + meta-плейсхолдери; exempt 00_00/00_06/00_07 | `bin/rails docs:check_refs` (HARD; `lib/docs_linter.rb#section_ref_after_doclink`) |
| **tokenomics/carbon rate One-Home** | mint-курс + carbon-курс — governance-змінні **параметри** → значення живе лише в home (`05_03` + business-view `07_01 §3`); re-statement деінде = silent drift при re-price (зловив дубль у 8 доках). Exempt homes + labeled-mirror `07_02` + manifest + 00_07 | `bin/rails docs:check_refs` (HARD 2026-05-31; `lib/docs_linter.rb`) |
| **rate-guard self-anchor** (DOC-T.40) | знешкоджує латентну міну tripwire-класу: гейт вище хардкодить governance-CHANGEABLE значення в регексі — ре-прайс дому тихо позбавляв би НОВЕ значення One-Home-нагляду. Anchor вимагає, щоб кожен дім (mint: `05_03`+`07_01`; carbon: `07_01` — 05_03 його не несе) досі МАТЧИВ власний регекс гейта → ре-прайс валить CI, доки регекс не оновлено свідомо разом із дзеркалами; текст порушення = чекліст дзеркал (called out: `manifest.md` — жанр-exempt з drift-гейта, тож це його ЄДИНЕ червоне світло при ре-прайсі, DOC-T.41) | `bin/rails docs:check_refs` (HARD 2026-07-16; `lib/docs_linter.rb#tokenomics_rate_anchor`) |
| **#anchor resolution** | кожен `#anchor`-фрагмент у doc-лінку (intra-doc — фрагмент до власного заголовка; cross-doc — `NN_NN_Name#fragment`) резолвиться в реальний heading-слаг цілі — стале посилання тихо кидає читача на верх сторінки, а §-label-гейт його не бачить. Випущено з on-demand `docs:graph` у HARD-gate (2026-06-01), коли всі anchors стали чистими; engine той самий (`DocsGraph.dangling_anchors`, тепер fence-aware) | `bin/rails docs:check_refs` (HARD 2026-06-01; `lib/docs_graph.rb`) |
| **external doc-path** | репо-файли ПОЗА `docs/` (`.github/` workflows+configs+copilot/labels, root `README`/`CLAUDE`/`AGENTS`, **+ source-дерева** `bin`/`lib`/`app`/`firmware`/`contracts`/`spec`/`scripts`/`tools`/`config`/`db` — code-коментарі) реферять канон-доки шляхом теж; renamed/renumbered док лишає їх stale, а in-docs гейт цього **не бачить** (blind spot, що ховав `docs/00_07_GitHub_Projects…`→00_05 + `docs/08_07_SEU…`→08_03 + source `00_08`→00_07 + `03_05`-rename residue у 8 файлах app/bin/spec). Флагає будь-який `docs/NN_NN_Name`, чий точний basename ≠ поточний док (лінтер `lib/docs_linter.rb` + його spec exempt — цитують stale-приклади як приклади) | `bin/rails docs:check_refs` (HARD 2026-06-02, source-scope 2026-06-03; `lib/docs_linter.rb`) |
| **source line-ref drift** (DOC-T.15 firmware + DOC-T.17 Ruby + DOC-T.29 class/prose + DOC-T.36 doc-id) | volatile line-refs у **чотирьох діалектах** у `docs/**` + `.github/**`: (1) path-form `*.c/*.h/*.rb/*.rake/*.sol:N`, (2) Ruby-symbol `ClassName:NNN` (namespaced або `Service`-suffixed — wire-field `Payload:16` НЕ FP), (3) укр-проза `(р.162)`/`(рядок 31)`, (4) doc-id `NN_NN:line` — line-ref У КАНОН-док (живий `03_02:9` зловив stan_audit 07-12; §-якорі = стабільна форма; 00_07 НЕ exempt — реф жив саме там). Файл росте → рядок указує на ХИБНИЙ зміст (FW.4-трійка цілила в `DEFAULT_TTL`; verified 2026-06-16: `BlockchainMintingService:107`→120, `(рядок 31)`→41). Цитуй стабільний symbol/`#define`/клас/метод, не номер. Сканує docs+.github (НЕ source-дерева — там line-ref у коментарях легітимні; root `CLAUDE.md` поза скоупом); НЕ skip-fence. Прибрав 30 firmware (DOC-T.15) + 10 Ruby (DOC-T.17) + `ClassName:NNN`/prose §06-кластер (DOC-T.29) | `bin/rails docs:check_refs` (HARD; `lib/docs_linter.rb`) |
| **deprecated terms (Ruthless Pruning)** | ретирований SSOT-токен не сміє повертатись в **активний** канон — enforcement-рука Ruthless Pruning (§4). Лише **однозначно** мертві рядки: HKDF `silkennet-v1-aes256`; партномер `ZP-3`/`ZP-5` (∅27 мм через-отв. п'єзо → SMD, `02_01 §3`); ретирований проєктний codename `Gaia 2.0` (→ SilkenNet / GaiaNexus, BIZ.16 — [`07_03 §5`](07_03_Academic_Integration_and_IP)). Ще-живий токен НЕ додається (LTC3108 виживає як DNP fallback → не guard-иться). Exempt: 00_06 (цитує приклади) · 00_07 (tracker) | `bin/rails docs:check_refs` (HARD 2026-06-02; `lib/docs_linter.rb`) |
| **superseded term in front-matter** | реверснуте рішення (`ATECC608B`→SE050, SEC.6) живе у BODY як legacy/reusable-патерн (тож `deprecated terms` не банить повністю), але НЕ сміє стояти у **🎯 Мета / ✅ Статус** front-matter, де лише ПОТОЧНЕ рішення — саме так SE050-міграція лишила 03_05 🎯 з `ATECC608B` як поточним SE (silent drift: `doc_structure_map.rb` 🎯-колонкою зловив, per-line гейт — ні). Скан лише front-matter-зрізу (🎯 … до 🔗). Exempt 00_06/00_07 | `bin/rails docs:check_refs` (HARD 2026-06-08; `lib/docs_linter.rb`) |
| **anchor dimension drift** | frozen anchor key-dims (`01_01 §1` home, radial+axial freeze 2026-06-20) — superseded діапазон біля part-keyword дрейфує при freeze: один фіз-анкер перетинає всі домени (geometry/metallurgy/capsule/pogo/economics/RF), тож freeze мусить стиснути старий діапазон у нове значення в **усіх** доках разом (мотив — 7-doc ручний grep-sweep). Ловить `20–30` (фланець/radome → Ø25) + `40–60` (Zone 2 → 50 мм) біля part-keyword; skip-fence; historical-exempt; ALL docs incl. owner. Оновити таблицю при наступному freeze (як `DEPRECATED_TERMS`) | `bin/rails docs:check_refs` (HARD 2026-06-20; `lib/docs_linter.rb`) |
| **thermal-stress One-Home** (HW.3.IS) | frozen press-fit/Lamé числа (`01_01 §4.2` + `protocols/anchor/fea_aging/THERMAL_STRESS_REPORT.md` home) — superseded baseline SF/P_c біля thermal-keyword дрейфує: числа сиділи на застарілому Ø10/3мм + несли `contact_pressure`-баг (`b=R_INNER` замість `R_INTERFACE`), frozen+fixed 2026-06-21, unified-Lamé re-run 2026-06-22 (SF 9.9×→3.4×→5.6× combined; P_c 34.7→22.6 баговий → 0.49-3.32→0.32-2.16). Ловить `9.9×`/`3.4×` (обидва superseded SF-покоління) біля SF/Lamé/PEEK/press-fit + `34.7`/`22.6` біля P_c/MPa/press-fit; skip-fence; historical-exempt (`correction`/`baseline`/`buggy`/`колишній`/`старий`/`артефакт` + англ-дзеркала `former`/`artifact`/`overstated` — цитата спростування легітимно називає старе число); сам `THERMAL_STRESS_REPORT` = owner-exempt по basename (DOC-T.42 ② довів скоуп до дому — його Correction-хронологія і заморожений pre-Correction §1-blockquote несуть superseded числа за призначенням). Оновити при наступному re-run (як `DEPRECATED_TERMS`) | `bin/rails docs:check_refs` (HARD 2026-06-21; `lib/docs_linter.rb`) |
| **CEM↔canon geometry** (HW.33) | shipped `tools/cad/cem/*.json` = дзеркало канон-геометрії (`01_01 §1.4` bus rod/channel/liner · `§4.3` barb h/α/β · `01_04 §5.5` bell rise/radius ≥). Context-anchored (regex на мітку, не голе число). Ловить те, що C#-xUnit (валідує record-**defaults**, ще й замикає off-spec `0.8×0.6`) + `anchor_dimension_drift` (лише прозові діапазони) пропускають — `bell_radius=5.0` не мав жодного асерту. Ruby-сіблінг `test_doc_cache_sync.py` | `ruby scripts/cem_canon_sync.rb` (HARD `docs.yml`; stdlib) |
| **cache↔doc numeric sync** (science, §01b flagship) | in-silico DFT/MD/mech числа: cache-SSOT (`protocols/ebfc/in_silico/**/SUMMARY.md` + L3-виходи + `thermal_penetration.json`) == canon-prose через context-anchored mutation-verified check — ловить numeric drift + provenance-mix (те саме число законне проти РІЗНИХ script-owner). Geometry-сіблінг `cem_canon_sync`. ⚠️ Wired у **`in_silico_smoke.yml`** (НЕ docs.yml) → `guard_registry_sync` CHECK A (docs.yml-only) його не бачить → §3-рядок ведеться вручну (стеля CHECK A — DOC-T.44) | `python3 -m pytest tools/in_silico/tests/test_doc_cache_sync.py` (HARD `in_silico_smoke.yml`) |
| **TRL range-consistency** | per-doc member-TRL у межах **band** модуля (`00_03 §1`): (a) рядок well-formed (current ≤ target) + (b) рядок ≤ max member-TRL під-доків (рядок = min, не вище за КОЖНОГО члена) + (c) member ≤ target модуля. Перевіряє лише **верхні** межі — нижня має легітимні винятки (рядок = min критичного шляху → під-док буває нижче: 06_01 off-path, 00_03 Статус звітує System-TRL). Owner-only-vocabulary, як інші value-гейти | `bin/rails docs:check_refs` (HARD 2026-06-03; `lib/docs_linter.rb`) |
| **Стан-лід audit** (DOC-T.35+36) | _(advisory, on-demand — ганяти на цемент/vilize-сесії, НЕ CI-gate)_ три осі по registry-айтемах 00_07 — **(3) [x]-staleness** (07-16): `- [x]`-чекбокс живе години-дні (цементуй у Стан/канон і зрізай — інакше ✅-історія топить відкрите); бокс із датою `✅ YYYY-MM-DD` старший ~14 днів → хіт, без дати — лічильник-нагадування. Осі 1-2 по `**Стан:**`-рядках: (1) **canon-claim** — код-символ зі Стану (snake_case/`Class#method`/`fn()` у code-span) відсутній у заявлених канон-домах айтема → підказка «∈ інший дім (wrong-дім?)» / «канон ніде не називає (діра або код-only)»; negation-exempt («вичищено X», «X = 0 hits», «зрізано/squash» — drift-fix мусить назвати хибний токен); historical sweep **DOC-T.37** (07-12, ~88 хітів → 6 розібраних-FP) вбив решту FP-класів у скрипт: ALL_CAPS = spec-at-source · file-exists · object-chain-цитати · memory-дім · `Class#`→клас-у-домі · діалект-нормалізація case/hyphen/space/Camel (`allow_iap_ssh`≡`allow-iap-ssh`) · 00_06-universal-дім · status-mute ⚪/⚫/🌿/🔗 (дім = призначення роботи, клейм активується при 🟢/🟡; повний перелік = шапка скрипта). Ловить клейм «Механіка — `NN_NN §X`», де §X резолвиться, а змісту нема (клас FW.58/MRV.1; HW.16-діру канонізовано цим аудитом 07-12). (2) **volatile-numbers** — число+лічильне слово: класи A (ID-число, виключено механічно) / B (event-history — ок) / C (поточний-код лічильник — ЄДИНИЙ порушник) / D (скоуп-вимога — ок) розбирати очима; HARD-lint **відхилено норою 07-12** (B/C/D лексично нерозрізнимі → FP-шторм). Semantic за природою → advisory за дизайном, як ref-graph (magic-marker уже фліпнуто HARD — DOC-T.46) | `ruby scripts/stan_audit.rb` |
| **spec-claim audit** (TEST.10) | _(advisory, on-demand — НЕ CI-gate; exit 0 ЗАВЖДИ, вердикт у виводі)_ розходження між НАЗВОЮ прикладу й тим, що він тверджує: усі твердження прикладу лише status-only / type-only / `not_to raise_error`-only / `have_key`-only / presence-only, а назва містить дієслово поведінки. Це «приклад, що не може ВПАСТИ» ([`04_06 §B.2`](04_06_Testing_Guide_and_Coverage) #15) — вимкнений детектор, який CI звітує зеленим. **HARD-гейт тут неможливий за побудовою** (матчери самі по собі доречні, дефект семантичний), а корпусний advisory відхилено виміром 2026-08-03: 282 кандидати при точності близько половини = звіт, якого не читають, тобто вимкнений гейт у костюмі звіту. Тому **дефолт — діф** (0–5 рядків на тракт), `--all` лишається для виміру КЛАСУ. 🔴 Друга його робота — чесний лічильник: обсяг класу тричі поспіль виміряли хибно, бо греп зараховував коментарі, що цитують стару форму; скрипт рахує код і прозу окремо. Стеля названа в шапці (рядковий парсер, англомовний дієслівний фільтр, не бачить двійникування) | `ruby scripts/spec_claim_audit.rb` |
| **ref-graph audit** | _(on-demand, НЕ CI-gate)_ orphan/dead-end сторінки · in/out-degree skew · one-way (asymmetric) sibling-лінки · linked-`§X` валідація — власний ref-граф канону (графовий вигляд NN_NN-лінків, якого flat per-line лінтери не дають). `#anchor`-резолюція звідси випущена у HARD-gate (рядок вище); граф лишає графовий вигляд, якого per-line гейти не дають | `bin/rails docs:graph` (`lib/docs_graph.rb`; spec `spec/lib/docs_graph_spec.rb`) |
| **log-mel contract** (FW.25) | §3.4 значення == C-дзеркало `logmel_contract.h` == python `contract.py` через stamped `contract_hash`; committed `firmware/common/logmel_*.h` == canonical regen — ловить тиху правку будь-якої сторони (значення-as-bytes, не проза) | `check_firmware_tables.py` (ci.yml, stdlib) + `emit_c --check` (ml_smoke.yml) |
| **mruby bytecode drift** (FW.46) | committed `firmware/common/lorenz_bytecode.h` мусить відповідати `bio_contract.rb`: light — sha256-stamp джерела в заголовку (stdlib, без mrbc); deep — mrbc-regen + diff (pinned mruby submodule) | `tools/firmware/check_bytecode.py` (ci.yml `firmware_test`) + `gen_bytecode.sh --check` (ci.yml `firmware_arm_build`) |
| **solc/pragma version One-Home** | locked compiler-версія (`pragma solidity 0.8.X` / foundry `solc_version`) живе лише в owner `05_03` (Pragma-таблиця; код-SSOT = `contracts/foundry.toml` + `*.sol`); re-statement деінде дрейфує миттю при бампі solc — зловив 9 stale `0.8.28`-копій across 00_05/05_04, що пережили 0.8.35-бамп до sweep'у. **НЕ** table-skip (05_04-drift жив у table-cell). Exempt: 05_03 owner + 00_06 (цитує приклад) + 00_07 (tracker). Owner-only vocabulary, як `tokenomics_rate_drift` | `bin/rails docs:check_refs` (HARD 2026-06-13; `lib/docs_linter.rb`) |
| **AI-vendor name One-Home** | AI-ростер волатильний → канон описує **ролі** (`frontier-LLM`/`coding-agent`), конкретні інстанси — лише у `00_02 §2` ростер-знімку; вендор-токен деінде дрейфує при зміні ростера. Токени: Gemini/Cursor/Copilot/Windsurf/ChatGPT/Grok/DeepSeek/«Claude Code». Case-sensitive (lowercase `cursor`/`grok` = UI/verb, не вендор). **Виключено** overloaded/generic: `Codex` (04_05 Lore / «The Codex» SSOT-guard / ADR-CDX), bare `Claude`/`Opus`/`Sonnet`/`Fable`. Skip-fence. Exempt: 00_02 owner + 00_06 + 00_07. Owner-only vocabulary, як `solc_pragma_version_drift` | `bin/rails docs:check_refs` (HARD 2026-06-13; `lib/docs_linter.rb`) |
| **model/service↔code registry sync** | `04_01` (data-model registry) ⟷ `app/models/`: model-файли ⟷ `### Model` код-спан-заголовки §2..§7b (1:1) · concern-файли ⟷ §1 · `PartitionMaintenanceWorker::PARTITIONED_TABLES` ⟷ §0/§11 · **прозовий лічильник** («всіх N моделей … M concerns») проти числа, яке гейт і так обчислює — `>`-callout-провенанс exempt (запис про ВЖЕ полагоджений дрейф ≠ поточний клейм). **+ `04_02` (service registry) ⟷ `app/services/**` + `app/workers/**`**: кожен клас згаданий (weaker «mentioned», бо сервіси розкидані прозою/§11-чергами — але ловить повністю відсутній, як `FactoryFlashing::*`-пропуск). Замінив **ручні «§12/§13b SSOT Drift Register»**, що мовчки протухли (заявляли 35 моделей при 36 файлах; пропустили цілий FactoryFlashing-namespace). НЕ `docs_linter` (code↔doc, не pure-doc) — окремий pure-Ruby скрипт. Тригер CI — і `docs/`, і `app/models/**`/`app/services|workers` (через push), тож дрейф ловиться з обох боків. | `ruby scripts/model_doc_sync.rb` (CI `docs.yml`) |
| **design-token compliance** (UI.1) | сира Tailwind-утиліта кольору (`bg-white`/`bg-black`/`*-{gray,zinc,neutral,slate,stone}-N`/`text-emerald-*`) у `app/views/shared/**` — ⚠️ **нейтральних родин пʼять із 2026-08-08**, доти regex знав лише `gray`, тож `bg-zinc-950` проходив навіть на суворій поверхні; розширення безпечне, бо нуль хітів у периметрі виміряно ПЕРЕД внесенням. 🔴 Стеля, названа явно: гейт сліпий до **градієнтних** утиліт (`from-`/`via-`/`to-black`) і до arbitrary-значень (`bg-[#000]`) — саме та форма, якою сира поверхня мігрувала з `telemetry/live_stream` — тобто рівно там, де [`04_04 §3.5`](04_04_Phlex_UI_and_Tailwind) її ЗАБОРОНЯЄ. 🔴 **Периметр був інвертований до 2026-08-07:** дефолт таска стояв на `app/views/components/`, де те саме §3.5 сиру Tailwind явно ДОЗВОЛЯЄ, тож сторож патрулював поверхню з м'яким правилом і на сувору не дивився взагалі. Після розвороту сувора поверхня зміряна чистою — це й зробило гейт підключабельним (ratchet: migrate-to-green ПЕРЕД гейтом). `app/views/layouts/` свідомо поза дефолтом — його єдиний хіт `backdrop:bg-black/60` є scrim'ом, який мусить лишатись чорним в обох темах. Міграційний бек-лог доменних компонентів — [`00_07`](00_07_Action_Plan_Tracker) UI.1, прогін `COMPONENTS=app/views/components/`. | `bin/rails gaia:lint_tokens` (CI `docs.yml`) |
| **component↔code registry sync** (UI.12) | `04_04` (component registry) ⟷ `app/views/`: доменні файли ⟷ рядки §6.4 (1:1) · `shared/{ui,iot,web3}` ⟷ §6.1/§6.2/§6.3 (1:1) · директорії-неймспейси ⟷ ASCII-дерево §1. Сиблінг `model_doc_sync` для останнього реєстру, що взагалі не мав сторожа. 🔴 **Несуче в цьому гейті — ПАРСЕР, а не порівняння:** реєстр пише компонент у **трьох** формах рядка (детальна `` `Ns::Class` `` · стиснена `` `Ns` `` + перелік листків · **bold** у shared), і сторож, що знає одну, звітує дрейф ≈усемеро більший за справжній — саме так було хибно зміряно його власний item. Четверта форма мусить бути навчена явно, інакше гейт червонітиме на чесному додаванні. Стеля названа: звіряються ЛИШЕ імена класів (не props, не проза), а дерево §1 — на рівні **неймспейсів**, бо листки свідомо мають один дім (§6); доти дерево дублювало реєстр і протухало швидше за нього. | `ruby scripts/component_doc_sync.rb` (CI `docs.yml`) |
| **protocols/ canon-ref resolution** (DOC-T.26) | `docs/protocols/**` (lab RFQ / in-silico / anchor IP / outreach) реферить top-level канон **relative-path** (`[NN_NN §X](../../NN_NN_Name)`) — форма, якої top-level гейти НЕ бачать (сканують `docs/*.md`, чекають абсолютний `(NN_NN_Name)` href) → rename/renumber/§-collapse канону тихо гноїть ці 84 рефи (клас §1.3→§5, але в protocols/). Валідує **RESOLUTION** (не форму, per Option A — форма вже чиста): (1) relative-target існує · (2) `NN_NN §X` резолвиться (reuse boundary/parent-aware `Tracker::Dashboard.file_section_dangling_refs`) · (3) `#anchor` резолвиться (`DocsGraph.anchor_set`) · (4) bare-mention NN_NN-prefix існує. Intra-subtree filename-лінки (`[SUMMARY.md](SUMMARY.md)`) = власна конвенція піддерева, поза скоупом. НЕ `docs_linter` (cross-file resolution) — окремий pure-Ruby скрипт. | `ruby scripts/protocols_ref_check.rb` (CI `docs.yml`) |
| **canonical source-block pin** | value-несучі const-блоки, що живуть і в коді (SSOT), і в **бажаних** doc-дзеркалах, запінені SHA-256 у `lib/canonical_block_pins.yml`. Інвентар: `lorenz_core` (хаос-константи) + `metabolic_gp_core` (GP/Δt = магнітуда емісії post-E.63 + 5-бітна wire-стеля) ← `firmware/bio_contracts/bio_contract.rb` · `wire_payload_format` ← `app/services/telemetry_unpacker_service.rb` · `slashing_curve` (GAMMA/PF_MAX/uplift-ваги; `slash()` необоротний) ← `app/services/blockchain_burning_service.rb` · `qatt_envelope` (Ed25519-конкатенація + era-residue) ← `app/workers/unpack_telemetry_worker.rb` · `solc_pragma` ← `contracts/foundry.toml` (**TOML** парситься тим самим `NAME = value`-регексом — рушій не чіпано; `SOLC_VERSION_RE` version-agnostic і owner-до-коду НЕ пінив, тож бамп лишав `05_03` stale і зеленим); дзеркала = CLAUDE.md §5 + `attractor.rb` + `queen_attest.h` + 03_04/03_05/05_03/05_05. Зміна пінованого блоку валить CI, доки дзеркала не reconciled + `bin/rails docs:repin`. Генералізація owner-only vocabulary на блоки, де дзеркало ПОТРІБНЕ, не заборонене (виноска нижче). ⚠️ **Пін односторонній за конструкцією** — хешується лише `source:`; `mirrors:` = проза, тож правка дзеркала наодинці CI не валить (C-half `queen_attest.h` і server-half `attractor.rb` reconcile-яться РУКАМИ на хіт) | `bin/rails docs:check_refs` (HARD 2026-06-16; `lib/docs_linter.rb#canonical_block_drift` + `lib/canonical_block_pins.yml`) |
| **SCC-rate model** (E.63) | self-consistency SCC-generation rate: packets×GP виводяться з ОДНОГО delta_t + anti-over-mint стеля — ловить фізично несумісний числа-дрейф (adversarial 2026-07-14: 24 пакети × 50 GP разом не існують) | `ruby tools/firmware/scc_rate.rb --assert` (HARD `docs.yml`) |
| **Queen energy-budget deploy-gate** (HW.39) | параметрична модель = SSOT енерго-чисел `02_05 §4`: Phase 1/2.5 winter-balance ≥ margin (assert), Phase 3 warn-only до Starlink bring-up | `ruby tools/firmware/queen_energy_budget.rb --assert` (HARD `docs.yml`) |
| **engine unit-tests** | движки гейтів (`lib/docs_linter.rb` / `lib/docs_toc.rb`) — чисті функції зі спеками; регресія движка = мовчки зламаний гейт, тож спеки ганяються В САМОМУ docs_check | `COVERAGE=0 bin/rspec spec/lib/docs_linter_spec.rb spec/lib/docs_toc_spec.rb` (docs.yml) |
| **guard-registry ⟷ код sync** (DOC-T.40) | сам §3 — ручне дзеркало CI-гейтів і гнило однонаправлено (нові гейти рядка не діставали: canonical pins / `--assert`-гейти мали тут нуль згадок). П'ять осей: (A) кожен `run:`-крок джоба `docs_check` названий тут VERBATIM · (B) кожен `failed <<`-лейбл `lib/tasks/docs.rake` + guard-метод `lib/tasks/tracker.rake` мапиться в рядок §3 (curated map у скрипті — новий гейт без рядка = RED; tripwire, як `DEPRECATED_TERMS`) · (C) кожен названий тут файл існує на диску · (D) parity анти-«декоративний guard»: `ssot_guard.yml` `paths:` ⟷ вбудовані `mappings` (тримав лише ⚠️-коментар) + pinned-sources ⊆ `changes`-фільтр `docs.yml` (клас: `bio_contract.rb` був поза фільтром — HARD-пін не ранився саме на тому PR, що його ламає) + `ruby_version_sync`-дзеркала і їх SSOT ⊆ того ж фільтра (OPS.13 — Dockerfile/Gemfile-бамп мусить тригерити parity-гейт, не обійти) + кожен pin-KEY названий у §3-рядку піна (третій пін не лишить інвентар stale) · (E) РЕВЕРС осі A: кожна §3-пара «команда (workflow-дім)» знаходить команду в run-кроках САМЕ ТОГО workflow, і кожна command-span живе хоч у якомусь workflow, якщо рядок не advisory/on-demand — клас «заявлений gating без CI-виклику», який DOC-T.41 зловив руками в `SECURITY_ASSURANCE_CASE` («cppcheck (MISRA) gating» без виклику) | `ruby scripts/guard_registry_sync.rb` (HARD `docs.yml` 2026-07-16) |
| **gate-perimeter meta-guard** (OPS.14) | сам required-check периметр — гнив односторонньо: новий детермінований PR-гейт міг народитися ПОЗА required-множиною, і ніхто б не помітив (саме так money-path Solidity-аудит лишався merge-advisory, поки канон казав «all gating»). Курований `PERIMETER`-SSOT класифікує КОЖЕН `pull_request`-тригернутий workflow у `.github/workflows/` на три відра: `:required` (merge-blocking через `if: always()`-агрегат, чиє `name:` ∈ branch-protection — усі **8**: `ci.yml`→«CI passed», `docs.yml`→«Docs passed», `solidity_audit.yml`→«Solidity passed», `dco.yml`→«DCO passed», `cad_smoke.yml`/`ml_smoke.yml`/`in_silico_smoke.yml`/`iac_scan.yml`→«CAD/ML/In-silico/IaC passed»), `:advisory_by_design` (свідомо НЕ блокує, з ратифікованою причиною — `ssot_guard.yml`), `:flip_pending` (МАЄ стати required, трекається в 00_07 — **наразі порожній**: міграція OPS.15/16 завершена 2026-07-19, останній мешканець `dco.yml` вийшов 2026-07-25 [UNI.20 👤-флип]). Чотири HARD-осі: (a) кожен PR-гейт класифікований — новий некласифікований → RED «класифікуй»; (b) жодного мертвого запису (workflow зник / більше не PR-тригер); (c) кожен `:required` реально має `if: always()`-агрегат (парс: job із `name:`==label + always()); **(f)** [OPS.23] кожен `:required`-агрегат, що ЗАЛЕЖИТЬ від path-фільтра `changes`, ще й стверджує його ВЛАСНИЙ результат — наявності агрегату замало: `changes` не несе `if:`, тож `success` — єдиний легальний для нього результат, тоді як гейтовані ним джоби `skipped` бувають законно, і сторож форми «чи щось упало/скасовано?» читає **skipped-фільтр** як звичайний path-скіп і зеленіє, не побігши. Дискримінатор саме `skipped`, а НЕ `cancelled` (той старий сторож ловив). ⚠️ Стеля (f): він вимагає падаючого кроку, зачепленого за результат фільтра, але не пінить дослівне написання умови — форм багато, і пін одного правопису відкидав би коректне переписування. Плюс advisory-список `:flip_pending` (migration-tracker, спадає до 0, як сіблінг-агрегати приземляються + branch-protection оновлюється). Сіблінг класу DOC-T.44 «гейт поза реєстром гниє в один бік». Опційний `--live` (локально, не CI — токен CI не читає protection) звіряє `:required`-мітки з реальними `required_status_checks`. Вхід `.github/**` уже в `changes`-фільтрі docs.yml → гейт не декоративний. **[DOC-T.51] + дві prose-claim перевірки** (потребують нуль токенів, тому бігають у CI, на відміну від `--live`): **(d)** числівник біля gate-колокації (`required status check`/`required-чек`/`PR-гейт`/`CI-gate`) у `docs/`·`.github/`·`.claude/`·`scripts/`·root-`*.md` мусить = лічильнику `:required`; **(e)** проза, що називає ОДИН конкретний воркфлоу flip-pending, мусить збігатися з його класом у `PERIMETER` — ловить самосуперечність шапки скрипта. ⚠️ Задекларована стеля обох: рядок з ISO-датою читається як історичний запис і пропускається | `ruby scripts/workflow_gate_perimeter.rb` (HARD `docs.yml` 2026-07-18) |
| **FIELDS ⟷ канон sync** (OPS.6) | `GithubBootstrap::FIELDS` (код-SSOT Projects V2-схеми, `lib/github_bootstrap.rb`) ⟷ таблиця `00_05 §1.1`, row-scoped: імена полів 1:1 + порядок · тип (Text/Single Select) · TRL-рядки пінюються числово («1-9, ніколи 10-12») · закриті переліки (Horizon/Agent/Cluster/Stage/Appetite) — точна впорядкована рівність спанів · Module ⟷ закрита дев'ятка `module:NN-slug` у `.github/labels.yml` (нормалізовано) · Cycle/Semester — canon-перелік мусить бути ПРЕФІКСОМ коду (клас «`Fall 2025-2026` перша в §1.1, відсутня в коді») + формат `Cycle YYYY.QN`. Привід: `Cross-cluster` пережив власне скасування 37 днів, а спека дрейф ЛОКАЛА (golden-тест на хибному значенні); `lib/github_bootstrap.rb` — у `changes`-фільтрі docs.yml, інакше гейт декоративний. Mutation-verified ×4 (вкл. відтворений phantom `06-infra`) | `ruby scripts/field_canon_sync.rb` (HARD `docs.yml` 2026-07-16) |
| **PR-labeler config gate** (OPS.3) | `.github/labeler.yml`, дві осі. **(A) негативний глоб ніколи не самодостатня гілка:** `actions/labeler` дефолтить КОЖЕН список матчерів у `any:`/OR — top-level під міткою · `any:` · матчери під одним `changed-files` · глоби всередині `any-glob-*` — тож негатив (`!app/services/iotex/**`) матчить усе ПОЗА виключеним піддеревом і липне на кожен PR. Клас був живий: `cluster:C-scaling` стояв на всіх PR до 2026-07-16 ([`00_05 §2.6`](00_05_GitHub_Projects_and_IaC_Automation)). Наївне формулювання «є `all:` над ним» пропускає дві реальні діри — негатив поруч із позитивом в ОДНОМУ `changed-files` (OR всередині AND-обгортки) та `all:`, що тримає ЛИШЕ негативний блок — тому правила розкладаються в диз'юнкцію кон'юнкцій (OR-вузли об'єднуються, AND-вузли перемножуються) і кожна гілка з негативом мусить нести позитивний глоб. Невідомий ключ матчера = RED: walker не бачить його вмісту, тож вкладений там негатив пройшов би сліпо. **(B)** ключі labeler ⊆ `.github/labels.yml` — phantom-ключ інертний (labeler лейбла не створює, правило просто не стріляє; клас `module:06-infra`); однонаправлено НАВМИСНО, бо labels.yml ширший (`type:*`/`priority:*`/ручні triage-маркери). **Стеля:** гейт бачить ФОРМУ, не намір — коректно обгорнутий негатив може виключати не те піддерево, а routing-coverage ([`00_05 §4.1`](00_05_GitHub_Projects_and_IaC_Automation)) лишається очима; перевірити правило живим PR гейт теж не може — payload `pull_request_target` заморожений, `gh run rerun` конфіг НЕ ре-читає. Mutation-verified ×7 (чотири класи самодостатнього негативу + phantom-ключ + typo-ключ + CONTROL: правильна `all:`-форма мусить лишатись зеленою) | `ruby scripts/labeler_config_check.rb` (HARD `docs.yml` 2026-07-29) |
| **governance-key sync** (GOV.2) | міст `ProtocolParameters.sol` ⟷ `Governance::ParameterSyncWorker` (`parameter_sync_worker.rb`) тримається ВИКЛЮЧНО на `keccak256(param_key.to_s)` — збігу РЯДКОВОГО імені; жоден гейт його не пінив. Три класи тихої відмови: (1) `KEY_*` on-chain без запису в `PARAMETER_MAP` → DAO голосує за параметр, який бекенд **ніколи не читає** (governance-no-op, CI зелений) · (2) ключ перейменований у Solidity → Ruby keccak цілить у мертвий слот → параметр **мовчки відкочується на хардкод-дефолт** (напр. `slash_gamma` назад на 1.3 попри голос) · (3) Lorenz-ключ **ПЕРЕЇХАВ** з `DCI_LOCKED_KEYS` у `PARAMETER_MAP` → governance дістає важіль ламати device↔server parity (FW.7) — рівно та катастрофа, проти якої tripwire і ставили. Економічна й Lorenz-множини звіряються **ОКРЕМО**: union-рівність до класу (3) СЛІПА (переїзд не міняє об'єднання), а класифікація за префіксом `KEY_LORENZ_*` безпечна — будь-яка невідповідність імені й рядка валиться в set-порівнянні. Канон-пін = «8 Lorenz-ключів» + їх перелік (`05_06 §7`) — структурний DCI-інваріант; к-сть **економічних свідомо НЕ пінимо** (легітимно росте з протоколом → volatile-лічильник флагав би кожне чесне додавання → шум → гейт вимкнуть, §1 no-volatile-counts). Ruby-бік regex-сканується (воркер тягне `eth` + Rails-стек, `require` неможливий) — патерн, що перестав матчити, = **abort-tripwire**, не vacuous-green. `contracts/**` + `app/**` уже в `changes`-фільтрі docs.yml, інакше гейт декоративний. Mutation-verified ×5 (три класи + канон-розбіжність + зламана форма). Сіблінг `field_canon_sync` / `cem_canon_sync` | `ruby scripts/governance_key_sync.rb` (HARD `docs.yml` 2026-07-17) |
| **governance-bounds sync** (GOV.3) | сіблінг GOV.2 на ІНШІЙ осі того самого worker-дзеркала: bounds (`min`/`max`) + `value_type` + `category`, які `PARAMETER_MAP` пише в кожен governance-синкований `SystemParameter`, мусять збігатися з дефолтом `db/seeds.rb`. Канон [`05_06 §7`](05_06_Governance_and_DAO) явно декларує «One-Home меж = `PARAMETER_MAP` ↔ `db/seeds.rb`», але `↔` нічим не енфорсився. Directional (`PARAMETER_MAP` ⊆ seeds з parity): кожен з 9 ключів мусить мати seed-default з ідентичним конфігом; зворотне свідомо НЕ чіпається (17 LOCAL/inert seed-ключів не синкаються за дизайном). Три класи тихої відмови: (1) **bounds-drift** — floor правлений лише в одному дзеркалі (клас E.64, review-caught 2026-07-11: `stress_threshold` 0.5 seed vs 0.65 map → governance-голос сів би на значення, яке друге дзеркало відхилило б) · (2) **type/category drift** — worker конвертує on-chain fixed-point ЗА `value_type` (integer truncate vs float-fraction) → mis-scale · (3) **`PARAMETER_MAP`-ключ без seed** — DAO голосує on-chain, а до першого sync `SystemParameter.current` = nil → код падає на хардкод-дефолт (seed = передумова керованого значення). Обидва боки regex-скануються (worker тягне `eth`+Rails-стек, seeds виконує AR `.create!` — `require` неможливий) → патерн, що перестав матчити, = abort-tripwire, не vacuous-green. `db/**` + `app/**` уже в `changes`-фільтрі docs.yml, інакше гейт декоративний. Mutation-verified ×4 (bounds · type · missing-seed · зламана форма). Сіблінг `governance_key_sync` | `ruby scripts/governance_bounds_sync.rb` (HARD `docs.yml` 2026-07-17) |
| **code→doc §-ref resolution** (DOC-T.48) | кожен `NN_NN §X`, цитований у **коментарях коду** (`app`/`spec`/`lib` `*.rb`) та в **routing-шарі** (`.claude/**/*.md` — скіли/промти реферять канон так само), резолвиться в реальний heading-anchor цілі. Закриває структурну сліпу зону: `docs:check_refs` сканує `docs/**` і **ніколи** не читає код, тож канон-renumber тихо гноїть код-коментарі — proof-case 2026-06-16: slashing-політика переїхала `00_01 §6` → `05_05`, доки підмели, а ~18 рефів `00_01 §6.x` в `app/`+`spec/` простояли stale тижнями, невидимі всім гейтам (external-doc-path лінтер ловить лише `docs/NN_NN_Name`-ШЛЯХИ, не голі `§`-рефи). Реюзить той самий boundary/parent-aware резолвер `file_section_dangling_refs`. **Report-only 2026-06-16 → HARD 2026-07-25** на власній умові з шапки скрипта («promote only if the false-positive rate proves to be zero across the codebase») — 0 порушень на повному 926-файловому скані. FP-клас, якого боялась report-only-ера, структурно звужений: резолвер матчить `§` лише за наявності `NN_NN` І лише **секційну мітку** (digit-led або одна літера + `.` + цифра), тож ілюстративні `§X`/`§NN`/`§SomeName` не матчаться взагалі, а `.x`-хвіст скіпається як wildcard. EXEMPT — рушій резолвера + його специ + `ssot-maintenance` SKILL (їхні «stale» рефи = навмисні фікстури/навчальні кейси). ✅ **Літерна половина стелі ЗНЯТА 2026-08-04 [DOC-T.60]** — звуження «`§`+ЦИФРА», взяте проти ілюстративних `§`, заразом виводило з-під нагляду **літерні** секції, а [`04_06`](04_06_Testing_Guide_and_Coverage) складається з них цілком, тож увесь тест-канон у коді та скілах не перевірявся (мутація: підкладена мертва `§A.999` → `EXIT 0`; стара регулярка не захоплювала її взагалі). 🔴 Розрізнювач — **ФОРМА МІТКИ, а не префікс `NN_NN`**: префікс вимагався завжди й жанрів не розділяє (`05_02 §Модель`, `03_04 §X.Y`, `07_01 §B-02`, `03_05 §FW.2` теж його несуть) — ці 74 прозово-іменовані рефи лишаються на `section_label_drift` ADVISORY свідомо. Вимір ПЕРЕД фліпом по всіх чотирьох корпусах спільного резолвера: 46 рефів увійшли в нагляд, 3 мертві. ⚠️ Стеля лишається ДВОСКЛАДОВОЮ, але друга складова тепер інша: **(а)** скан бере `*.rb`, тож `lib/tasks/*.rake` поза покриттям; **(б)** [`04_06`](04_06_Testing_Guide_and_Coverage) адресує свої 30 best-practice як `§A.16` — той самий простір токенів, що його секції `§A.1`..`§A.10`; крос-док резолвиться лише СЕКЦІЙНЕ читання, тож цитуй секцію + називай BP прозою (`04_06 §A.4`, BP 16–17) | `ruby scripts/code_doc_section_refs.rb` (HARD `docs.yml` 2026-07-25) |
| **phantom tracker-ID у коді** | кожен tracker-ID, цитований у code/workflow-тексті (source-дерева + `.github` + root-`*.md`), мусить резолвитись проти 00_07: **item-ID** (вкл. 📌/🗄️ — архівні лишаються citable) **АБО facet справжнього айтема** (`FW.20-S2`/`ARCH.41-B`/`HW.3.IS` — база ∈ ID-сет **І** сам facet declared verbatim у **тілі живого `####`-айтема**, `item_body_text` — НЕ по всьому файлу: whole-file фолбек робив некролог самореферентною пасткою — §🗄️/DOC-T table-row, що документує ретирування sub-ID, вічно тримав його «резолвним», так 8 рефів `HW.1`-родини пережили 11 проходів; DOC-T.42 ①, mutation-verified у три плеча). Обидві половини несучі: без «база = item» голий ID без дому проходить на випадковій згадці в чужій прозі (так `E.2` жив у тілі SEC.1 — 14-та сирота, close-review 07-16); без «facet declared» проходить `ARCH.35-Q2Q` (справжня база, вигаданий суфікс = proof-case). Родини derived з ID-сету, сепаратор `[.\-]` **як у `Tracker::Dashboard`** (`\.\d` робив гейт сліпим до всіх дефіс-нумерованих: `SLASH-1` — 33 файли! — `PUMA-IPV6-1`; клас `protocols_ref_check`, close-review 07-16); `/`-родини (`ARCH.64/65`) розкриваються; range-split (`E.20-E.34`) — dotted-only, інакше ріже фікстури (`TEST-DEVICE-001`). `CHANGELOG` = **advisory-звіт, не exempt** (release-please-генерований → правити не можна, але це найкращий індекс «які ID колись існували»: whole-file exempt ховав `E.28` — близнюка `OPS.8` з того самого прун-коміту). Стеля: dotless word-ID (`SE050-MIGRATION`) резолвиться лише буквально — форма нерозрізнима з ALL-CAPS-прозою; некролог, написаний УСЕРЕДИНІ `####`-тіла, досі імунізує (форма не бачить семантики — табличний некролог був домінантним і закритим кейсом). Скоуп: source-дерева + `.claude` + `.github` + root-`*.md`; розширення `md`+`json` (DOC-T.42: `HW.3.IS` живе у `zone2_sleeve.json`-prose, скіл `picogk` ніс stale sub-ID). Улов: 13 сирітських ID → §🗄️ (+`E.2`/`E.8` після звуження) + фантом `PERF-3` у живому коді. Сіблінг `protocols_ref_check` для ID-простору | `ruby scripts/code_tracker_id_check.rb` (HARD `docs.yml` 2026-07-16) |
| **Ruby-version parity** (OPS.13) | Ruby-версія живе у 8 дзеркалах (`.ruby-version` = SSOT · `Gemfile` hard-pin · `.rvmrc` · `Dockerfile` FROM-tag · CLAUDE/AGENTS/copilot-орієнтації · [`06_01`](06_01_Deployment_Kamal_Terraform) Docker-stage) — Dependabot `docker`-ecosystem бампає ЛИШЕ Dockerfile: образ несе нову версію, `bundle install` вмирає на hard-pin, а PR зелений, бо жоден PR-тригер образ не збирає (`mirror-ghcr.yml` їде з `main`; живий капкан 2026-07-16 = PR #463). Кожне дзеркало = curated regex-набір; патерн, що перестав матчити = dead-mirror tripwire (стиль `field_canon_sync`) — список не гниє мовчки. Дзеркала ⊆ `changes`-фільтра docs.yml енфорсить CHECK D `guard_registry_sync` (скрипт експортує `MIRRORS`). Сіблінг-half: `docker_smoke` job у `ci.yml` (paths-gated повний build без push, member required-агрегату `ci-ok`) — знімає false-green клас для ВСІХ образ-бампів; повний Ruby-бамп = рецепт скіла `dependency-update` | `ruby scripts/ruby_version_sync.rb` (HARD `docs.yml` 2026-07-16) |
| **SDL static≡tpl** (INF.17) | Akash `deploy.yaml` ⟷ `deploy.yaml.tpl`: `services` ≡ `deployment` (сервіс поза `deployment:` ніколи не орендується) + compute-profile існує + обидва маніфести несуть той самий service-set. Pure ruby+yaml. ⚠️ Wired у **`ci.yml`** (`alloy_config_validate`, НЕ docs.yml) → `guard_registry_sync` CHECK A2 (DOC-T.44) стереже §3-реєстрацію (та сама «поза docs.yml»-стеля, що `test_doc_cache_sync`) | `ruby scripts/sdl_consistency_check.rb` (HARD `ci.yml`) |
| **deploy-secret placement** (INF.22) | Akash SDL secrets-at-rest: жоден committed key-літерал у service env (усі `REQUIRED_SECRET_NOT_SET`/`${tpl}`) · money-signing quintet JOB-ONLY (allow-list, не web/coap deny) · retired `ORACLE_PRIVATE_KEY` не воскресає · no present-empty env (`VAR=`) · `.dockerignore` тримає credentials поза публічним GHCR-образом. CI-гейт на SDL-placement (runtime `web3_network_guard` — інша половина). ⚠️ Wired у **`ci.yml`** (НЕ docs.yml) → CHECK A2 DOC-T.44 | `ruby scripts/deploy_secret_scan.rb` (HARD `ci.yml`) |
| **conda-lock ⟷ environment.yml** (FW.47) | committed `tools/in_silico/conda-lock.yml` == `tools/in_silico/environment.yml` (job `smoke` споживає ЛОК, не джерело → правка джерела без regen тихо ставила б старий пінований env). `--check-input-hash` = local hash-compare; drift → re-solve → `git diff` валить. config-mirror сіблінг `ruby_version_sync`. ⚠️ Wired у **`in_silico_smoke.yml`** (`lock_sync`, НЕ docs.yml) → CHECK A2 DOC-T.44 | `conda-lock lock --check-input-hash` (HARD `in_silico_smoke.yml`) |
| **offering-lexicon** (BIZ.22) | інвест-мова на поверхнях, які клієнт отримує. **HARD:** рендеровані **значення** локалей (не ключі), `docs/manifest.md`, `README.md`. **Звіт без падіння:** response-ключі `api/**`+`blueprints/**`, `app/views/**`, `db/seeds.rb` — до securities-присуду. Два набори термінів (OFFERING скрізь / HOMONYM лише в локаль-значеннях), три названі стелі й FP-класи, на яких гейт стоїть, — **шапка скрипта** (вона дім, бо не може розійтися з кодом). Присуд-дім — [`07_01 §8`](07_01_Nature_as_a_Service_Contracts) | `ruby scripts/offering_lexicon_check.rb` (HARD `docs.yml` 2026-07-25; `spec/lib/offering_lexicon_check_spec.rb`) |
| **SPDX-заголовки** (UNI.3) | in-scope source-файл без per-file `SPDX-License-Identifier`, чужий ідентифікатор, або мова без відомого комент-синтаксису. Гейт — **той самий двигун**, що робив розкатку, у `--check`-режимі: він не може розійтися з тим, що стереже (хвороба всіх рукописних дзеркал-гейтів). Скоуп (дерево × розширення), названі стелі та причина кожного виключення — **шапка скрипта** (вона дім). 🔴 Файл із **чужим копірайтом** репортується й НІКОДИ не штампується — вирішений випадок іде в DENY, щоб гейт не червонів вічно; детектор лишається на новий вендорнутий файл. Постава-дім — [`07_03 §3`](07_03_Academic_Integration_and_IP) | `ruby scripts/spdx_headers.rb --check` (HARD `docs.yml` 2026-07-26; `spec/lib/spdx_headers_spec.rb`) |
| **Memory-gate self-test** (DOC-T.59) | Гейт персистентної памʼяті (`.claude/hooks/memory_gate.sh`) не мав ЖОДНОГО тесту — і вже одного разу шипнув перевірку, структурно нездатну спрацювати: `overlap_check` стояв порогом 120 при фізичному максимумі корпусу 72, тобто був зелений на будь-якому стані, тижнями, і читався «грубим», а не мертвим. Батарея будує одноразовий фікстурний корпус у `mktemp` і наводить на нього гейт через `MEMORY_GATE_DIR`, тож CI не потребує доступу до самого корпусу (той живе ПОЗА репо — саме тому дім гейта тут, а не в `spec/`). **Кейс 1 — ЗДОРОВИЙ корпус, що мусить мовчати**, і кейс «дубль із роутером» теж: без цих двох батарея «чи стріляє» проходить для гейта, що кричить завжди. Покриває обидві стійки (`--audit` І PostToolUse stdin-контракт — одностійкова версія лишала момент дії без нагляду), обидві гілки розрізнення «стиснення ⊥ виселення», і декларує власні перевизначення порогів. ✅ **Стелю «фікстура ділить `MEMORY_GATE_REPO` з живим репо» ЗНЯТО** — батарея будує власне фікстурне репо (`$d.repo`), тож `DEADPATH` більше не відповідає станом чужого дерева. 🔴 **Вісь `CANONREF` додано 2026-08-04 [DOC-T.60]:** канон-рефи `NN_NN §X` живуть і в памʼяті, а не сканував їх **ніхто** — `code_doc_section_refs.rb` ходить по `app`/`spec`/`lib`+`.claude`, і корпус поза репо недосяжний для CI за побудовою; саме звідти агент процитував мертву `04_06 §A.10а` як канонічну. Резолвер **переюзано** (`file_section_dangling_refs`, docs-дерево передається ЯВНО — інакше фікстура мовчки відповідала б живим каноном), обидві стійки, курований per-file `exempt` для навчального `05_03 §749`. 🔴 **`CANONREF-EXEMPT-DEAD` додано 2026-08-09:** цей `exempt` **не стеріг сам себе**, тоді як його близнюк сотнею рядків нижче (`EXEMPT-DEAD` на `skill #N`) стеріг — одна таблиця, один закон, застосований до однієї з двох, у тому самому скрипті. Виняток, чий предмет зник, перестає щось захищати й починає благословляти НАСТУПНИЙ фантом, що сяде на ту адресу; помирає він двома способами (файл перестав цитувати реф · секція виросла й реф уже не dangling), і обидва означають, що запис має піти. Улов на живому корпусі **0** — тобто це батарея, що пінує досягнутий інваріант, а не worklist. Негативний контроль несучий: під фікстурним репо ці шляхи не існують, і «не застосовно» ≠ «протухло». Мутація в обидва плеча: «не стріляє» валить лише позитивний кейс, «стріляє завжди» — лише негативний. 🔴 **Вісь `NUMREF` додано 2026-08-04 [DOC-T.60]:** третій адресний простір — `скіл #N`, номер ПУНКТУ всередині секції, якого не парсить ЖОДЕН §-резолвер за побудовою. Вимір по всій популяції: 55 адрес із однозначною ціллю, **3 мертві** — і всі три однієї форми, якої припис не передбачав: номер РЯДКА, вдягнений як номер пункту (`frontend` визначає 17 пунктів). Такі адреси влучають у день написання й зсуваються мовчки з першої ж правки вище. Корінь виміряний: `frontend` несе 12 несучих абзаців БЕЗ номера, `ssot-maintenance` — 13, `backend` нуль — і в `backend` нуль дефектів класу, тож цитата в ненумерований абзац деградує або в сусідній номер, або в номер рядка. Скіл-дерево передається ЯВНО (та сама герметичність, що в `CANONREF`); негативний контроль обовʼязковий, бо детектор, що кричить на кожен `#N` біля імені скіла, спалив би 55 живих цитат. ⚠️ Стеля в шапці функції: ловить номер ПОЗА діапазоном, а «номер є, але про інше» — семантика, яку знаходить лише READ. 🔴 **Смуга `WARN` дістала кейси 2026-08-04 [DOC-T.59]:** при 25 наявних вона не мала ЖОДНОГО — а саме в ній зараз живуть усі пʼять правило-домів фази 2 (27–287 B під порогом), тобто непокритою була гілка, у яку впирається кожна наступна міграція. Дві половини: позитивна пінить **формулювання**, бо рядок доти друкував лише далекий `CAP`, і дім на 35 973 B читався як «ще 5 kB запасу» — тоді як `--audit` кінчається `[ -z "$out" ]`, тож той самий рядок уже робив EXIT=1 (**гейт описував одну величину, міряючи іншу**; робоча стеля — `WARN`, а `CAP` лише міняє пораду); негативна доводить, що детектор не кричить завжди. Мутації в обидва плеча: вимкнена гілка валить ЛИШЕ позитивний кейс, «кричить завжди» — лише негативні контролі, а відкат тексту на старий — лише кейс на формулювання. 🔴 **Вісь `ONEWAY` додано 2026-08-05 [DOC-T.61]:** правило-дім цитує журнал/проєктний файл як провенанс, а джерело не має струни назад — тож читач ДЖЕРЕЛА не дізнається, що клас уже має адресу, і наступний прохід виводить його з нуля (саме ця механіка родила DOC-T.58 і робила числа фази 2 такими, що ростуть замість збігатися). Три дизайн-рішення, кожне куплене окремо: детектор живе **поза `--audit`** (як `--genre`) — улов у десятках, тож у батареї він зробив би EXIT=1 вічним і привчив скіпати єдину стійку, що має лишатись гучною; групує **по ЦІЛІ**, бо роутер пишеться в неї, і одна правка закриває кілька пар; а одиниця — **ПОКРИТТЯ** (скільки домів цитує файл, що їх ігнорує), і це єдиний поріг у скрипті, який НЕ є розміром: роутер у `project_*`-джерело його збільшує, тож байтова метрика оцінила б правильний фікс як регресію — рівно тому вісь і лишалась невиміряною. Мутація в обидва плеча: позитивний кейс ловить односпрямовану пару фікстури (при `ONEWAY_MIN=1`, бо робочий поріг 2 ранжує worklist, а не визначає клас — стеля тріажу не сміє ховати єдиний доказ, що детектор працює), негативний доводить, що він міряє **взаємність**, а не наявність лінка: щойно ціль відповідає, та сама пара мусить замовкнути. 🔴 **Вісь `PROSE` додано 2026-08-05 [DOC-T.61]:** екстрактор струн стояв на класі `[a-zA-Z0-9_-]`, тож поняття, взяте в подвійні дужки кирилицею (`«дім»`, `«струну»`) або багатокрапкою, **не діставало жодного вердикту** — його відкидали ДО розгалуження, і три такі жили в корпусі під зеленим гейтом. Діагноз саме «сліпий», а не «плутає», і це різні хвороби: сусідній `oneway_check` тим часом бачив ту саму струну широким класом і мовчки викидав як «не present» — **дві граматики над одним корпусом, і струна, що не належить жодному детектору**. Екстрактор зведено до граматики `oneway_check`, але вердикт **свідомо ОКРЕМИЙ** від `DANGLING`: там хтось мав намір злінкувати й ціль зникла (лік — полагодити ціль), тут наміру не було взагалі (лік — зняти дужки), тож спільне повідомлення радило б хибне на хибній підставі. Розширення класу **вимагає** `while read`: `for l in $(…)` розщепив би багатослівний вміст на фантомні струни. Заразом знято латентну половину того ж класу всередині ТІЄЇ САМОЇ функції — індексні лінки читались вужчим класом (без дефіса й великих), ніж струни, тож файл із дефісом в імені був би струнно-видимий і невидимий для `BROKEN`/`ORPHAN`. Мутація в обидва плеча: повернення вузького класу валить ЛИШЕ новий позитивний кейс, а негативний доводить, що новий вердикт **не зʼїв старий** — справжній латинський dangling мусить лишитись `DANGLING`, інакше фікс проміняв би одну сліпоту на іншу, і лічба цього не показала б. 🔴 **Дві поправки, куплені adversarial'ом проти цього ж фікса — обидві про те, що напівфікс гірший за жоден.** (а) Вердикт спершу жив лише в `--audit`, тобто **мовчав у момент, коли дужки друкують** — та сама двостійкова розбіжність, за яку файл уже платив (діра `UNSTRUNG`); струнну ногу винесено в `strings_check`, і її ганяють ОБИДВІ стійки (решта `integrity_check` до стійки запису не годиться: `ORPHAN` на щойно створеному файлі ОЧІКУВАНИЙ, і той момент належить `route_check`). (б) Граматика «індексний лінк на файл» мала **три копії**, а розширили одну: імʼя з дефісом тоді читається як живий лінк для `integrity_check`, тоді як обидва лічильники досяжності його не бачать — і здоровий корпус дістає звинувачення в **eviction**, тобто пораду на хибній підставі, найгіршу форму відмови ратчета. Зведено в `IDX_LINK_RE` (One-Home). ⚠️ Кейс на це спершу вийшов **вакуумним** і пережив мутацію: дискримінатор «eviction ⊥ compression» біжить лише НИЖЧЕ ратчета, тож без `MEMORY_GATE_IDX_BASELINE`-override гілки не досягав — пінити треба ЗДОРОВУ гілку, бо відмова тут бреше в заспокійливий бік. **Осі, додані після першої реєстрації** (по одному датованому буліту на вісь — конвенція рядка, і саме її вимагає §Guard-craft #34): (в) `--oneway` (DOC-T.61, 2026-08-05) — джерело, яке дім цитує провенансом, а воно назад не роутить; **worklist-режим ПОЗА `--audit`** свідомо, бо улов у десятках зробив би EXIT 1 вічним; env-важіль `MEMORY_GATE_ONEWAY_MIN`, не голий `ONEWAY_MIN`. (г) `PROSE` (DOC-T.61, 2026-08-05) — термін у подвійних дужках, який ніколи не був слагом: `integrity_check` був кирилично сліпий до цієї форми. (д) **peer-line виключення в `--oneway`** (2026-08-05) — рядок `Related:` / `See`-перелік є рівневою крос-цитатою, односпрямованою ЗА ДИЗАЙНОМ, тож рахувати його провенансом означало систематично завищувати: з чотирьох звітованих пар справжньою була одна. Виміряно по корпусу 265 струн зі стрілкою проти 25 рівневих, READ усіх 25 не знайшов серед них провенансу; mutation-verified парою плечей. (е) **`BROKEN` + `FORMAT` + `override_check` перенесено в стійку ЗАПИСУ** (2026-08-08) — жили лише в `--audit`, тобто мовчали в момент дії: `BROKEN` народжується Edit'ом у `MEMORY.md` (найредагованіший файл корпусу), `FORMAT` — записом, що зронив ключ, а `override_check` звітував про чужі пороги в **одній стійці з чотирьох**, тож експортований `MEMORY_GATE_*` роззброював хук безшумно. Винесено у спільні `broken_check`/`format_check_one`, щоб стійки ганяли ОДИН код; `ORPHAN` лишається audit-only свідомо (очікуваний на новому файлі — той момент належить `route_check`). **І сам КЛАС дістав носія, бо в писаній формі правило впало тричі:** різниця стійок тепер ОБЧИСЛЮЄТЬСЯ з файлу й звіряється з двома оголошеними винятками (`overlap_check` — O(n²) і клас накопичується; `integrity_check` — його частини вже в стійці запису поодинці), тож перевірка, додана в одну стійку, червонить self-test поіменно. 🔴 **І сам детектор паритету мав ДРУГУ половину, знайдену мутацією ЙОГО САМОГО 2026-08-08:** різниця рахувалась лише в один бік («є в `--audit`, нема в стійці запису»), а частини оголошеного винятку `integrity_check` живуть у стійці запису ВИКЛЮЧНО, тобто стоять поза відніманням **за побудовою** — зняття `format_check_one` зі стійки запису повертало чистий `PARITY-OK`. Проза над винятком уже НАЗИВАЛА три делеговані частини; назвати не означало перевірити. Тепер виняток **оплачує себе**: перелік делегованого перевіряється на присутність з іншого боку (`PARITY-EXEMPT-UNPAID`), а сам перелік стережеться від протухання (`PARITY-GHOST` — імʼя, що вже не є функцією файлу, саме є знахідкою). Клас-дім → скіл `ssot-maintenance` §Guard-craft. (ж) **`MOJIBAKE`** (2026-08-08) — обірваний багатобайтовий символ (`U+FFFD`) посеред кирилиці: файл лишається валідним текстом, речення читається, усі перевірки зелені, а слово вже не те; у корпусі поза git це тиха корупція без відкату. Двічі за одну сесію спіймало око, жодного разу прилад. Обидві стійки; **негативний контроль несучий** — детектор, що спрацьовував би на здоровій кирилиці чи гліфах (`⊥ ⛔ 🔴`), був би гірший за відсутній. (з) **`--weight`** (2026-08-08, DOC-T.62) — прицінює частку корпусу «про агента» проти «про продукт»; **ПРИНЦИПОВО без порога й завжди exit 0**, бо поріг тут запрошував би різати шар, який не дає перекуповувати уроки; друкує ВИЛКУ з іменами swing-файлів, бо одна цифра була б вердиктом у костюмі виміру. (и) **`--stops`** (2026-08-08) — worklist негативного знання (⛔ / won't-do / «не відбудовувати»), обчислюваний із джерела щоразу, бо ручний реєстр був би четвертим дзеркалом, а протухла заборона гірша за відсутню. Набір патернів **виміряний**: `⊥` ВИКЛЮЧЕНО (≈15–20% точності — за визначенням корпусу це маркер компромісу, і більшість хітів це заголовки «⊥ Контр-правило»), `do NOT`/`don't` додано (≈90%, ловлять те, чого не торкається жоден український маркер); режим **декларує власну стелю при кожному прогоні** (floor, не census: повний READ дає 2–4×, і ~половина навіть маркованих рядків не несе перевірної адреси) (і) **`HOMOGLYPH`** (2026-08-08, DOC-T.62) — суфікси адрес пунктів скілів жили у ДВОХ абетках (`26a` U+0061 і `26а` U+0430), і половини падали в ПРОТИЛЕЖНІ боки, обидві мовчки: кириличний МАРКЕР не входив в адресний простір узагалі (клас `[a-z]` його не бере, рядок не дає нічого), а кирилична ЦИТАТА зрізалась до голого номера й резолвилась у СУСІДА при зеленому `include?` — хибна адреса, що ПРОХОДИТЬ, гірша за фантом, бо про неї більше ніхто не спитає. Прив’язка до вікна `skill #N` несуча й має власний негативний кейс: канон-номери — ІНШИЙ адресний простір і законно кириличні (`04_06 §A.2` правило `10а` є справжнім заголовком), тож детектор, ключований на символі замість вікна, вимагав би перейменувати живий канон. (ї) **Периметр цитат розширено з памʼяті на РЕПО** (2026-08-08, DOC-T.62) — доти скан читав `Dir["*.md"]` під `MEM_DIR` і репо не відкривав ніколи, тож фантом у трекері, скіл, що цитує інший скіл, і кожен `skill #N` у `spec/`·`docs/`·`tools/`·`firmware/` були недосяжні ЗА ПОБУДОВОЮ при зеленому гейті (виміряно: 97 цитат у памʼяті, 33 поза нею). Розширення НЕ безкоштовне й тримається на трьох речах разом: **(1)** якір звужено з «імʼя поруч» до **скіл-конструкції** (backtick-и або `name-скіл`), бо над `docs/` `deploy`/`frontend` — звичайні слова, і «верифікувати deploy … Pre-Flight #9» читалось цитатою на скіл без пункту 9; виміряно, що цей якір знімає обидва такі хибні позитиви й не втрачає ЖОДНОЇ справжньої цитати; **(2)** ілюстрація фантома байт-у-байт дорівнює фантому, тож єдиний чесний інструмент — **оголошений ПЕР-РЕФ виняток** (бланкетний по файлу мовчки зняв би перевірку з усіх інших адрес того файлу), і сам файл гейта мусить виключати ВЛАСНІ фікстури, інакше widening червонить гейт на його ж навчальному матеріалі; **(3)** виняток, чий предмет зник, сам є знахідкою (`EXEMPT-DEAD`) — інакше він мовчки благословить наступний фантом, що сяде на ту адресу. Вендорні дерева виключено: 96% глоба, і без цього аудит ішов 34 с замість 15, а `PostToolUse`-хук такої ціни просто вимикають. 🔴 **І саме це виключення тримало CI червоним, бо фільтр міряв АБСОЛЮТНИЙ шлях** (виправлено 2026-08-08): фікстурне репо батареї живе під `mktemp`, а на Linux це `/tmp/…`, тож патерн `/(…|tmp)/` відкидав УВЕСЬ фікстурний периметр. Діагностична прикмета варта більшого за сам баг: мовчали рівно два ПОЗИТИВНІ репо-кейси, а всі негативні проходили **ВАКУУМНО** (порожній периметр мовчить безкоштовно), тому батарея звітувала 49/2 і виглядала як два локальні дефекти, а не як знесений периметр. Локально зелено **за побудовою**: macOS `mktemp` ігнорує `TMPDIR` і завжди віддає `/var/folders/…`, тобто платформенний розкол, якого жоден локальний прогін не бачить — відтворення вимагало підміни `root` на `/tmp/…`. Лік структурний: `reject` стоїть ПІСЛЯ `.map` і фільтрує **репо-відносний** шлях, бо абсолютний несе ще й імена батьківських каталогів, до нашого дерева не належні. (й) **`NAME`** (2026-08-08, DOC-T.62) — рівність ключа `name:` та ІМЕНІ ФАЙЛУ не перевіряло ніщо: `format_check_one` питав лише про присутність ключа. Це два написання однієї тотожності, з яких **адресою є лише одне** — струни резолвляться через `ls | grep -qix`, індексні рядки через `(file.md)`, тобто корпус навігує файловою назвою, тоді як `name:` — те, чим читач (і будь-який агент, що вірить frontmatter) вважає сам запис. Дорога форма не косметична, а **перейменування**: файл переїхав, струни й індексний рядок полагоджено, а протухлий ключ далі називає слаг, який або не резолвиться, або — гірше — резолвиться в СУСІДА; це рівно та тиха резолюція в чужий дім, за яку вже заплатив адресний простір `skill #N`. Корпус на момент вводу чистий, тож це **батарейний кейс, а не worklist**: вердикт існує, щоб тримати чесним майбутнє перейменування. Чотири кейси, і несучий серед них — **негативний** (перевірка біжить по кожному файлу на кожен запис, тож детектор, що вважав би легальне імʼя розбіжністю, зняли б того ж дня) плюс **case-only** форма, яку файлова система macOS резолвить мовчки й тому ховає. 🔴 **`UNSTRUNG` розширено з журналів на ВЕСЬ корпус 2026-08-08 [DOC-T.62]:** перевірка «на цей файл ніщо не лінкує» жила з предметом `log_*`, бо журнали — це ті, кого свідомо нема в індексі, і та рамка замерзла в периметрі. Але ПОДІЯ, яку вона стереже, — правка, що зрізає останню струну, — не частіше падає на журнал, ніж на правило-дім; не-журнал при цьому лишає індексний рядок і мовчки перестає резонувати, а рядок є слабшим із двох носіїв. Обидві стійки, улов на живому корпусі **0 зі 125**, тобто пінується вже досягнутий інваріант, а не відкривається worklist. Заразом знято самоцитування (старий `grep -r` по каталогу зараховував сам файл, тобто файл сертифікував власну досяжність) і **сліпу пляму паритет-детектора**, який сканує лише імена на `*_check`. Три кейси, кожен мутаційно доведений: журнальна межа · самоцитування · формулювання `GENRE`, яке тим самим проходом перестало бути вироком і стало ТРИГЕРОМ із тріажем — носієм тут є сам текст повідомлення, бо саме там стоїть читач у момент зрізу. 🔴 **Вісь `AMBIG` додано 2026-08-08 [DOC-T.62]:** чотири скіли законно ведуть кілька нумерованих послідовностей, тож `#N` називає дві різні речі, і гола цитата резолвиться в ту, яку читач зустріне першою — мовчки, бо номер ІСНУЄ і `NUMREF` зелений за побудовою. Дефект не «є дубль» (їх 24 і жоден не помилка письма), а «процитували БЕЗ секції»; улов вузького питання нульовий, тому це батарея, а не worklist. **Одиниця — ФАЙЛ, і це несуче:** `ssot-maintenance` повторює кожен свій номер у двох файлах, бо `guard-craft.md` є джерелом, а блок у `SKILL.md` із нього ГЕНЕРУЄТЬСЯ; лічба по скілу назвала б усі 45 дублями й почервоніла б на найздоровішому інваріанті дерева. Три кейси, і третій пінить саме одиницю. 🔴 **Вісь `DARK` (розвʼязання ruby) додана 2026-08-09 [DOC-T.63]:** десять перевірок гейта — ruby-heredoc'и, і кожна відкривалась вартовим `command -v ruby`, тобто перевіряла ПРИСУТНІСТЬ інтерпретатора, а не його ПРИДАТНІСТЬ. Виміряно в живій сесії: голова `PATH` вела на gemset версії, знятої при бампі, тож `command -v ruby` резолвився в macOS 2.6 — під ним один heredoc не парситься, ще два вмирають на завантаженні `lib/tracker/dashboard.rb`, і **девʼять із 64 кейсів падали, поки `--audit` друкував `OK` і виходив нулем**. CI цього не бачить за побудовою (`docs.yml` ставить `.ruby-version`), тобто зелений там назавжди — інверсія «local green ≠ CI», де червоне саме там, де гейт реально біжить. Лік — не «перевір версію», а **резолвити** (`$MEMORY_GATE_RUBY` → PATH → `~/.rvm/rubies/default` → `/usr/bin/ruby`, з пробою на `filter_map`+endless-def, тобто на ті фічі, яких код справді потребує), плюс fail-CLOSED-ліхтар: порожній набір знахідок означає «чисто» лише тоді, коли всі перевірки ВИКОНАЛИСЬ. Заразом знято тихий `rescue Exception → exit 0`, чий власний коментар заявляв «never a false alarm» — саме він глушив `CANONREF` без жодного сліду. Три кейси, і два з них контролі: резолвер під голим PATH · непридатний ruby ЧЕРВОНИТЬ замість `OK` · здоровий прогін про темряву мовчить (інакше перший задовольнявся б гейтом, що кричить завжди). | `bash .claude/hooks/memory_gate.sh --selftest` (HARD `docs.yml` 2026-08-04) |
| **DCO sign-off** (UNI.20) | кожен НЕ-бот коміт inbound-PR несе `Signed-off-by`, що **збігається з автором** (не лише наявність трейлера); `[bot]`-автори exempt. Чому author-match, чому боти виняток і чому власний гейт замість GitHub App — **шапка скрипта**. Job падає на порушенні; branch-protection-флип на `DCO passed` **зроблено 2026-07-25** — 8-й required-контекст (`:required`; звірено проти API, не з канону) → [`00_07`](00_07_Action_Plan_Tracker) UNI.20. Постава DCO>CLA + дві застороги — [`07_03 §3`](07_03_Academic_Integration_and_IP). 🔴 **Другий носій додано 2026-08-08, і без нього реєстр брехав про периметр:** цей job стереже **INBOUND PR**, а авторизований прямий push у `main` PR не відкриває — тобто шлях, яким їде більшість комітів, єдиний наявний гейт не бачив **за побудовою** (сам push друкує `Bypassed rule violations … 8 of 8 required status checks are expected`, тож увесь required-периметр на цьому шляху **advisory**). Правило «завжди `-s`» при цьому стояло у двох місцях і рецидивувало двічі (три коміти, потім шість), бо носієм називали «прогнати команду перед push'ем» — тобто ЩЕ ОДНЕ правило, яке треба памʼятати. Закрито `.githooks/pre-push` + `core.hooksPath`; свідомо **лише DCO** (повна сюїта на кожному push купує звичку обходити хук через `--no-verify`, і тоді на шляху не лишається нічого) | `ruby scripts/dco_check.rb` (`dco.yml` 2026-07-25; `spec/lib/dco_check_spec.rb`) + `.githooks/pre-push` (локальний, мутаційно перевірений) |
| **Guard-craft index ⇄ source** (DOC-T.62) | «Сліпоти гейта» скіла `ssot-maintenance` живуть у його `guard-craft.md` (читається на вимогу), а `SKILL.md` несе **генерований** однорядковий індекс — бо цей скіл авто-інвокується майже щосесії, а тіла потрібні лише при роботі з гейтом (винос зняв **69 kB** з автоматичного завантаження). Рукописний індекс був би другим домом для кожного з них, тобто рівно класом, який ці ж пункти й описують, тож гейт тримає інваріант «індекс = функція джерела». Стереже й три речі, яких diff не бачить: **номери append-only** (на них 65 цитат у 33 файлах; зниклий номер осиротив би їх), **дублі номерів**, і **word-floor** — рядок індексу коротший за поріг вироджується в ЯРЛИК, а не носія, і полагодити його можна ЛИШЕ в джерелі. ⚠️ Межа названа в скрипті: підлога ловить КОРОТКІСТЬ, не абстрактність — механічної перевірки на «чи цей рядок стріляє» не існує | `ruby scripts/guard_craft_index.rb --check` (`docs.yml`; `--write` регенерує) |
| **zsh split-scan self-test** (DOC-T.62) | Правило C у `.claude/hooks/bash_verify_guard.sh` **відмовляє** у виклику за словом парсера `.claude/hooks/zsh_split_scan.rb`, тож сканер стоїть на критичному шляху КОЖНОГО Bash-виклику — а це рукописний stateful-скан лапок і `$()`-глибини, тобто рівно те, що гниє тихо. Клас: zsh, на відміну від bash, **не робить word-splitting** голого скаляра, тож `for x in $list` пробігає ОДИН раз по всьому рядку, а `set -- $pair` лишає `$2` порожнім для БУДЬ-ЯКОГО входу — і перевірка, що йде далі, рапортує успіх незалежно від того, що їй дали. Вимір 2026-08-08 по 17 524 викликах / 32.3 доби: широкий якір ~975/міс при ~2–3% точності (**відмовлено — не переоткривати**), вузький — 18 знахідок (~17/міс), і всі **18 із 18 звірені з ЗАПИСАНИМ виводом того самого виклику**, а не прочитанням коду; серед них хибно-зелене «порожньо = добре» перед `rm` семи файлів памʼяті та `EXIT=0` шести гейтів перед `git push`. Батарея пінить ОБИДВА плеча: 5 кейсів, що мусять ловитись, і 12 безпечних ідіом (`$(subst)` · zsh-масив · глоб · RHS присвоєння · дужка всередині лапок), що мусять мовчати — детектор, який кричить завжди, блокував би звичайну роботу. Мутація по кожному з шести винятків червонить рівно свій кейс, і два перші кейси виявились ВАКУУМНИМИ саме нею. | `ruby .claude/hooks/zsh_split_scan.rb --selftest` (HARD `docs.yml` 2026-08-08) |

> **Скоуп-стеля реєстру §3 (DOC-T.44).** `guard_registry_sync` тримає §3⟷CI двонапрямно: **CHECK A** — кожен `run:`-крок джоба `docs_check` (`docs.yml`) названий тут дослівно; **CHECK A2** — курований allow-list canon↔code/config-mirror гейтів, що живуть **ПОЗА `docs.yml`** (`check_firmware_tables`/`check_bytecode`/`gen_bytecode`·`sdl_consistency_check`·`deploy_secret_scan` у `ci.yml`; `emit_c --check` у `ml_smoke`; `test_doc_cache_sync`·`conda-lock` у `in_silico_smoke`) — кожен мусить мати §3-рядок І досі бігти у своєму workflow (новий такий гейт → додати у `CANON_CODE_GATES_OUTSIDE_DOCS` + §3-рядок, інакше RED; retired → dead-map RED); **CHECK E** — реверс (кожна §3-команда живе у названому workflow). **Стеля:** A2 навмисно **curated**, не авто-скан кожного workflow (більшість кроків `ci.yml`/`solidity_audit` = build/test/lint/fuzz, не реєстрові — розрізнити їх авто неможливо). Code-коментарні `NN_NN §X`-рефи по `app`/`spec`/`lib` **+ `.claude/**`** (routing-шар skills/prompts) стереже `scripts/code_doc_section_refs.rb` — **HARD-крок `docs.yml` з 2026-07-25** (промоут відбувся; FP-rate 0 на корпусі). ⚠️ Попередня редакція цієї стелі досі твердила «report-only, свідомо НЕ CI-гейт, промоут лише коли FP-rate доведено 0», тобто **два записи одного реєстру про один предмет несли протилежні присуди, і обидва були зелені** — бо `guard_registry_sync` звіряє КОМАНДИ, а не прозу стелі над ними.

**Правило при зміні факту:** правити лише у home (§2) → рефи лишаються чинними; будь-який новий NN_NN-док/реф — `docs:check_refs` має лишатись зеленим перед merge.

> **Doc ↔ code sync (model/service реєстри).** `db/structure.sql` після `db:migrate` (і `app/models/` / `app/services/` / `app/workers/`) — authoritative reality; реєстри-доки [`04_01`](04_01_Data_Models_and_Entities) (БД-шар) і [`04_02`](04_02_Business_Logic_and_Services) (service-шар) — її SSOT-опис. Метод drift-resolution: **код випередив док** (нова модель/сервіс/таблиця) → онови реєстр-док; **док випередив код** (документована сутність без файлу) → задача в [`00_07`](00_07_Action_Plan_Tracker); **нема ні там, ні там** → рішення (потрібне → 00_07; ні → видали план з доку). Механічну частину енфорсить `model_doc_sync` (§3); решта (AASM-стани, поліморфні `_type/_id` пари) — ручний cool-down аудит ([`00_04 §5.3`](00_04_Shape_Up_Operations_and_RnD_Clusters)). Колишні per-doc «SSOT Drift Register» секції (04_01 §12 / 04_02 §13b) тепер тонкі вказівники сюди — датований лог виправлень живе в git, не в каноні.

> **Чому value-guards «знають» константи (Lorenz β, RTC-реєстри, mint/carbon-курс) — і це НЕ оверінжиніринг:** це **owner-only vocabulary**, а не оракул коректності. Гейт не перевіряє, що β=8/3 *правильне* — він перевіряє, що воно не **продубльоване** поза домом (03_04 §4.1). Хардкод значення в регексі — **навмисний tripwire**: коли governance перепрайсить курс або змінять β, гейт червоніє й **примушує** свідомо оновити всі згадки, а не лишити тихий stale — **разом із регексом самого гейта** (інакше НОВЕ значення лишиться без One-Home-нагляду — латентна міна, зафіксована DOC-T.40). Це фіча, не технічний борг. Довгостроково ці параметри стануть runtime-керованими (`ProtocolParameters`, [`05_06`](05_06_Governance_and_DAO)) — тоді guard природно еволюціонує з value-literal у reference-check. Engine-и (`lib/docs_*.rb`) — **чисті Ruby-функції без Rails-залежності** (unit-tested; rake-таска лише оркеструє I/O), тож логіка гейту не прив'язана до стеку.

> **Швидкий локальний прогін (без Rails):** `ruby scripts/docs_check.rb` ганяє `docs:check_refs` + `tracker:check` за ~0.3 с (vs ~1.2 с `bin/rails`), reuse-ячи ТІ САМІ rake-тіла (нуль дублювання — не може розійтися з CI); потрібен лише `ruby` + `rake` (default gem), без `bundle`/БД. CI-дім цих гейтів — **`docs.yml` (єдиний; дубль із `ci.yml` прибрано 2026-06-02, щоб mixed code+docs PR не ганяв їх двічі)**; `docs.yml` тригериться й на `.github/**` (щоб external-doc-path guard бачив зміни поза `docs/`). `main` **захищено** branch-protection: SSOT-гейти required через always-on агрегат **`docs-ok` (`Docs passed`)** у `docs.yml` — path-gated `docs_check` не може бути hard-required напряму (блокував би code-only PR, що його skip'ить), а `docs-ok` (`if: always()`) ніколи не skip'иться. Канон CI — `06_07 §2`.

**Публікація канону на Wiki:** дзеркалить `docs/NN_NN_*.md` → GitHub Wiki — нормалізує лінки (canon → bare wiki-link; non-doc repo-файли → absolute `blob/main` URL) + переносить зображення. Публікація **автоматична on-merge** (тригер/off-switch — дім [`06_07 §1`](06_07_CICD_and_Runbook_Index), не дублюється тут); локальний `bin/rails wiki:sync` лишається як dry-run-режим (§4 крок 5). Engine `lib/wiki_link_normalizer.rb` (unit-tested), один на обидва шляхи. Деталі — `lib/tasks/wiki.rake`.

> **Wiki — генерований артефакт, one-way (docs/ → Wiki), НЕ редагується вручну.** SSOT живе ТІЛЬКИ в `docs/`; правка через web-інтерфейс Wiki буде затерта наступним sync (тож drift «хтось поправив у Wiki, і воно не повернулось» не виникає — Wiki не є джерелом). Нормалізатор **обов'язковий**: сирий `git push --mirror` зламав би всі canon-лінки (напр. [`05_05`](05_05_Slashing_and_Risk_Policy) у Wiki має стати bare-сторінкою), repo-file-посилання та зображення — саме тому існує `wiki_link_normalizer.rb`, а не голий mirror. Тобто це НЕ «подвійне життя SSOT», а детермінована one-way генерація (як збірка сайту з джерела).

---

## 🔀 4. Module-restructure / extract-to-new-page

Коли SSOT-факт переростає свій док (тема ≈ пів-сторінки + розпорошена дублями по сусідах), її виносять у **власну канон-сторінку**. Метод (verified 05/07-реструктуризацією 2026-05-30: slashing `00_01 §6` → `05_05`, governance `05_03 §749` → `05_06`; та таксономічною реструктуризацією v3 2026-05-30: junk-drawer Module 00 → Foundation (Vision+Method, 00) + Tier I (Система 01–06) + Tier II (Програма 07–08)):

1. **Migrate-first:** наповнити новий дім ПОВНОЮ субстанцією + verify present ПЕРШ ніж різати джерело (zero-loss).
2. **Stub + pointer:** джерело лишає тонкий vision/ref-stub `→ новий_дім`; механіка реферить існуючі доми, не дублює.
3. **Cross-ref sweep, anchored:** масовий re-point `NN_NN §X` → новий дім, **прив'язаний до якоря** (напр. лише рядки з конкретним old-ID), щоб не зачепити однойменні внутрішні §X інших доків. Ruby-скрипт-**файл** (не inline `-e`) з dry-run + presence-check + zero-loss referrer-diff. Для wholesale-renumber — single-pass simultaneous replace (одна alternation, ordered specific→generic) проти chained-rewrite багів; spine-owner-refs не в map → виживають.
4. **Реєстр + індекс:** оновити `§2` home-registry, `00_00` reading-order, README; archival-pointer у `00_07` (DOC-row).
5. **Per-фаза gate:** `docs:check_refs` + `tracker:check` зелені; zero-loss set-diff (referrers before/after = 0 lost); `wiki:sync` dry-run.

> **Ruthless Pruning (зворотний бік zero-content-loss).** «Zero-loss» стосується **реструктуризації** (перенесення *живого* факту в новий дім), а НЕ архівного накопичення. Ключова межа — **мертвий артефакт vs живе обґрунтування**: спростовану гіпотезу, застарілу залізку чи чисту історію реалізації (напр. гіпотеза «44 мВ streaming-potential» як джерела енергії, 4 голки-електроди Кельвіна, U.FL-кабель, breadboard-каскад LTC3108→Meissner) **видаляють з активного канону**; натомість **стисле design-justification** («чому обрали X, а не Y» — 1–2 рядки + pointer) **лишається інлайн**, бо пояснює *поточний* дизайн. NB: ретирується мертва **роль**, не завжди весь компонент — LTC3108 виключено як первинний boost-каскад, але він **виживає** як DNP cold-start fallback ([`02_03 §1.5`](02_03_BQ25570_MPPT_Nano_Power)) і як bench-harvester гібридного стенда ([`02_04 §5`](02_04_Bench_Build_Guide)), тож лишається у каноні легітимно. Мертві ідеї роздувають контекст для LLM-агентів → ризик галюцинацій «воскреслими» концептами (Git тримає історію, цього досить). Якщо артефакт має історичну/навчальну цінність — виносять у явно марковану **legacy-appendix** сторінку (`*_Appendix`-basename авто-exempt-иться лінтерами; з AI-контексту виключають через `.aiignore`/`.cursorignore`), а не лишають у активному дереві. Колишній взірець 02_04 перепрофільовано у живий bench-guide ([`02_04`](02_04_Bench_Build_Guide)) під повним docs-gate — legacy-каскад там несе живу bench-роль, не воскреслий production-концепт. Повернення ретированого **імені** (лише однозначно мертвого — напр. партномер ZP-3/ZP-5; НЕ ще-живий LTC3108) замикає `DocsLinter::DEPRECATED_TERMS` (§3).
