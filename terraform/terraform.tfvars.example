# GCP Project
project_id = "your-gcp-project-id"
region     = "europe-west1"
zone       = "europe-west1-b"

# Database
db_password          = "CHANGE_ME_use_a_strong_password_16_chars_minimum"
db_tier              = "db-custom-2-7680"
db_availability_type = "REGIONAL"
db_disk_size_gb      = 50
db_max_connections   = "400"
db_read_replica_count = 0

# Protection
enable_deletion_protection = true

# SSH Access — IMPORTANT: restrict to your IP/VPN CIDR in production.
# 0.0.0.0/0 is rejected by variable validation. Leave [] to skip the SSH firewall
# entirely (reach instances via IAP / OS Login / gcloud).
ssh_user          = "deploy"
ssh_public_key    = "ssh-ed25519 AAAA... deploy@silken-net"
ssh_source_ranges = ["203.0.113.0/24"] # CHANGE_ME to your VPN/office CIDR (or [] to skip)

# Redis — REMOVED from GCP (Memorystore has no public IP).
# Use Upstash (serverless Redis with TLS public endpoints) instead.
# Set REDIS_URL=rediss://default:password@endpoint.upstash.io:6379 in secrets.
# Free tier (10k commands/day) is sufficient through TRL 8.
# See: docs/06_02_Akash_Network_Integration.md
