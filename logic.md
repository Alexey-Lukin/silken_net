# 🌲 Silken Net: Специфікація Логічного Ядра (Services & Workers)

Цей документ описує архітектуру сервісів та фонових воркерів екосистеми **Silken Net** — децентралізованої мережі моніторингу лісів (D-MRV) та автоматизованого реагування.

---

## 🧠 I. Сервіси (Services) — Логічне ядро

Сервіси відповідають за складні обчислення, інтерпретацію бінарних даних та взаємодію з блокчейном.

| Сервіс | Функція | Опис |
| --- | --- | --- |
| **`TelemetryUnpackerService`** | **L3 Декодер** | Розпаковує 21-байтні бінарні чанки телеметрії. Проводить калібрування сенсорів та первинний розрахунок Z-стабільності. |
| **`OtaPackagerService`** | **Кузня Оновлень** | Фрагментує прошивки та TinyML-моделі на пакети під MTU (LoRa/CoAP). Додає заголовки цілісності та маніфести. |
| **`AlertDispatchService`** | **Нервовий Центр** | Аналізує вхідні дані на предмет пожеж, вандалізму чи стресу. Приймає рішення про створення `EwsAlert`. |
| **`EmergencyResponseService`** | **Рефлектор Дії** | Визначає протокол фізичної відповіді (відкриття клапанів, сирен) на основі типу загрози в кластері. |
| **`HardwareKeyService`** | **Ключник** | Керує життєвим циклом AES-ключів. Реалізує протокол **Dual-Key Handshake** для безпечної ротації. |
| **`InsightGeneratorService`** | **Оракул** | Добова агрегація. Включає **AI Fraud Guard**, що перевіряє дерева на фрод через порівняння з кліматичним базлайном. |
| **`BlockchainMintingService`** | **Скарбник** | Пакетна емісія токенів (SCC/SFC) у мережі Polygon. Оптимізовано для Gas Saving Mode (batchMint). |
| **`BlockchainBurningService`** | **Меч Правосуддя** | Розраховує суму вилучення та ініціює блокчейн-функцію `slash` при порушенні умов контракту. |

---

## ⚙️ II. Воркери (Workers) — Виконавчі м'язи

Асинхронні процеси Sidekiq, що забезпечують масштабованість та відмовостійкість системи.

### 📡 Рівень Зв'язку (Uplink/Downlink)

1. **`UnpackTelemetryWorker`** (`uplink`): Вхідна точка для CoAP/UDP. Дешифрує батчі та підтверджує успішну ротацію ключів шлюзу.
2. **`GatewayTelemetryWorker`** (`default`): Діагностика "Королев" (шлюзів). Стежить за напругою батарей, температурою вузлів та сигналом зв'язку.
3. **`ActuatorCommandWorker`** (`downlink`): Доставка наказів на актуатори. Використовує Grace Period для вибору вірного ключа шифрування.
4. **`ResetActuatorStateWorker`** (`downlink`): **Кенозис**. Автоматично повертає актуатор у стан спокою після завершення часу дії наказу.
5. **`OtaTransmissionWorker`** (`downlink`): Транслятор еволюції. Передає прошивки чанк за чанком, враховуючи час запису у Flash-пам'ять STM32.

### ⚖️ Рівень Аудиту та Токеноміки

6. **`DailyAggregationWorker`** (`low`): **Хронометрист**. Диригент добового циклу. Запускає агрегацію інсайтів та подальший ланцюг аудиту.
7. **`ClusterHealthCheckWorker`** (`default`): **Вартовий**. Ретроспективний аудит NaaS-контрактів за конкретну дату на предмет порушень гомеостазу.
8. **`TokenomicsEvaluatorWorker`** (`default`): **NAM-ŠID**. Аудит балів росту та ініціація емісії SCC, якщо поріг у 10,000 балів перетнуто.
9. **`MintCarbonCoinWorker`** (`web3`): **NAM-TAR**. Емісійний вузол. Включає протокол атомарного ролбеку балів при критичних помилках RPC.
10. **`BurnCarbonTokensWorker`** (`critical`): **Екзекутор**. Негайне виконання Slashing-вироку та створення "надгробного" запису в MaintenanceRecord.
11. **`InsurancePayoutWorker`** (`web3`): **Гарант**. Автоматичне виконання виплат за параметричним страхуванням при настанні страхових подій.

### 📢 Рівень Оповіщення

12. **`AlertNotificationWorker`** (`alerts`): **Голос Цитаделі**. Багатоканальна розсилка тривог: ActionCable (UI), Push-сповіщення та SMS (для Critical Severity).

---

## 📐 III. Математичні модулі

* **`SilkenNet::Attractor`**: Математичне серце системи. Реалізація атрактора Лоренца для визначення біологічного гомеостазу.

$$\begin{cases} \dot{x} = \sigma(y - x) \\ \dot{y} = x(\rho - z) - y \\ \dot{z} = xy - \beta z \end{cases}$$



Використовується для ідентифікації стресу дерева через відхилення траєкторії $z$ у фазовому просторі.

---

## 🛡️ IV. Політика Безпеки

1. **Zero-Trust:** Кожен пакет шифрується AES-256 (Hardware-bound).
2. **Idempotency:** Всі фінансові воркери мають захист від повторного виконання (status guards/locking).
3. **Resilience:** Система підтримує 10+ ретраїв для Web3 операцій та 3–5 для апаратних команд.

> **Статус документа:** Актуально.
> **Версія системи:** 2.0.0-PRO (Genesis).
